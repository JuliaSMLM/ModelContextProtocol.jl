var documenterSearchIndex = {"docs":
[{"category":"section","location":"prompts/#MCP-Prompts","page":"Prompts","text":"Prompts are template-based messages that language models can use. Each prompt has a name, description, arguments, and message templates.","title":"MCP Prompts"},{"category":"section","location":"prompts/#Prompt-Structure","page":"Prompts","text":"Every prompt in ModelContextProtocol.jl is represented by the MCPPrompt struct:\n\nname: Unique identifier for the prompt\ndescription: Human-readable explanation of the prompt's purpose\narguments: List of parameters the prompt accepts\nmessages: Template messages with placeholders for arguments\ntitle: Optional human-friendly display name\nicons: Optional icons for UI display\n_meta: Optional metadata for protocol extensions, emitted verbatim in prompts/list\ncompletions: Optional completion/complete suggestion sources per argument name (see Argument Completion below)","title":"Prompt Structure"},{"category":"section","location":"prompts/#Creating-Prompts","page":"Prompts","text":"Here's how to create a basic prompt:\n\nNote: The role field uses the Role enum with values user and assistant. You may need to import these:\n\nimport ModelContextProtocol: user, assistant\n\ngreeting_prompt = MCPPrompt(\n    name = \"greeting\",\n    description = \"Personalized greeting message\",\n    arguments = [\n        PromptArgument(\n            name = \"name\",\n            description = \"User's name\",\n            required = true\n        ),\n        PromptArgument(\n            name = \"time_of_day\",\n            description = \"Morning, afternoon, or evening\",\n            required = false\n        )\n    ],\n    messages = [\n        PromptMessage(\n            role = user,\n            content = TextContent(\n                text = \"Hello! {?time_of_day?Good {time_of_day}}! My name is {name}.\"\n            )\n        )\n    ]\n)","title":"Creating Prompts"},{"category":"section","location":"prompts/#Arguments","page":"Prompts","text":"Prompt arguments are defined using the PromptArgument struct:\n\nname: Parameter identifier\ndescription: Explanation of the parameter\nrequired: Whether the argument must be provided (default: false)\ntitle: Optional human-friendly display name","title":"Arguments"},{"category":"section","location":"prompts/#Template-Syntax","page":"Prompts","text":"Prompt templates support parameter substitution and conditional blocks:\n\nBasic substitution: {parameter_name}\nConditional blocks: {?parameter_name?content if parameter exists}","title":"Template Syntax"},{"category":"section","location":"prompts/#Argument-Completion","page":"Prompts","text":"A prompt's completions field supplies completion/complete suggestions for its arguments, so clients can offer values as the user fills the prompt in. It maps an argument name to either a Vector{String} (served filtered by prefix against the partial value) or a function — value -> values, or (value, context_args) -> values to also receive the arguments already resolved for the request:\n\nreport_prompt = MCPPrompt(\n    name = \"city_report\",\n    description = \"Report on a city\",\n    arguments = [PromptArgument(name = \"city\", description = \"City name\", required = true)],\n    messages = [PromptMessage(content = TextContent(text = \"Report for {city}\"))],\n    completions = Dict{String,Any}(\"city\" => [\"paris\", \"london\", \"tokyo\"])\n)\n\nThe completion capability is advertised by default, and completion/complete is served in both protocol eras; suggestions are capped at 100 values with total/hasMore reported to the client. See the completion section of The Modern Era for the wire format, and Resources for the equivalent on resource-template variables.","title":"Argument Completion"},{"category":"section","location":"prompts/#Registering-Prompts","page":"Prompts","text":"Prompts can be registered with a server in two ways:\n\nDuring server creation:\n\nserver = mcp_server(\n    name = \"my-server\",\n    prompts = my_prompt  # Single prompt or vector of prompts\n)\n\nAfter server creation:\n\nregister!(server, my_prompt)","title":"Registering Prompts"},{"category":"section","location":"prompts/#Directory-Based-Organization","page":"Prompts","text":"Prompts can be organized in directory structures and auto-registered:\n\nmy_server/\n└── prompts/\n    ├── greeting.jl\n    └── faq.jl\n\nEach file must define exactly one MCPPrompt as the file's final expression — the loader registers the value of the last expression and ignores everything else defined in the file:\n\n# greeting.jl\nusing ModelContextProtocol\n\ngreeting_prompt = MCPPrompt(\n    name = \"greeting\",\n    description = \"Personalized greeting message\",\n    arguments = [\n        PromptArgument(name = \"name\", description = \"User's name\", required = true)\n    ],\n    messages = [\n        PromptMessage(\n            role = user,\n            content = TextContent(text = \"Hello! My name is {name}.\")\n        )\n    ]\n)\n\nThen auto-register from the directory:\n\nserver = mcp_server(\n    name = \"my-server\",\n    auto_register_dir = \"my_server\"\n)","title":"Directory-Based Organization"},{"category":"section","location":"prompts/#Advanced-Examples","page":"Prompts","text":"","title":"Advanced Examples"},{"category":"section","location":"prompts/#Multi-Message-Conversation-Prompt","page":"Prompts","text":"conversation_prompt = MCPPrompt(\n    name = \"code_review\",\n    description = \"Code review conversation template\",\n    arguments = [\n        PromptArgument(\n            name = \"language\",\n            description = \"Programming language\",\n            required = true\n        ),\n        PromptArgument(\n            name = \"code\",\n            description = \"Code to review\",\n            required = true\n        ),\n        PromptArgument(\n            name = \"focus_area\",\n            description = \"Specific area to focus on\",\n            required = false\n        )\n    ],\n    messages = [\n        PromptMessage(\n            role = user,\n            content = TextContent(\n                text = \"Please review this {language} code:{?focus_area? Focus on {focus_area}.}\"\n            )\n        ),\n        PromptMessage(\n            role = user,\n            content = TextContent(text = \"```{language}\\n{code}\\n```\")\n        ),\n        PromptMessage(\n            role = assistant,\n            content = TextContent(\n                text = \"I'll analyze this {language} code{?focus_area? with focus on {focus_area}}.\"\n            )\n        )\n    ]\n)","title":"Multi-Message Conversation Prompt"},{"category":"section","location":"prompts/#Prompt-with-Image-Content","page":"Prompts","text":"visual_prompt = MCPPrompt(\n    name = \"analyze_diagram\",\n    description = \"Analyze a diagram or chart\",\n    arguments = [\n        PromptArgument(name = \"image_path\", description = \"Path to image\", required = true),\n        PromptArgument(name = \"question\", description = \"Question about the image\", required = false)\n    ],\n    messages = [\n        PromptMessage(\n            role = user,\n            content = TextContent(\n                text = \"Please analyze this diagram{?question?: {question}}\"\n            )\n        ),\n        # Note: In practice, you'd load the actual image data\n        PromptMessage(\n            role = user,\n            content = ImageContent(\n                data = UInt8[],  # Placeholder for image bytes\n                mime_type = \"image/png\"\n            )\n        )\n    ]\n)","title":"Prompt with Image Content"},{"category":"section","location":"claude/#Using-with-Claude-Desktop","page":"Claude Desktop","text":"ModelContextProtocol.jl can be integrated with Anthropic's Claude Desktop application to allow Claude to access your custom tools, resources, and prompts.","title":"Using with Claude Desktop"},{"category":"section","location":"claude/#Setup-Instructions","page":"Claude Desktop","text":"Launch Claude Desktop application\nGo to File → Settings → Developer\nClick the Edit Config button\nA configuration file will open in your default editor","title":"Setup Instructions"},{"category":"section","location":"claude/#Configuration","page":"Claude Desktop","text":"To register your MCP servers with Claude, modify the configuration file with entries for each server.","title":"Configuration"},{"category":"section","location":"claude/#Important:-Full-Paths-Required","page":"Claude Desktop","text":"⚠️ You must use the full absolute path to your Julia project and scripts. The path should include:\n\nThe --project flag pointing to your ModelContextProtocol.jl directory\nThe full path to the script file","title":"Important: Full Paths Required"},{"category":"section","location":"claude/#Example-Configuration","page":"Claude Desktop","text":"{\n  \"mcpServers\": {\n    \"time\": {\n      \"command\": \"julia\",\n      \"args\": [\n        \"--project=/home/username/ModelContextProtocol\",\n        \"/home/username/ModelContextProtocol/examples/time_server.jl\"\n      ],\n      \"env\": {}\n    },\n    \"mcp_tools_example\": {\n      \"command\": \"julia\",\n      \"args\": [\n        \"--project=/home/username/ModelContextProtocol\",\n        \"/home/username/ModelContextProtocol/examples/reg_dir.jl\"\n      ],\n      \"env\": {}\n    },\n    \"http_server\": {\n      \"command\": \"npx\",\n      \"args\": [\"mcp-remote\", \"http://127.0.0.1:3000\", \"--allow-http\"],\n      \"env\": {}\n    }\n  }\n}","title":"Example Configuration"},{"category":"section","location":"claude/#Platform-Specific-Path-Examples","page":"Claude Desktop","text":"macOS/Linux:\n\n\"args\": [\n  \"--project=/home/username/julia_projects/ModelContextProtocol\",\n  \"/home/username/julia_projects/ModelContextProtocol/examples/time_server.jl\"\n]\n\nWindows:\n\n\"args\": [\n  \"--project=C:\\\\Users\\\\username\\\\Documents\\\\ModelContextProtocol\",\n  \"C:\\\\Users\\\\username\\\\Documents\\\\ModelContextProtocol\\\\examples\\\\time_server.jl\"\n]\n\nNote: Julia packages are typically cloned without the .jl extension. If you cloned via Pkg.dev(\"ModelContextProtocol\"), check your .julia/dev/ directory for the exact path.\n\nNote: On Windows, use double backslashes (\\\\) in JSON strings.\n\nFor each server entry:\n\n\"time\", \"mcp_tools_example\": Unique identifiers for your servers (you choose these names)\n\"command\": The command to run (should be \"julia\" for STDIO servers, \"npx\" for HTTP servers via mcp-remote)\n\"args\": Array of arguments:\nFirst argument: \"--project=/full/path/to/ModelContextProtocol\" (the package directory)\nSecond argument: Full path to your server script\n\"env\": Optional environment variables (can be empty {})","title":"Platform-Specific Path Examples"},{"category":"section","location":"claude/#Using-HTTP-Servers","page":"Claude Desktop","text":"For HTTP-based MCP servers, you need to:\n\nStart your HTTP server separately:\ncd /path/to/ModelContextProtocol\njulia --project=. examples/simple_http_server.jl\nOr with full paths:\njulia --project=/home/username/ModelContextProtocol /home/username/ModelContextProtocol/examples/simple_http_server.jl\nNote: The directory is typically ModelContextProtocol without .jl extension.\nConfigure Claude to connect via mcp-remote:\n\"http_server\": {\n  \"command\": \"npx\",\n  \"args\": [\"mcp-remote\", \"http://127.0.0.1:3000\", \"--allow-http\"]\n}\n\nNote: The --allow-http flag is required because the HTTP transport does not terminate TLS — it serves plain HTTP. To reach it over HTTPS, put it behind a TLS-terminating reverse proxy or tunnel; see Deployment.","title":"Using HTTP Servers"},{"category":"section","location":"claude/#Remote-and-Authenticated-Servers","page":"Claude Desktop","text":"The configuration above runs the server as a local subprocess (or reaches a local HTTP port through mcp-remote), which is why 127.0.0.1 works. Custom connectors are different: Claude Desktop and claude.ai connect to them from Anthropic's cloud, not from your device, so localhost, LAN, and VPN-only addresses cannot work — the server needs a publicly reachable HTTPS URL.\n\nDeployment — reaching a remote server: TLS front, reverse proxy, egress range.\nAuthentication — bearer-token validation once the server is exposed.","title":"Remote and Authenticated Servers"},{"category":"section","location":"claude/#Applying-Changes","page":"Claude Desktop","text":"For configuration changes to take effect:\n\nSave the configuration file\nClose all running Claude processes:\nOn Windows: Use Task Manager to end all Claude processes\nOn macOS: Quit the application\nRestart the Claude Desktop application","title":"Applying Changes"},{"category":"section","location":"claude/#Using-Your-MCP-Server","page":"Claude Desktop","text":"Once configured, you can tell Claude to use your MCP server:\n\nPlease connect to the MCP server named \"time\" and tell me the current time.\n\nClaude will connect to your server, discover available tools and resources, and use them to fulfill your requests.","title":"Using Your MCP Server"},{"category":"section","location":"claude/#Troubleshooting","page":"Claude Desktop","text":"If Claude cannot connect to your server:\n\nVerify full paths: Ensure you're using absolute paths for both --project and the script file\nCheck directory name: Julia packages are cloned without .jl extension (e.g., ModelContextProtocol not ModelContextProtocol.jl)\nCheck the server name: Must match exactly what's in your configuration\nVerify the paths:\nTest your command in a terminal first:\njulia --project=/your/full/path/ModelContextProtocol /your/full/path/ModelContextProtocol/examples/time_server.jl\nIf this works in terminal, it should work in Claude\nPrecompile dependencies: Run this first to avoid timeout issues:\ncd /path/to/ModelContextProtocol\njulia --project=. -e \"using Pkg; Pkg.instantiate(); using ModelContextProtocol\"\nCheck Claude's Developer Console:\nOpen Claude Desktop\nPress Cmd+Opt+I (Mac) or Ctrl+Shift+I (Windows/Linux)\nLook for error messages in the Console tab","title":"Troubleshooting"},{"category":"section","location":"claude/#Common-Issues","page":"Claude Desktop","text":"\"Package ModelContextProtocol not found\": The --project path is incorrect\n\"No such file or directory\": The script path is incorrect (check if directory has .jl extension or not)\nServer timeout: Julia compilation is slow on first run - precompile first\nWindows path issues: Remember to use double backslashes in JSON\nWrong directory name: Package directories typically don't include .jl (use ModelContextProtocol not ModelContextProtocol.jl)","title":"Common Issues"},{"category":"section","location":"deployment/#Deployment","page":"Deployment","text":"This guide is a reproducible recipe for exposing an authenticated remote MCP server to Claude clients. It assumes you have read Authentication and have a working HTTP server; here we cover the parts outside the Julia process — how Claude reaches your server, how to front it with TLS, and how to stand up the Authorization Server that mints the tokens your JWKSValidator verifies.\n\nEvery hostname, IP, and port below is a placeholder (example.org, RFC 5737 documentation addresses). Substitute your own.","title":"Deployment"},{"category":"section","location":"deployment/#First,-decide-who-connects-—-it-changes-everything","page":"Deployment","text":"The single most important fact, and the one most likely to waste an afternoon if you get it wrong: for Claude's remote connectors, the MCP requests do not come from your machine — they come from Anthropic's cloud.\n\nClient Data plane originates from Implication\nClaude Desktop / web / mobile / Cowork custom connectors Anthropic's cloud Server must be reachable over the public internet from Anthropic's egress range. LAN-only / VPN-only / localhost cannot work.\nclaude.ai account connectors surfaced in Claude Code Anthropic's cloud Same as above.\nClaude Code servers added with claude mcp add / .mcp.json (stdio or remote HTTP/SSE) your local machine Direct from the CLI; localhost, LAN, and VPN targets all work; no public exposure needed.\n\nThis is documented by Anthropic: a custom connector \"connects to your remote MCP server from Anthropic's cloud infrastructure, rather than from your local device,\" and a server \"behind a VPN, or blocked by a firewall won't connect, even if you can reach it from your own machine.\"\n\nConsequences for a connector deployment:\n\nThe server must have a public, TLS-terminated HTTPS URL.\n\"It works from my laptop\" proves nothing — test from off-network.\nAnthropic publishes a stable egress range for these outbound calls (160.79.104.0/21 at the time of writing; confirm against Anthropic's current IP addresses documentation). You can allowlist it (see Locking the data plane to Anthropic).\n\nIf your only consumer is Claude Code via claude mcp add, you can skip all of the public-fronting machinery and point it straight at http://localhost:PORT — but you still want auth if the port is reachable by anyone else.","title":"First, decide who connects — it changes everything"},{"category":"section","location":"deployment/#Architecture","page":"Deployment","text":"                          PUBLIC INTERNET (HTTPS only)\n  ┌───────────────┐                                   ┌──────────────────────┐\n  │ Claude client │ ── HTTPS ───────────────────────▶ │  TLS-terminating      │\n  │ (or Anthropic │   bearer token                    │  front:               │\n  │  cloud)       │                                   │  reverse proxy / tunnel│\n  └───────────────┘                                   └───────────┬──────────┘\n                                                                   │ plaintext, on\n                                                                   │ loopback or trusted LAN\n                          ┌────────────────────────────┬──────────┴──────────┐\n                          ▼                             ▼\n                ┌───────────────────┐        ┌──────────────────────┐\n                │ Authorization     │        │ MCP server           │\n                │ Server (Keycloak) │◀──────▶│ (ModelContextProtocol│\n                │ issues + signs    │  JWKS  │  validates tokens)    │\n                │ tokens            │        └──────────────────────┘\n                └───────────────────┘\n\nThe MCP server is an OAuth Resource Server: it never issues tokens, only validates them. Both the MCP server and the Authorization Server must be publicly reachable, but the traffic splits across parties — be precise about who calls what:\n\nthe user's browser hits the AS's authorize endpoint to log in;\nAnthropic's cloud exchanges the authorization code at the AS's token endpoint and makes the MCP requests (the data plane);\nthe MCP server itself fetches the AS's JWKS (and, for IntrospectionValidator, calls the AS) to verify tokens.\n\nJWKS is pulled by your server, not by Anthropic — which matters for firewalling (below).\n\nYou need a TLS front. Two tiers, by what you have available.","title":"Architecture"},{"category":"section","location":"deployment/#Tier-1-—-your-own-reverse-proxy-certificate-(recommended)","page":"Deployment","text":"Best when you control a host with a public IP (or a forwarded port) and can obtain a publicly-trusted certificate (e.g. Let's Encrypt). Lowest latency, and no third party ever terminates TLS on your tokens.\n\nTerminate TLS at a reverse proxy and route by path to the MCP server and the AS. Example with nginx, both behind one hostname:\n\n# inside the server { listen 443 ssl; server_name mcp.example.org; ... } block\n# (ssl_certificate / ssl_certificate_key from your ACME client)\n\n# --- MCP server ---\nlocation /mcp/ {\n    proxy_pass http://127.0.0.1:8080/;   # trailing slash strips /mcp/ ; server is at root\n    proxy_http_version 1.1;\n    proxy_set_header Host              $host;\n    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;\n    proxy_set_header X-Forwarded-Proto $scheme;\n    proxy_set_header Connection        \"\";      # keep-alive for the SSE stream\n    proxy_buffering off;                         # do NOT buffer Server-Sent Events\n    proxy_read_timeout 3600s;                    # long-lived SSE / Tasks\n}\nlocation = /mcp { return 308 /mcp/; }            # tolerate the no-trailing-slash form\n\n# --- Authorization Server (Keycloak, served under /auth) ---\nlocation /auth/ {\n    proxy_pass http://127.0.0.1:8447;            # NO trailing slash: preserve the /auth prefix\n    proxy_http_version 1.1;\n    proxy_set_header Host              $host;\n    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;\n    proxy_set_header X-Forwarded-Proto $scheme;\n    proxy_set_header X-Forwarded-Port  443;\n}\n\nwarning: Two `proxy_pass` slash conventions\nThe MCP block uses proxy_pass …:8080/ with a trailing slash so nginx strips the /mcp/ prefix (the Julia server serves at /). The Keycloak block uses …:8447 without a trailing slash so the /auth prefix is preserved (Keycloak is configured to serve under /auth). Mixing these up is the most common cause of 404s.\n\nwarning: Streamed responses need `proxy_buffering off`\nThe Streamable HTTP transport uses Server-Sent Events for notifications and Tasks. If the proxy buffers responses, the client never sees streamed events. Turn buffering off and set a long read timeout on the MCP location. The same settings are required for modern-era (2026-07-28) clients: their subscriptions/listen replaces the GET SSE stream with a long-lived POST response stream, which a buffering proxy or a short proxy_read_timeout will stall or cut just the same.\n\nIf you can add DNS records, dedicated subdomains (mcp.example.org, auth.example.org), each with its own server block, avoid the prefix-stripping subtlety entirely — at the cost of a certificate that covers both names.","title":"Tier 1 — your own reverse proxy + certificate (recommended)"},{"category":"section","location":"deployment/#Host-firewall-—-check-which-one-is-actually-active","page":"Deployment","text":"If the MCP server and reverse proxy are on different hosts, the proxy host must be allowed to reach the server's port. A trap we hit: systemctl is-active ufw reported active while ufw status was inactive and firewalld was the real enforcer. Verify the enforcing firewall, then scope an allow rule to the proxy's address. With firewalld:\n\nsudo firewall-cmd --permanent \\\n  --add-rich-rule='rule family=\"ipv4\" source address=\"192.0.2.10\" port port=\"8080\" protocol=\"tcp\" accept'\nsudo firewall-cmd --reload\n\nThe MCP server must also bind to an address the proxy can reach (0.0.0.0 or the LAN IP) — not 127.0.0.1 — when the proxy is on another host.","title":"Host firewall — check which one is actually active"},{"category":"section","location":"deployment/#Locking-the-data-plane-to-Anthropic","page":"Deployment","text":"Because connector traffic arrives only from Anthropic's published egress range, you can restrict the public MCP path to it as defense-in-depth. Do this at the layer that sees the data plane — at the proxy, per-path, since the rest of port 443 may serve other things:\n\nlocation /mcp/ {\n    allow 160.79.104.0/21;   # Anthropic egress (verify current range)\n    allow 192.0.2.0/24;      # your own test network, optional\n    deny all;\n    # ... proxy_pass etc. as above ...\n}\n\nnote: Scope the allowlist to the MCP path only\nRestrict the allowlist to the MCP data plane (/mcp). Do not blanket-allowlist /auth: its authorize endpoint is hit by the user's browser (not Anthropic), and its JWKS endpoint must stay reachable from your own MCP server — which is what fetches the keys. (The AS's token endpoint is hit by Anthropic during the code exchange.) Allowlisting /auth to Anthropic's range would break both browser login and your server's key fetch.","title":"Locking the data plane to Anthropic"},{"category":"section","location":"deployment/#Tier-2-—-Cloudflare-named-tunnel","page":"Deployment","text":"Best when you have no public IP, are behind CGNAT, cannot open inbound ports, or want to hide your origin address. A cloudflared tunnel makes an outbound connection to Cloudflare's edge, which fronts a public HTTPS hostname for you.\n\nUse a named tunnel (requires a free Cloudflare account and a domain on Cloudflare), not a quick tunnel — quick tunnels hand out a new random hostname on every restart, which forces you to re-edit every callback URL each time.\nCloudflare terminates TLS at its edge, so Cloudflare sees your tokens in plaintext there. That is the trade for not exposing an origin; if it is unacceptable, use Tier 1.\nLatency is comparable to Tier 1 for connector traffic (the client is Anthropic's cloud either way), so choose on reachability and trust, not speed.","title":"Tier 2 — Cloudflare named tunnel"},{"category":"section","location":"deployment/#The-Authorization-Server","page":"Deployment","text":"Any standards-compliant OAuth 2.x / OIDC server works. Keycloak is used here because it is free, self-hostable, supports identity brokering, and — unlike a bare OAuth app — implements the discovery and PKCE that Claude's connector flow expects. Do not write your own.","title":"The Authorization Server"},{"category":"section","location":"deployment/#Minimal-realm","page":"Deployment","text":"Create a realm (e.g. main).\nCreate a user, or federate one (below).\nRegister a public client for Claude (PKCE, no secret):\nredirect URIs: for setup, https://claude.ai/* and https://claude.com/*. Wildcards are a testing shortcut only — they widen redirect-abuse risk; in production register the exact Claude callback URL(s) and drop the wildcards.\nIn Claude Desktop, this client id goes in Advanced → OAuth Client ID. Claude's connectors accept a pre-registered client id, so you do not need Dynamic Client Registration.\nAdd an audience mapper so issued tokens carry aud = https://mcp.example.org/mcp — this must equal your OAuthConfig.audience.\nAdd a client scope (e.g. mcp:read) that appears in the token's scope claim.\n\nwarning: Keycloak behind a sub-path\nIf Keycloak is served under /auth (as in the nginx example), it must both serve there and advertise URLs there, or the issuer in its tokens won't match what your server expects. Start it with both:--http-relative-path=/auth  --hostname=https://mcp.example.org/auth  --proxy-headers=xforwardedSetting --hostname to the bare origin (no /auth) makes Keycloak advertise an issuer without the path, and token validation then fails on iss. Verify with:curl -s https://mcp.example.org/auth/realms/main/.well-known/openid-configuration | jq .issuer\n# → \"https://mcp.example.org/auth/realms/main\"","title":"Minimal realm"},{"category":"section","location":"deployment/#Optional:-GitHub-identity-federation","page":"Deployment","text":"To let users sign in with GitHub (so your allowlist is GitHub usernames), add GitHub as an identity provider in Keycloak — the MCP server still just sees ordinary signed JWTs.\n\nCreate a GitHub OAuth App (identity scopes only).\nSet its Authorization callback URL to Keycloak's broker endpoint: https://mcp.example.org/auth/realms/main/broker/github/endpoint.\nAdd a GitHub identity provider in Keycloak with that app's client id/secret.\n\nwarning: The callback URL is GitHub's, not Claude's\n…/broker/github/endpoint goes in the GitHub App's callback field, never in Claude Desktop's OAuth Client ID field (that takes the Keycloak client id). And if Keycloak's public hostname ever changes, this callback must be updated — a stale value yields GitHub's \"The redirect_uri is not associated with this application\" error, which is a GitHub error, not a Keycloak one.\n\nnote: Brokered usernames are normalized\nKeycloak lowercases federated usernames, so GitHub login Alice arrives as alice in the token. Allowlist username matching is case-insensitive by default (AuthMiddleware's case_insensitive_allowlist), so either case in your allowlist works; set it false for exact matching and then use the lowercase form (or inspect a real token's preferred_username).","title":"Optional: GitHub identity federation"},{"category":"section","location":"deployment/#Wiring-the-MCP-server","page":"Deployment","text":"The Julia side is small once the AS exists — it is the example from Authentication, with the URLs lined up:\n\nusing ModelContextProtocol\n\nissuer   = \"https://mcp.example.org/auth/realms/main\"\nresource = \"https://mcp.example.org/mcp\"\n\nauth = create_auth_middleware(\n    OAuthConfig(issuer = issuer, audience = resource, required_scopes = [\"mcp:read\"]),\n    validator = JWKSValidator(\"$issuer/protocol/openid-connect/certs\"),\n    allowlist = Set([\"alice\"]),   # GitHub usernames (matched case-insensitively by default)\n)\nmeta = ProtectedResourceMetadata(\n    resource = resource, authorization_servers = [issuer], scopes_supported = [\"mcp:read\"])\n\nserver = mcp_server(name = \"secure-server\", tools = [...])\n# bind to 0.0.0.0 if the reverse proxy is on another host; 127.0.0.1 if co-located\nserver.transport = HttpTransport(host = \"0.0.0.0\", port = 8080, auth = auth, resource_metadata = meta)\nconnect(server.transport)\nstart!(server)\n\nnote: Per-tool scopes must be issued and advertised\nrequired_scopes = [\"mcp:read\"] above is the server-wide baseline. If you gate individual tools with MCPTool(required_scopes = [\"mcp:write\"]), the AS must actually issue mcp:write in the token, and you should list it in ProtectedResourceMetadata(scopes_supported = [...]) so clients can discover it — per-tool scopes are not advertised through tools/list.\n\nThe three URLs that must agree:\n\nValue Set in Must equal\nissuer OAuthConfig.issuer the AS's advertised issuer (check its discovery doc)\naudience OAuthConfig.audience the AS audience-mapper value, and your public MCP URL\nJWKS URL JWKSValidator(...) $issuer/protocol/openid-connect/certs (Keycloak)","title":"Wiring the MCP server"},{"category":"section","location":"deployment/#Verifying-end-to-end","page":"Deployment","text":"Before involving Claude, prove the chain with curl. Mint a token from the AS — a direct-grant (Resource Owner Password) client is convenient for local verification only. OAuth 2.1 discourages that grant, so disable it in production and rely on the authorization-code + PKCE flow Claude actually uses. Call the server through the public URL:\n\n# 1. mint a token (direct grant; needs a test client with that flow enabled)\nTOKEN=$(curl -s -X POST \\\n  https://mcp.example.org/auth/realms/main/protocol/openid-connect/token \\\n  -d grant_type=password -d client_id=test-cli -d client_secret=… \\\n  -d username=alice -d password=… | jq -r .access_token)\n\n# 2. no token → 401 with a resource_metadata pointer\ncurl -s -o /dev/null -w '%{http_code}\\n' -X POST https://mcp.example.org/mcp/ \\\n  -H 'Content-Type: application/json' -d '{\"jsonrpc\":\"2.0\",\"method\":\"tools/list\",\"id\":1}'   # → 401\n\n# 3. valid token → initialize succeeds\ncurl -s -X POST https://mcp.example.org/mcp/ \\\n  -H \"Authorization: Bearer $TOKEN\" -H 'Content-Type: application/json' \\\n  -H 'Accept: application/json, text/event-stream' \\\n  -d '{\"jsonrpc\":\"2.0\",\"method\":\"initialize\",\"params\":{\"protocolVersion\":\"2025-11-25\",\"capabilities\":{},\"clientInfo\":{\"name\":\"curl\",\"version\":\"1\"}},\"id\":1}'\n\n# 4. modern-era clients never send `initialize` — verify the chain for them with a\n#    stateless `_meta`-carrying request instead (see The Modern Era for the details)\ncurl -s -X POST https://mcp.example.org/mcp/ \\\n  -H \"Authorization: Bearer $TOKEN\" -H 'Content-Type: application/json' \\\n  -H 'Accept: application/json, text/event-stream' \\\n  -H 'MCP-Protocol-Version: 2026-07-28' -H 'Mcp-Method: tools/list' \\\n  -d '{\"jsonrpc\":\"2.0\",\"method\":\"tools/list\",\"params\":{\"_meta\":{\"io.modelcontextprotocol/protocolVersion\":\"2026-07-28\",\"io.modelcontextprotocol/clientCapabilities\":{}}},\"id\":2}'\n\nDecode the token (jwt payload) and confirm iss, aud, and scope match your config — a 401 after a clean mint is almost always one of those three disagreeing.\n\nThen add it in Claude Desktop: Settings → Connectors → Add custom connector, URL https://mcp.example.org/mcp/, Advanced → OAuth Client ID = your Keycloak client id.\n\nnote: Benign log noise\nConnector clients open and close connections frequently; the HTTP transport may log EOFError/broken-pipe on those closes. These are harmless connection teardowns, not request failures — confirm health with a successful tool call, not by the absence of such log lines.","title":"Verifying end-to-end"},{"category":"section","location":"deployment/#Security-checklist","page":"Deployment","text":"TLS terminates on infrastructure you trust (Tier 1) or you accept Cloudflare as the terminator (Tier 2). Never expose the plaintext MCP port publicly.\nissuer and audience are set and verified against the AS's real discovery document.\nThe AS is a battle-tested product, not hand-rolled. The MCP server only validates.\nTokens are never logged, and never forwarded to application upstreams. (A validator may send the token to its configured introspection/JWKS AS endpoint, or to GitHub — that is the validation call itself, not a pass-through to your downstream APIs.)\nOptionally allowlist Anthropic's egress range on the MCP data path.\nRe-test from off your network — the connector data plane is not your laptop.","title":"Security checklist"},{"category":"section","location":"auto-registration/#Auto-Registration-System","page":"Auto-Registration","text":"ModelContextProtocol.jl provides an auto-registration system that automatically loads and registers MCP components (tools, prompts, and resources) from a directory structure. This enables clean organization of large MCP servers with many components.","title":"Auto-Registration System"},{"category":"section","location":"auto-registration/#Overview","page":"Auto-Registration","text":"Instead of manually registering each component, you can organize them in directories and let the system auto-register them:\n\nserver = mcp_server(\n    name = \"my-large-server\",\n    version = \"1.0.0\",\n    auto_register_dir = \"path/to/components\"\n)\n\nThe system will automatically scan for and register all components found in the directory structure.","title":"Overview"},{"category":"section","location":"auto-registration/#Directory-Structure","page":"Auto-Registration","text":"The auto-registration system expects a specific directory layout:\n\ncomponents/\n├── tools/          # MCPTool definitions, one per file\n│   ├── calculator.jl\n│   ├── file_reader.jl\n│   └── statistics.jl\n├── prompts/        # MCPPrompt definitions, one per file\n│   ├── analysis.jl\n│   └── code_review.jl\n└── resources/      # MCPResource definitions, one per file\n    ├── config.jl\n    └── docs.jl\n\nExactly three subdirectory names are scanned — tools/, resources/, and prompts/. Any other directory in the component root is ignored, and the scan is not recursive: only .jl files sitting directly in one of those three directories are loaded, never files in nested subdirectories.\n\nResourceTemplates have no auto-registration directory. Pass them at server creation with mcp_server(resource_templates = [...]) or add them later with register!(server, template).","title":"Directory Structure"},{"category":"section","location":"auto-registration/#Component-File-Format","page":"Auto-Registration","text":"","title":"Component File Format"},{"category":"section","location":"auto-registration/#Tool-Files-(tools/)","page":"Auto-Registration","text":"Each file must define exactly one MCPTool, and it must be the file's final expression. The loader evaluates the file in a fresh module and registers the value the file returns — anything defined earlier is available as a helper but is never registered, so a second tool in the same file is silently dropped. Give each tool its own file:\n\n# tools/calculator.jl\nusing ModelContextProtocol\n\n# Simple calculator tool\ncalculator_tool = MCPTool(\n    name = \"calculate\",\n    description = \"Perform basic arithmetic calculations\",\n    parameters = [\n        ToolParameter(\n            name = \"expression\",\n            type = \"string\",\n            description = \"Mathematical expression to evaluate\",\n            required = true\n        )\n    ],\n    handler = function(params)\n        expr = params[\"expression\"]\n        try\n            result = eval(Meta.parse(expr))\n            return TextContent(text = \"Result: $result\")\n        catch e\n            return TextContent(text = \"Error: $e\")\n        end\n    end\n)\n\n# tools/statistics.jl\nusing ModelContextProtocol\n\n# Statistics tool\nstats_tool = MCPTool(\n    name = \"statistics\",\n    description = \"Calculate statistics for a dataset\",\n    parameters = [\n        ToolParameter(\n            name = \"data\",\n            type = \"array\",\n            description = \"Array of numbers\",\n            required = true\n        )\n    ],\n    handler = function(params)\n        data = params[\"data\"]\n        mean_val = sum(data) / length(data)\n        return TextContent(text = \"Mean: $mean_val\")\n    end\n)","title":"Tool Files (tools/)"},{"category":"section","location":"auto-registration/#Prompt-Files-(prompts/)","page":"Auto-Registration","text":"Define one MCPPrompt per file, again as the file's final expression:\n\n# prompts/analysis.jl\nusing ModelContextProtocol\n\ndata_analysis_prompt = MCPPrompt(\n    name = \"analyze_data\",\n    description = \"Analyze a dataset and provide insights\",\n    arguments = [\n        PromptArgument(\n            name = \"dataset\",\n            description = \"Description of the dataset to analyze\",\n            required = true\n        ),\n        PromptArgument(\n            name = \"focus_areas\",\n            description = \"Specific areas to focus analysis on\",\n            required = false\n        )\n    ],\n    messages = [\n        PromptMessage(\n            role = ModelContextProtocol.user,\n            content = TextContent(\n                text = \"\"\"Analyze the following dataset: {dataset}\n                \n{?focus_areas?Focus particularly on: {focus_areas}}\n\nProvide:\n1. Key insights and patterns\n2. Statistical summary  \n3. Recommendations for further analysis\"\"\"\n            )\n        )\n    ]\n)","title":"Prompt Files (prompts/)"},{"category":"section","location":"auto-registration/#Resource-Files-(resources/)","page":"Auto-Registration","text":"Define one MCPResource per file for data access, as the file's final expression:\n\n# resources/config.jl\nusing ModelContextProtocol\nusing URIs\n\napp_config_resource = MCPResource(\n    uri = URI(\"config://app/settings\"),\n    name = \"Application Configuration\",\n    description = \"Current application settings and configuration\",\n    mime_type = \"application/json\",\n    data_provider = function()\n        return Dict(\n            \"version\" => \"1.0.0\",\n            \"debug_mode\" => false,\n            \"max_connections\" => 100,\n            \"features\" => [\n                \"auto_registration\",\n                \"http_transport\",\n                \"sse_streaming\"\n            ]\n        )\n    end\n)","title":"Resource Files (resources/)"},{"category":"section","location":"auto-registration/#Usage-Examples","page":"Auto-Registration","text":"","title":"Usage Examples"},{"category":"section","location":"auto-registration/#Basic-Auto-Registration","page":"Auto-Registration","text":"using ModelContextProtocol\n\n# Create server with auto-registration\nserver = mcp_server(\n    name = \"my-server\",\n    version = \"1.0.0\",\n    auto_register_dir = joinpath(@__DIR__, \"mcp_components\")\n)\n\n# Start server (all components are automatically registered)\nstart!(server)","title":"Basic Auto-Registration"},{"category":"section","location":"auto-registration/#With-HTTP-Transport","page":"Auto-Registration","text":"using ModelContextProtocol\nusing ModelContextProtocol: HttpTransport\n\n# Create server with auto-registration\nserver = mcp_server(\n    name = \"http-server\",\n    auto_register_dir = \"components\"\n)\n\n# Add HTTP transport\ntransport = HttpTransport(port = 3000)\nserver.transport = transport\nModelContextProtocol.connect(transport)\n\nstart!(server)","title":"With HTTP Transport"},{"category":"section","location":"auto-registration/#Manual-Registration-Combined","page":"Auto-Registration","text":"You can combine auto-registration with manual registration:\n\n# Manual tool\nmanual_tool = MCPTool(\n    name = \"special_tool\",\n    description = \"Manually registered tool\",\n    parameters = [],\n    handler = params -> TextContent(text = \"Hello from manual tool!\")\n)\n\n# Server with both auto and manual registration\nserver = mcp_server(\n    name = \"mixed-server\",\n    tools = [manual_tool],  # Manual registration\n    auto_register_dir = \"components\"  # Auto registration\n)","title":"Manual Registration Combined"},{"category":"section","location":"auto-registration/#Shared-State","page":"Auto-Registration","text":"Components loaded via auto-registration can share state through the global Main.storage dictionary:\n\n# tools/store_data.jl\nif !isdefined(Main, :storage)\n    @eval Main storage = Dict{String, Any}()\nend\n\n# Tool that stores data\nstore_tool = MCPTool(\n    name = \"store_data\",\n    description = \"Store data in shared storage\",\n    parameters = [\n        ToolParameter(name = \"key\", type = \"string\",\n                      description = \"Key to store the value under\", required = true),\n        ToolParameter(name = \"value\", type = \"string\",\n                      description = \"Value to store\", required = true)\n    ],\n    handler = function(params)\n        Main.storage[params[\"key\"]] = params[\"value\"]\n        return TextContent(text = \"Data stored successfully\")\n    end\n)\n\n# tools/get_data.jl\nif !isdefined(Main, :storage)\n    @eval Main storage = Dict{String, Any}()\nend\n\n# Tool that retrieves data\nget_tool = MCPTool(\n    name = \"get_data\", \n    description = \"Get data from shared storage\",\n    parameters = [\n        ToolParameter(name = \"key\", type = \"string\",\n                      description = \"Key to look up\", required = true)\n    ],\n    handler = function(params)\n        value = get(Main.storage, params[\"key\"], \"Not found\")\n        return TextContent(text = \"Value: $value\")\n    end\n)","title":"Shared State"},{"category":"section","location":"auto-registration/#Best-Practices","page":"Auto-Registration","text":"","title":"Best Practices"},{"category":"section","location":"auto-registration/#Organization-Strategies","page":"Auto-Registration","text":"By Domain: Group related functionality\ncomponents/\n├── tools/\n│   ├── database_tools.jl\n│   ├── file_tools.jl\n│   └── api_tools.jl\nBy Naming Prefix: Convey grouping in the file name, since subdirectories under tools/, resources/, and prompts/ are never scanned — every component file must sit directly in one of those three directories\ncomponents/\n├── tools/\n│   ├── basic_math.jl\n│   ├── basic_text.jl\n│   ├── advanced_ml_analysis.jl\n│   └── advanced_data_processing.jl\nBy Team: Organize by development team\ncomponents/\n├── tools/\n│   ├── backend_team.jl\n│   ├── frontend_team.jl\n│   └── data_team.jl","title":"Organization Strategies"},{"category":"section","location":"auto-registration/#Component-Design","page":"Auto-Registration","text":"Single Responsibility: Each tool should have a clear, focused purpose\nError Handling: Always handle errors gracefully in tool handlers\nDocumentation: Use clear descriptions and parameter documentation\nReturn Types: Be explicit about return types for better error detection\n\nmy_tool = MCPTool(\n    name = \"well_designed_tool\",\n    description = \"Clear description of what this tool does\",\n    parameters = [\n        ToolParameter(\n            name = \"input\",\n            type = \"string\",\n            description = \"Detailed description of this parameter\",\n            required = true\n        )\n    ],\n    handler = function(params)\n        try\n            # Tool logic here\n            result = process(params[\"input\"])\n            return TextContent(text = result)\n        catch e\n            return TextContent(text = \"Error: $(string(e))\")\n        end\n    end,\n    return_type = TextContent  # Explicit return type\n)","title":"Component Design"},{"category":"section","location":"auto-registration/#Scaling-Considerations","page":"Auto-Registration","text":"Module Isolation: Each component file runs in its own module to avoid conflicts\nStartup Time: Large numbers of components may increase server startup time\nMemory Usage: Monitor memory usage with many auto-registered components\nError Isolation: One broken component file won't prevent others from loading","title":"Scaling Considerations"},{"category":"section","location":"auto-registration/#Troubleshooting","page":"Auto-Registration","text":"","title":"Troubleshooting"},{"category":"section","location":"auto-registration/#Common-Issues","page":"Auto-Registration","text":"Components Not Loading\nCheck file permissions (files must be readable)\nVerify directory structure matches expected layout\nCheck for syntax errors in component files\nImport Errors\nThe auto-registration system automatically imports ModelContextProtocol\nAvoid conflicting using statements in component files\nUse fully qualified names for other packages\nVariable Name Conflicts\nEach component runs in its own module, so variable names are isolated\nGlobal state should use Main.storage, not module-level variables","title":"Common Issues"},{"category":"section","location":"auto-registration/#Debugging","page":"Auto-Registration","text":"Enable verbose logging to see what components are being registered:\n\nusing Logging\nglobal_logger(ConsoleLogger(Logging.Debug))\n\nserver = mcp_server(\n    name = \"debug-server\",\n    auto_register_dir = \"components\"\n)\n\nYou'll see messages like:\n\n[ Info: Registered MCPTool from /path/to/components/tools/calculator.jl\n[ Info: Registered MCPPrompt from /path/to/components/prompts/analysis.jl\n\nA file whose final expression is not the type expected for its directory is skipped with a warning — a stray trailing statement after the component is the usual cause:\n\n┌ Warning: File /path/to/components/tools/helpers.jl did not return a MCPTool (got Nothing)\n\nNote that a file defining two components produces no warning at all: the last one is registered and the earlier one is dropped silently, which is why one component per file matters.","title":"Debugging"},{"category":"section","location":"auto-registration/#Complete-Example","page":"Auto-Registration","text":"See examples/reg_dir.jl and examples/reg_dir_http.jl for complete working examples of auto-registration with both stdio and HTTP transports.","title":"Complete Example"},{"category":"section","location":"oauth/#Authentication","page":"Authentication","text":"This page documents how ModelContextProtocol.jl authenticates HTTP requests. It is precise about what the package does and — just as importantly — what it does not do, because the security of your server depends on understanding the boundary.","title":"Authentication"},{"category":"section","location":"oauth/#What-this-package-is,-and-is-not","page":"Authentication","text":"The package implements an OAuth 2.1 Resource Server (RS) for the Streamable HTTP transport, per the MCP authorization specification — it guards the endpoint itself, so 2025-11-25 legacy sessions and 2026-07-28 modern requests are validated alike:\n\nIt validates bearer tokens presented on incoming requests (signature and/or claims, or remote introspection).\nIt advertises where tokens come from via RFC 9728 Protected Resource Metadata.\nIt attaches the authenticated principal to each request so tool handlers can make authorization decisions.\n\nIt is not an Authorization Server (AS). It does not issue tokens, run login or consent screens, implement Dynamic Client Registration, or handle PKCE redirects. Those are the job of an external AS — Keycloak, Auth0, Okta, an in-house IdP, or GitHub. The Deployment guide walks through standing one up end-to-end. The division is deliberate: a Resource Server is a small, auditable surface; an Authorization Server is thousands of lines of security-critical token machinery that you should not hand-roll.\n\nThe stdio transport is unauthenticated by design — it is a local subprocess pipe, so the trust boundary is the operating system, not a token. Everything below concerns the HTTP transport.","title":"What this package is, and is not"},{"category":"section","location":"oauth/#The-request-lifecycle","page":"Authentication","text":"When auth is configured on the HTTP transport, every request (except the public metadata endpoint, below) goes through authenticate_request:\n\nExtract the bearer token from the Authorization: Bearer <token> header. A missing header → 401; a malformed header → 401.\nValidate the token with the configured TokenValidator. What this checks depends on the validator (signature, claims, or remote introspection).\nAllowlist (optional): if an allowlist is set, the authenticated principal's username or subject must be in it, otherwise 403.\nOn success, the AuthenticatedUser is carried to the tool handler as ctx.authenticated_user.\n\nTransport-level authentication failures never reveal why a token was rejected (expired vs. wrong issuer vs. bad signature vs. a missing server-wide scope). The client receives a fixed, generic OAuth error so the endpoint cannot be used as a token/policy oracle; the specific reason is retained only for server-side logging. (Per-tool required_scopes are different: they are checked after authentication, and the -32004 error does name the missing scope — see Per-tool authorization.)\n\nOutcome HTTP status WWW-Authenticate\nNo token 401 Bearer resource_metadata=\"…\"\nInvalid/expired/forged token 401 Bearer error=\"invalid_token\", …\nValid token, missing scope or not allowlisted 403 Bearer error=\"insufficient_scope\", …\n\nWhen resource_metadata is configured on the transport (strongly recommended whenever auth is enabled), the WWW-Authenticate header points the client at the Protected Resource Metadata document (below) — how a compliant client discovers your Authorization Server and starts a token flow. Without it the header still signals the error but carries no discovery pointer.","title":"The request lifecycle"},{"category":"section","location":"oauth/#Wiring-auth-into-the-HTTP-transport","page":"Authentication","text":"Two keyword arguments on the HTTP transport turn auth on:\n\nusing ModelContextProtocol\n\nauth = create_auth_middleware(\n    OAuthConfig(\n        issuer   = \"https://auth.example.org/realms/main\",   # expected `iss`\n        audience = \"https://mcp.example.org/mcp\",             # expected `aud`\n        required_scopes = [\"mcp:read\"],                       # required on every request\n    ),\n    validator = JWKSValidator(\"https://auth.example.org/realms/main/protocol/openid-connect/certs\"),\n    allowlist = Set([\"alice\", \"bob\"]),                        # optional\n)\n\nmeta = ProtectedResourceMetadata(\n    resource = \"https://mcp.example.org/mcp\",\n    authorization_servers = [\"https://auth.example.org/realms/main\"],\n    scopes_supported = [\"mcp:read\"],\n)\n\nserver = mcp_server(name = \"secure-server\", tools = [...])\nserver.transport = HttpTransport(host = \"127.0.0.1\", port = 8080,\n                                 auth = auth, resource_metadata = meta)\nconnect(server.transport)\nstart!(server)\n\nauth::Union{AuthMiddleware,Nothing} — nothing (the default) disables auth.\nresource_metadata::Union{ProtectedResourceMetadata,Nothing} — served, unauthenticated, at the transport's /.well-known/oauth-protected-resource path (clients must read it before they have a token). Note the distinction between that backend path and the advertised URL: the URL in WWW-Authenticate is built from the metadata's resource field — <resource>/.well-known/oauth-protected-resource — so with resource = \"https://mcp.example.org/mcp\" clients are pointed at https://mcp.example.org/mcp/.well-known/oauth-protected-resource. Make sure your proxy routes that public path back to the transport (see Deployment).\n\nwarning: Bind address vs. exposure\nHttpTransport does not terminate TLS and does no network-level access control. Put it behind a TLS-terminating reverse proxy (see Deployment) and bind it to 127.0.0.1 (or a LAN address the proxy can reach), never directly to a public interface.","title":"Wiring auth into the HTTP transport"},{"category":"section","location":"oauth/#OAuthConfig","page":"Authentication","text":"OAuthConfig describes what a valid token must look like, independent of how it is verified:\n\nOAuthConfig(;\n    issuer::String,                                   # expected `iss` claim\n    audience::String,                                 # expected `aud` claim (your resource)\n    required_scopes::Vector{String} = String[],       # all must be present on every request\n    jwks_uri::Union{String,Nothing} = nothing,        # informational\n    introspection_endpoint::Union{String,Nothing} = nothing,  # used by IntrospectionValidator\n)\n\nissuer and audience are fail-closed: when set, a token that lacks a matching iss/aud is rejected (a forged token cannot pass by simply omitting the claim). audience matches a string aud exactly, or membership when aud is an array. Leave a field empty (\"\") only if you deliberately want to skip that check.\n\nrequired_scopes is checked on every request by the claims-based validators — JWTValidator, JWKSValidator, and IntrospectionValidator. JWTValidator/JWKSValidator read the JWT's scope (space-delimited string) or scp (array) claim; IntrospectionValidator reads the scope field of the RFC 7662 introspection response. The SimpleTokenValidator and GitHubOAuthValidator do not consult OAuthConfig.required_scopes; with those, enforce authorization through the allowlist or in your handlers. For per-tool scopes (a write tool that needs mcp:write while reads need only mcp:read), declare MCPTool(required_scopes = [...]) — see Per-tool authorization below.","title":"OAuthConfig"},{"category":"section","location":"oauth/#The-validator-ladder","page":"Authentication","text":"A TokenValidator decides whether a token is genuine. Pick by token type and trust model — they are ordered here from least to most appropriate for tokens from an external issuer.\n\nValidator Token type Verifies Use when\nSimpleTokenValidator opaque string static map lookup dev / trusted static API keys\nJWTValidator JWT claims only — no signature dev/test, or behind a gateway that already verified the signature\nJWKSValidator JWT signature (JWKS) + claims tokens from an external AS — recommended\nIntrospectionValidator opaque or JWT remote call to the AS (RFC 7662) opaque tokens you can't verify locally\nGitHubOAuthValidator GitHub access token GitHub /user API call authenticating GitHub users directly","title":"The validator ladder"},{"category":"section","location":"oauth/#JWKSValidator-—-signature-verification-(recommended)","page":"Authentication","text":"JWKSValidator(jwks_uri::String;\n              allowed_algs = [\"RS256\", \"RS384\", \"RS512\"],\n              clock_skew_seconds = 60,\n              refresh_interval_seconds = 300,\n              allow_insecure_http = false)\nJWKSValidator(keyset::JWTs.JWKSet; kwargs...)   # pre-built / static key set\n\nVerifies the token's RSA signature against the issuer's JSON Web Key Set (RFC 7517), then applies the same fail-closed claim checks as JWTValidator. This is the correct choice for any token minted by an external Authorization Server. Security-relevant behavior, in order:\n\nAlgorithm allowlist, before any cryptography. A token whose header alg is not in allowed_algs is rejected immediately. This is what blocks the classic alg=none bypass and the RS256→HS256 key-confusion attack. The default permits the RSA family only; do not add HS* (HMAC) algorithms for keys published in a public JWKS.\nkid required. Tokens without a key id are rejected — the validator never guesses which key to use.\nLazy, rate-limited key loading. Construction never touches the network, so the server starts even while the AS is down (requests fail closed until keys load). An unknown kid triggers at most one JWKS re-fetch per refresh_interval_seconds (default 300), so an attacker spraying random kid values cannot hammer the JWKS endpoint. Fetches use bounded timeouts, a 1 MB response cap, and never hold the validator's lock during network I/O.\nPlaintext rejected. An http:// JWKS URL is refused at construction (a network attacker could swap in their own signing key) unless you pass allow_insecure_http = true for localhost/testing. https:// and file:// URLs, and a directly-injected JWTs.JWKSet, are supported.\nMalformed upstream fails closed. A garbage or oversized JWKS document fails authentication while retaining the previously cached keys, rather than erroring the request or dropping all keys.\n\nclock_skew_seconds (default 60) is the tolerance applied to exp/nbf: a token is only rejected once it is more than clock_skew_seconds past its expiry. This is standard practice for cross-host clock drift; lower it if your RS and AS share a clock.","title":"JWKSValidator — signature verification (recommended)"},{"category":"section","location":"oauth/#JWTValidator-—-claims-only-(no-signatures)","page":"Authentication","text":"# `insecure_skip_signature_verification` defaults to `false`, and the constructor THROWS\n# unless you pass `true` — an explicit acknowledgement that signatures are not verified:\nJWTValidator(insecure_skip_signature_verification = true, clock_skew_seconds = 60)\n\ndanger: No signature verification — explicit opt-in required\nJWTValidator decodes and validates JWT claims but does not verify the cryptographic signature. Because the signature is unchecked, any caller can forge a token carrying the expected iss/aud/scopes — \"trusting the issuer\" is not, by itself, enough (it rejects alg=none, but that is no substitute for verification). To prevent accidental insecure deployment it refuses to construct unless you pass insecure_skip_signature_verification = true. Use it only in development/testing, or when a trusted component in front of the server (e.g. a gateway) has already verified the signature. For tokens that arrive directly from a client, use JWKSValidator.","title":"JWTValidator — claims only (no signatures)"},{"category":"section","location":"oauth/#IntrospectionValidator-—-RFC-7662","page":"Authentication","text":"IntrospectionValidator(; client_id = nothing, client_secret = nothing)\n\nPOSTs the token to the AS's introspection endpoint (OAuthConfig.introspection_endpoint) and trusts the AS's verdict. Appropriate for opaque tokens that cannot be validated locally. It enforces active == true, and binds iss/aud fail-closed when those are configured (so a token active at a shared AS but minted for a different resource cannot be replayed against yours). A numeric exp in the response, if present, must be in the future — but the validator does not require the AS to return exp. Each request is a network round-trip to the AS.","title":"IntrospectionValidator — RFC 7662"},{"category":"section","location":"oauth/#GitHub","page":"Authentication","text":"auth = create_github_auth(;\n    allowed_users = [\"alice\", \"bob\"],   # GitHub logins; empty = any authenticated user\n    required_org  = \"JuliaSMLM\",         # optional: require *active* org membership\n    cache_ttl_seconds = 300,\n    case_insensitive_allowlist = true)   # fold username case (default)\n\nValidates a GitHub access token by calling GitHub's /user API, with a short-lived cache. required_org additionally requires active (not pending) membership in the named organization. This authenticates GitHub users directly against the package — a different model from brokering GitHub through an Authorization Server (which is what the Deployment guide does, and which the package sees as ordinary JWTs).","title":"GitHub"},{"category":"section","location":"oauth/#Assembling-the-middleware","page":"Authentication","text":"create_auth_middleware ties an OAuthConfig to a validator:\n\ncreate_auth_middleware(config::OAuthConfig;\n                       validator::TokenValidator,                 # REQUIRED — no default\n                       allowlist::Union{Set{String},Nothing} = nothing,\n                       case_insensitive_allowlist::Bool = true,   # fold username case (see Allowlists)\n                       enabled::Bool = true) -> AuthMiddleware\n\nvalidator is required and has no default — the package will never silently select an unsafe validator for you. Two convenience constructors exist: create_simple_auth (a Dict of API keys → usernames) and disable_auth (an explicit, clearly-named no-op for development).","title":"Assembling the middleware"},{"category":"section","location":"oauth/#Allowlists","page":"Authentication","text":"When an allowlist::Set{String} is configured, a successfully authenticated principal must additionally have its username or subject in the set, or the request is 403. This is the simplest authorization model: \"valid token and on the list.\"\n\nnote: Username matching is case-insensitive; subjects are exact\nBy default the username comparison is case-insensitive (case_insensitive_allowlist = true), because brokered identity normalizes case — Keycloak, for example, lowercases federated usernames, so a GitHub login Alice arrives as alice in the token. The opaque OAuth subject is always matched exactly (case-folding a stable identifier could collide two principals). Pass case_insensitive_allowlist = false (on AuthMiddleware or any create_*_auth constructor) for exact username matching.","title":"Allowlists"},{"category":"section","location":"oauth/#Per-tool-authorization","page":"Authentication","text":"OAuthConfig.required_scopes gates the whole server. To require a scope for a specific tool, declare required_scopes on the tool:\n\nMCPTool(\n    name = \"delete_record\",\n    description = \"Delete a record.\",\n    parameters = [ToolParameter(name = \"id\", description = \"record id\", type = \"string\", required = true)],\n    required_scopes = [\"mcp:write\"],\n    handler = (args) -> TextContent(text = \"deleted $(args[\"id\"])\"),\n)\n\nAt tools/call dispatch — before the handler runs, and before the task/sync split so both execution paths are gated — every scope in required_scopes must be present on the authenticated principal's scopes. On a miss the call is refused with a JSON-RPC -32004 (INSUFFICIENT_SCOPE) error naming the missing scope(s), and the handler never runs. required_scopes is server-side policy and is not emitted in tools/list.\n\nThe exact predicate is ctx.authenticated_user !== nothing: the check runs whenever the request carries an authenticated principal. With auth = nothing (no middleware) authenticated_user is nothing and the check is skipped — the server performs no authorization at all. disable_auth is not the same: it yields an anonymous AuthenticatedUser with no scopes, so a tool with non-empty required_scopes is denied under it, not skipped — don't rely on disable_auth() to bypass per-tool scopes. required_scopes is therefore meaningful only alongside an auth middleware whose validator populates scopes: JWTValidator/JWKSValidator read the JWT's scope/scp claim, and IntrospectionValidator reads the introspection response's scope.\n\nFor authorization that depends on the arguments rather than a static scope set, inspect the principal inside a context-aware handler and return a tool-level error instead:\n\nMCPTool(\n    name = \"transfer\",\n    description = \"Move funds; large transfers also need 'mcp:admin'.\",\n    parameters = [ToolParameter(name = \"amount\", description = \"amount\", type = \"number\", required = true)],\n    required_scopes = [\"mcp:write\"],   # baseline gate, enforced at dispatch\n    handler = (args, ctx) -> begin\n        scopes = ctx.authenticated_user === nothing ? String[] : ctx.authenticated_user.scopes\n        if args[\"amount\"] > 10_000 && !(\"mcp:admin\" in scopes)\n            return CallToolResult(\n                content = [TextContent(text = \"insufficient_scope: transfers over 10000 require 'mcp:admin'\")],\n                is_error = true)\n        end\n        TextContent(text = \"transferred $(args[\"amount\"])\")\n    end,\n)\n\nThe principal is available as ctx.authenticated_user (an AuthenticatedUser, or nothing when the transport has no auth configured; disable_auth() instead yields an anonymous AuthenticatedUser) with .username, .subject, .scopes, .provider, and the raw .claims. A declarative required_scopes miss surfaces as a JSON-RPC error (the call was refused before running); a handler returning CallToolResult(is_error = true) reports a tool-level failure to the model — both are distinct from the transport-level 403 a client gets for a server-wide scope/allowlist failure, which the model never sees.","title":"Per-tool authorization"},{"category":"section","location":"oauth/#Protected-Resource-Metadata-(RFC-9728)","page":"Authentication","text":"A compliant MCP client that hits a 401 reads the WWW-Authenticate: … resource_metadata=\"<url>\" header, fetches that document, and learns which Authorization Server to obtain a token from. Construct it directly or with a helper:\n\nmeta = create_protected_resource_metadata(\n    \"https://mcp.example.org/mcp\",                       # this resource\n    [\"https://auth.example.org/realms/main\"],            # its authorization server(s)\n    scopes = [\"mcp:read\"])\n\nIt is served, unauthenticated, at /.well-known/oauth-protected-resource. For GitHub, create_github_resource_metadata fills in GitHub's authorization server URL.","title":"Protected Resource Metadata (RFC 9728)"},{"category":"section","location":"oauth/#Security-checklist","page":"Authentication","text":"Prefer JWKSValidator for external tokens; reach for JWTValidator only in dev/test or behind a component that has already verified the signature.\nAlways set issuer and audience in OAuthConfig — they are the difference between \"a valid token\" and \"a valid token for this server.\"\nTerminate TLS in front of the transport; never expose the plaintext HTTP port. Never log bearer tokens.\nDo not pass a client's token through to an upstream API. If you must call an upstream, exchange the token (RFC 8693) or use the server's own credentials.\nKeep the error surface generic (the package already does this) — don't add tool output that leaks why authentication failed.\n\nSee Deployment for a complete, reproducible setup behind a reverse proxy with a real Authorization Server.","title":"Security checklist"},{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"Pages = [\"api.md\"]","title":"API Reference"},{"category":"section","location":"api/#Primary-Interface","page":"API Reference","text":"","title":"Primary Interface"},{"category":"section","location":"api/#Server-Operations","page":"API Reference","text":"","title":"Server Operations"},{"category":"section","location":"api/#Component-Types","page":"API Reference","text":"","title":"Component Types"},{"category":"section","location":"api/#Tools","page":"API Reference","text":"","title":"Tools"},{"category":"section","location":"api/#Resources","page":"API Reference","text":"","title":"Resources"},{"category":"section","location":"api/#Prompts","page":"API Reference","text":"","title":"Prompts"},{"category":"section","location":"api/#Content-Types","page":"API Reference","text":"","title":"Content Types"},{"category":"section","location":"api/#Abstract-Types","page":"API Reference","text":"","title":"Abstract Types"},{"category":"section","location":"api/#Concrete-Content-Types","page":"API Reference","text":"","title":"Concrete Content Types"},{"category":"section","location":"api/#Resource-Content-Types","page":"API Reference","text":"","title":"Resource Content Types"},{"category":"section","location":"api/#Tool-Results","page":"API Reference","text":"","title":"Tool Results"},{"category":"section","location":"api/#Transport-Configuration","page":"API Reference","text":"","title":"Transport Configuration"},{"category":"section","location":"api/#Server-Type","page":"API Reference","text":"","title":"Server Type"},{"category":"section","location":"api/#Resource-Subscriptions","page":"API Reference","text":"","title":"Resource Subscriptions"},{"category":"section","location":"api/#Handler-Context-Helpers","page":"API Reference","text":"","title":"Handler Context Helpers"},{"category":"section","location":"api/#Multi-Round-Trip-Requests-(MRTR)","page":"API Reference","text":"","title":"Multi Round-Trip Requests (MRTR)"},{"category":"section","location":"api/#Tasks-Extension","page":"API Reference","text":"","title":"Tasks Extension"},{"category":"section","location":"api/#Protocol-Version-Negotiation","page":"API Reference","text":"","title":"Protocol Version Negotiation"},{"category":"section","location":"api/#Authentication-(OAuth-Resource-Server)","page":"API Reference","text":"","title":"Authentication (OAuth Resource Server)"},{"category":"section","location":"api/#Configuration-and-Middleware","page":"API Reference","text":"","title":"Configuration and Middleware"},{"category":"section","location":"api/#Token-Validators","page":"API Reference","text":"","title":"Token Validators"},{"category":"section","location":"api/#Protected-Resource-Metadata-(RFC-9728)","page":"API Reference","text":"","title":"Protected Resource Metadata (RFC 9728)"},{"category":"section","location":"api/#Request-Authentication","page":"API Reference","text":"","title":"Request Authentication"},{"category":"section","location":"api/#Utility-Functions","page":"API Reference","text":"","title":"Utility Functions"},{"category":"section","location":"api/#Transport-Options","page":"API Reference","text":"ModelContextProtocol.jl supports multiple transport mechanisms:","title":"Transport Options"},{"category":"section","location":"api/#STDIO-Transport-(Default)","page":"API Reference","text":"server = mcp_server(name = \"my-server\")\nstart!(server)  # Uses StdioTransport by default","title":"STDIO Transport (Default)"},{"category":"section","location":"api/#HTTP-Transport","page":"API Reference","text":"server = mcp_server(name = \"my-http-server\")\ntransport = HttpTransport(; port = 3000)\nconnect(transport)   # binds the port and starts the listener\nstart!(server; transport = transport)\n\n# With custom configuration\ntransport = HttpTransport(;\n    host = \"127.0.0.1\",  # Important for Windows\n    port = 8080,         # Default port\n    endpoint = \"/\"       # Default endpoint\n)\nconnect(transport)\nstart!(server; transport = transport)\n\nNote: HTTP transport currently supports HTTP only, not HTTPS. For production use:\n\nUse mcp-remote with --allow-http flag for secure connections\nOr deploy behind a reverse proxy (nginx, Apache) for TLS termination","title":"HTTP Transport"},{"category":"section","location":"api/#Internal-API","page":"API Reference","text":"The following internal types and functions are documented for developers working on the package itself.","title":"Internal API"},{"category":"section","location":"api/#Protocol-Types","page":"API Reference","text":"","title":"Protocol Types"},{"category":"section","location":"api/#Core-Implementation","page":"API Reference","text":"","title":"Core Implementation"},{"category":"section","location":"api/#Transport-Implementation","page":"API Reference","text":"","title":"Transport Implementation"},{"category":"section","location":"api/#Authentication-Implementation","page":"API Reference","text":"","title":"Authentication Implementation"},{"category":"section","location":"api/#Utilities","page":"API Reference","text":"","title":"Utilities"},{"category":"function","location":"api/#ModelContextProtocol.mcp_server","page":"API Reference","text":"mcp_server(; name::String, version::String=\"1.0.0\",\n         tools::Union{Vector{MCPTool},MCPTool,Nothing}=nothing,\n         resources::Union{Vector{MCPResource},MCPResource,Nothing}=nothing,\n         prompts::Union{Vector{MCPPrompt},MCPPrompt,Nothing}=nothing,\n         description::String=\"\",\n         instructions::String=\"\",\n         capabilities::Vector{Capability}=default_capabilities(),\n         auto_register_dir::Union{String,Nothing}=nothing,\n         title::Union{String,Nothing}=nothing,\n         icons::Union{Vector{MCPIcon},Nothing}=nothing) -> Server\n\nPrimary entry point for creating and configuring a Model Context Protocol (MCP) server.\n\nArguments\n\nname::String: Unique identifier for the server instance\nversion::String: Your server implementation version (defaults to \"1.0.0\") - YOUR server's version, not the MCP protocol version\ntools: Tools to expose to the model\nresources: Resources available to the model\nresource_templates: Parameterized resource families (RFC 6570 {var} URI templates with a provider; advertised via resources/templates/list)\nprompts: Predefined prompts for the model\ndescription::String: Optional server description\ninstructions::String: Optional natural-language guidance for LLM clients on using this server (returned by legacy initialize and modern server/discover)\ncapabilities::Vector{Capability}: Server capability configuration\nauto_register_dir: Directory to auto-register components from\ntitle::Union{String,Nothing}: Optional human-friendly display name for the server\nicons::Union{Vector{MCPIcon},Nothing}: Optional icons for the server\nmrtr_state_key::Union{Vector{UInt8},Nothing}: Optional MRTR requestState signing key (≥32 bytes) shared across replicas so retries survive re-routing; defaults to a random per-server key (single-instance safe). Tokens also carry a format version: replicas on different package versions that change it reject each other's tokens (\"unsupported version\"), so a rolling upgrade across such a change should drain in-flight MRTR exchanges (bounded by the 600s state TTL)\n\nReturns\n\nServer: A configured server instance ready to handle MCP client connections\n\nExample\n\nserver = mcp_server(\n    name = \"my-server\",\n    version = \"1.0.0\",  # Your server version\n    title = \"My MCP Server\",\n    description = \"Demo server with time tool\",\n    tools = MCPTool(\n        name = \"get_time\",\n        description = \"Get current time\",\n        parameters = [],\n        handler = args -> Dates.format(now(), \"HH:MM:SS\")\n    )\n)\nstart!(server)\n\n\n\n\n\n","title":"ModelContextProtocol.mcp_server"},{"category":"function","location":"api/#ModelContextProtocol.start!","page":"API Reference","text":"start!(server::Server; transport::Union{Transport,Nothing}=nothing) -> Nothing\n\nStart the MCP server, setting up logging and entering the main server loop.\n\nArguments\n\nserver::Server: The server instance to start\ntransport::Union{Transport,Nothing}: Optional transport to use. If not provided, uses StdioTransport\n\nReturns\n\nNothing: The function returns after the server stops\n\nThrows\n\nServerError: If the server is already running\n\n\n\n\n\n","title":"ModelContextProtocol.start!"},{"category":"function","location":"api/#ModelContextProtocol.stop!","page":"API Reference","text":"stop!(server::Server) -> Nothing\n\nStop a running MCP server: the server loop observes active == false on its next iteration and exits with the transport still open, so shutdown can deliver each subscriptions/listen stream's graceful closing result before the transport closes.\n\nOn HTTP this takes effect within the loop's bounded read wait. On stdio the loop blocks in readline, so stop! only takes effect when the next message — or EOF — arrives; the normal stdio lifecycle ends via stdin EOF, which runs the same graceful-closure path.\n\nArguments\n\nserver::Server: The server instance to stop\n\nReturns\n\nNothing: The function returns after setting the server to inactive\n\nThrows\n\nServerError: If the server is not currently running\n\n\n\n\n\n","title":"ModelContextProtocol.stop!"},{"category":"function","location":"api/#ModelContextProtocol.register!","page":"API Reference","text":"register!(server::Server, component::Union{Tool,Resource,MCPPrompt}) -> Server\n\nRegister a tool, resource, or prompt with the MCP server.\n\nArguments\n\nserver::Server: The server to register the component with\ncomponent: The component to register (can be a tool, resource, or prompt)\n\nReturns\n\nServer: The server instance for method chaining\n\n\n\n\n\n","title":"ModelContextProtocol.register!"},{"category":"type","location":"api/#ModelContextProtocol.MCPTool","page":"API Reference","text":"MCPTool(; name::String, description::String, parameters::Vector{ToolParameter}=ToolParameter[],\n      input_schema::Union{Nothing,AbstractDict}=nothing, handler::Function,\n      return_type::Type=Vector{Content}) <: Tool\n\nImplement a tool that can be invoked by clients in the MCP protocol.\n\nFields\n\nname::String: Unique identifier for the tool\ndescription::String: Human-readable description of the tool's purpose\nparameters::Vector{ToolParameter}: Simple parameters (ignored if input_schema is provided)\ninput_schema::Union{Nothing,AbstractDict}: Raw JSON Schema for complex parameter types\nhandler::Function: Function that implements the tool's functionality\nreturn_type::Type: Expected return type of the handler (defaults to Vector{Content})\nannotations::Union{Nothing,Dict{String,Any}}: Optional tool annotations (behavioral hints for clients), e.g. Dict(\"readOnlyHint\" => true, \"destructiveHint\" => false, \"idempotentHint\" => true, \"openWorldHint\" => false). Emitted verbatim intools/list`.\noutput_schema::Union{Nothing,AbstractDict}: Optional JSON Schema for the tool's structured result, emitted as outputSchema in tools/list. Pair it with a CallToolResult(structured_content=…) so clients can validate the structured output.\n_meta::Union{Nothing,Dict{String,Any}}: Optional metadata for protocol extensions, emitted verbatim in tools/list when set\ntask_support::Symbol: Task-augmented execution support (MCP 2025-11-25, experimental): :forbidden (default — calls run synchronously), :optional (client may run the call as a task), or :required (client must run the call as a task). Emitted as execution.taskSupport in tools/list for clients that negotiated 2025-11-25.\nrequired_scopes::Vector{String}: OAuth scopes the caller must hold to invoke this tool. Enforced at tools/call dispatch when the request carries an authenticated principal (HTTP auth active): every listed scope must be present on ctx.authenticated_user.scopes or the call is refused with an insufficient-scope error. Empty (default) means no per-tool requirement. When auth is not configured (no authenticated user) the check is skipped — the server performs no authorization. Not emitted in tools/list (it is server-side policy, not part of the tool's schema).\n\nParameter Definition\n\nTools can define parameters in two ways:\n\nSimple parameters using parameters::Vector{ToolParameter}: Good for flat schemas with basic types (string, number, boolean).\nComplex schemas using input_schema::AbstractDict: Supports arrays, enums, nested objects, and any valid JSON Schema. When provided, input_schema takes precedence over parameters.\n\nHandler Return Types\n\nThe tool handler can return various types which are automatically converted:\n\nAn instance of the specified Content type (TextContent, ImageContent, etc.)\nA Vector{<:Content} for multiple content items (can mix TextContent, ImageContent, etc.)\nA Dict (automatically converted to JSON and wrapped in TextContent)\nA String (automatically wrapped in TextContent)\nA Tuple{Vector{UInt8}, String} (automatically wrapped in ImageContent)\nA CallToolResult object for full control over the response (including error handling)\n\nWhen returntype is Vector{Content} (default), single Content items are automatically wrapped in a vector. Note: When returning CallToolResult directly, the returntype field is ignored.\n\n\n\n\n\n","title":"ModelContextProtocol.MCPTool"},{"category":"type","location":"api/#ModelContextProtocol.ToolParameter","page":"API Reference","text":"ToolParameter(; name::String, description::String, type::String, required::Bool=false,\n              default::Any=nothing, header::Union{String,Nothing}=nothing)\n\nDefine a parameter for an MCP tool.\n\nFields\n\nname::String: The parameter name (used as the key in the params dictionary)\ndescription::String: Human-readable description of the parameter\ntype::String: Type of the parameter as specified in the MCP schema (e.g., \"string\", \"number\", \"boolean\")\nrequired::Bool: Whether the parameter is required for tool invocation\ndefault::Any: Default value for the parameter if not provided (nothing means no default)\nheader::Union{String,Nothing}: Optional SEP-2243 header-mirroring suffix, emitted as x-mcp-header in the generated schema: modern HTTP clients mirror the argument as an Mcp-Param-<header> request header (for routing), which the server validates against the body\n\n\n\n\n\n","title":"ModelContextProtocol.ToolParameter"},{"category":"type","location":"api/#ModelContextProtocol.MCPIcon","page":"API Reference","text":"MCPIcon(; src::String, mimeType=nothing, sizes=nothing, theme=nothing)\n\nIcon metadata for tools, resources, prompts, and server info (MCP 2025-11-25).\n\nFields\n\nsrc::String: URI pointing to the icon (http/https URL or data: URI)\nmimeType::Union{String,Nothing}: MIME type override (e.g., \"image/png\", \"image/svg+xml\")\nsizes::Union{Vector{String},Nothing}: Size hints (e.g., [\"48x48\", \"any\"])\ntheme::Union{String,Nothing}: Which background the icon is designed for (\"light\" or \"dark\")\n\n\n\n\n\n","title":"ModelContextProtocol.MCPIcon"},{"category":"type","location":"api/#ModelContextProtocol.MCPResource","page":"API Reference","text":"MCPResource <: Resource\n\nImplement a resource that clients can access in the MCP protocol. Resources represent data that can be read by models and tools.\n\nFields\n\nuri::URI: Unique identifier for the resource\nname::String: Human-readable name for the resource\ndescription::String: Detailed description of the resource\nmime_type::String: MIME type of the resource data\ndata_provider::Function: Zero-argument function returning the resource data for resources/read. Return a TextResourceContents/BlobResourceContents (or a vector of them) for full control — BlobResourceContents is how binary resources are served (base64 blob on the wire). A String is used as the text verbatim; any other value is JSON-encoded into a text contents entry with the resource's mime_type.\nannotations::AbstractDict{String,Any}: Additional metadata for the resource\n\n\n\n\n\n","title":"ModelContextProtocol.MCPResource"},{"category":"type","location":"api/#ModelContextProtocol.ResourceTemplate","page":"API Reference","text":"ResourceTemplate(; name::String, uri_template::String,\n               mime_type::Union{String,Nothing}=nothing, description::String=\"\",\n               title::Union{String,Nothing}=nothing,\n               icons::Union{Vector{MCPIcon},Nothing}=nothing,\n               data_provider::Union{Function,Nothing}=nothing,\n               _meta::Union{Nothing,Dict{String,Any}}=nothing)\n\nDefine a parameterized family of resources: an RFC 6570 URI template (level-1 {var} placeholders, advertised via resources/templates/list) plus a provider that serves any resources/read whose URI matches the template.\n\nFields\n\nname::String: Name of the resource template\nuri_template::String: URI template with {var} placeholders; each placeholder matches one path segment (no /)\nmime_type::Union{String,Nothing}: MIME type shared by all matching resources\ndescription::String: Human-readable description of the template\ntitle::Union{String,Nothing}: Optional human-friendly display name\nicons::Union{Vector{MCPIcon},Nothing}: Optional icons for UI display\ndata_provider::Union{Function,Nothing}: Called for a matching resources/read as provider(uri::String) — or opt into provider(uri::String, vars::Dict{String,String}) to receive the extracted template variables. Return values follow the same contract as MCPResource providers (ResourceContents/vector, String verbatim, JSON fallback). Templates without a provider are advertised but not readable.\n_meta::Union{Nothing,Dict{String,Any}}: Optional metadata for protocol extensions, emitted verbatim in resources/templates/list when set\ncompletions::Union{Nothing,Dict{String,Any}}: Optional completion/complete sources per template variable name: a Vector{String} (served filtered by prefix against the partial value) or a function value -> values / (value, context_args) -> values returning the suggestions\n\n\n\n\n\n","title":"ModelContextProtocol.ResourceTemplate"},{"category":"type","location":"api/#ModelContextProtocol.MCPPrompt","page":"API Reference","text":"MCPPrompt(; name::String, description::String=\"\",\n        arguments::Vector{PromptArgument}=PromptArgument[],\n        messages::Vector{PromptMessage}=PromptMessage[],\n        title::Union{String,Nothing}=nothing,\n        icons::Union{Vector{MCPIcon},Nothing}=nothing,\n        completions::Union{Nothing,Dict{String,Any}}=nothing)\n\nImplement a prompt or prompt template as defined in the MCP schema. Prompts can include variables that are replaced with arguments when retrieved.\n\nFields\n\nname::String: Unique identifier for the prompt\ndescription::String: Human-readable description of the prompt's purpose\narguments::Vector{PromptArgument}: Arguments that this prompt accepts\nmessages::Vector{PromptMessage}: The sequence of messages in the prompt\ntitle::Union{String,Nothing}: Optional human-friendly display name\nicons::Union{Vector{MCPIcon},Nothing}: Optional icons for UI display\n_meta::Union{Nothing,Dict{String,Any}}: Optional metadata for protocol extensions, emitted verbatim in prompts/list when set\ncompletions::Union{Nothing,Dict{String,Any}}: Optional completion/complete sources per argument name: a Vector{String} (served filtered by prefix against the partial value) or a function value -> values / (value, context_args) -> values returning the suggestions\n\n\n\n\n\n","title":"ModelContextProtocol.MCPPrompt"},{"category":"type","location":"api/#ModelContextProtocol.PromptArgument","page":"API Reference","text":"PromptArgument(; name::String, description::String=\"\", required::Bool=false,\n              title::Union{String,Nothing}=nothing)\n\nDefine an argument that a prompt template can accept.\n\nFields\n\nname::String: The argument name (used in template placeholders)\ndescription::String: Human-readable description of the argument\nrequired::Bool: Whether the argument is required when using the prompt\ntitle::Union{String,Nothing}: Optional human-friendly display name\n\n\n\n\n\n","title":"ModelContextProtocol.PromptArgument"},{"category":"type","location":"api/#ModelContextProtocol.PromptMessage","page":"API Reference","text":"PromptMessage(; content::Union{TextContent, ImageContent, AudioContent, ResourceLink, EmbeddedResource}, role::Role=user)\n\nRepresent a single message in a prompt template.\n\nFields\n\ncontent::Union{TextContent, ImageContent, AudioContent, ResourceLink, EmbeddedResource}: The content of the message\nrole::Role: Whether this message is from the user or assistant (defaults to user)\n\n\n\n\n\n","title":"ModelContextProtocol.PromptMessage"},{"category":"type","location":"api/#ModelContextProtocol.Content","page":"API Reference","text":"Content\n\nAbstract base type for all content formats in the MCP protocol. Content can be exchanged between clients and servers in various formats.\n\n\n\n\n\n","title":"ModelContextProtocol.Content"},{"category":"type","location":"api/#ModelContextProtocol.ResourceContents","page":"API Reference","text":"ResourceContents\n\nAbstract base type for resource content formats in the MCP protocol. Resources can contain different types of content based on their MIME type.\n\n\n\n\n\n","title":"ModelContextProtocol.ResourceContents"},{"category":"type","location":"api/#ModelContextProtocol.TextContent","page":"API Reference","text":"TextContent(; type::String=\"text\", text::String, \n            annotations::Union{Nothing,Dict{String,Any}}=nothing,\n            _meta::Union{Nothing,Dict{String,Any}}=nothing) <: Content\n\nText-based content for messages and tool responses.\n\nFields\n\ntype::String: Content type identifier (always \"text\")\ntext::String: The actual text content\nannotations::Union{Nothing,Dict{String,Any}}: Optional annotations for the client\n_meta::Union{Nothing,Dict{String,Any}}: Optional metadata for protocol extensions\n\n\n\n\n\n","title":"ModelContextProtocol.TextContent"},{"category":"type","location":"api/#ModelContextProtocol.ImageContent","page":"API Reference","text":"ImageContent(; type::String=\"image\", data::Vector{UInt8}, mime_type::String,\n             annotations::Union{Nothing,Dict{String,Any}}=nothing,\n             _meta::Union{Nothing,Dict{String,Any}}=nothing) <: Content\n\nImage content for messages and tool responses.\n\nFields\n\ntype::String: Content type identifier (always \"image\")\ndata::Vector{UInt8}: Raw image data (automatically base64-encoded when serialized)\nmime_type::String: MIME type of the image (e.g., \"image/png\")\nannotations::Union{Nothing,Dict{String,Any}}: Optional annotations for the client\n_meta::Union{Nothing,Dict{String,Any}}: Optional metadata for protocol extensions\n\n\n\n\n\n","title":"ModelContextProtocol.ImageContent"},{"category":"type","location":"api/#ModelContextProtocol.AudioContent","page":"API Reference","text":"AudioContent(; type::String=\"audio\", data::Vector{UInt8}, mime_type::String,\n             annotations::Union{Nothing,Dict{String,Any}}=nothing,\n             _meta::Union{Nothing,Dict{String,Any}}=nothing) <: Content\n\nAudio content for messages and tool responses (protocol 2025-03-26+).\n\nFields\n\ntype::String: Content type identifier (always \"audio\")\ndata::Vector{UInt8}: Raw audio data (automatically base64-encoded when serialized)\nmime_type::String: MIME type of the audio (e.g., \"audio/wav\"), serialized as mimeType\nannotations::Union{Nothing,Dict{String,Any}}: Optional annotations for the client\n_meta::Union{Nothing,Dict{String,Any}}: Optional metadata for protocol extensions\n\n\n\n\n\n","title":"ModelContextProtocol.AudioContent"},{"category":"type","location":"api/#ModelContextProtocol.EmbeddedResource","page":"API Reference","text":"EmbeddedResource(; type::String=\"resource\", resource::Dict{String,Any},\n                 annotations::Union{Nothing,Dict{String,Any}}=nothing,\n                 _meta::Union{Nothing,Dict{String,Any}}=nothing) <: Content\n\nEmbedded resource content for inline resource data.\n\nFields\n\ntype::String: Content type identifier (always \"resource\")\nresource::Dict{String,Any}: The embedded resource data\nannotations::Union{Nothing,Dict{String,Any}}: Optional annotations for the client\n_meta::Union{Nothing,Dict{String,Any}}: Optional metadata for protocol extensions\n\n\n\n\n\n","title":"ModelContextProtocol.EmbeddedResource"},{"category":"type","location":"api/#ModelContextProtocol.ResourceLink","page":"API Reference","text":"ResourceLink(; type::String=\"resource_link\", uri::String, name::String,\n             description::Union{String,Nothing}=nothing,\n             mime_type::Union{String,Nothing}=nothing,\n             title::Union{String,Nothing}=nothing,\n             annotations::Union{Nothing,Dict{String,Any}}=nothing,\n             _meta::Union{Nothing,Dict{String,Any}}=nothing) <: Content\n\nLink to a resource a tool result can reference without embedding it (protocol 2025-06-18). Serialized per the MCP spec as {\"type\": \"resource_link\", \"uri\": ..., \"name\": ..., ...}.\n\nFields\n\ntype::String: Content type identifier (always \"resource_link\")\nuri::String: URI of the linked resource\nname::String: Name of the resource\ndescription::Union{String,Nothing}: Optional description of the resource\nmime_type::Union{String,Nothing}: Optional MIME type, serialized as mimeType\nsize::Union{Int,Nothing}: Optional resource size in bytes\ntitle::Union{String,Nothing}: Optional human-readable title\nannotations::Union{Nothing,Dict{String,Any}}: Optional annotations for the client\n_meta::Union{Nothing,Dict{String,Any}}: Optional metadata for protocol extensions\n\n\n\n\n\n","title":"ModelContextProtocol.ResourceLink"},{"category":"type","location":"api/#ModelContextProtocol.TextResourceContents","page":"API Reference","text":"TextResourceContents(; uri::URI, mime_type::String=\"text/plain\", text::String) <: ResourceContents\n\nText content for resources in the MCP protocol.\n\nFields\n\nuri::URI: Resource identifier\nmime_type::String: MIME type of the text content\ntext::String: The actual text content\n\n\n\n\n\n","title":"ModelContextProtocol.TextResourceContents"},{"category":"type","location":"api/#ModelContextProtocol.BlobResourceContents","page":"API Reference","text":"BlobResourceContents(; uri::URI, mime_type::String=\"application/octet-stream\", \n                    blob::Vector{UInt8}) <: ResourceContents\n\nBinary content for resources.\n\nFields\n\nuri::URI: Resource identifier\nmime_type::String: MIME type of the binary content\nblob::Vector{UInt8}: Raw binary data (automatically base64-encoded when serialized)\n\n\n\n\n\n","title":"ModelContextProtocol.BlobResourceContents"},{"category":"type","location":"api/#ModelContextProtocol.CallToolResult","page":"API Reference","text":"CallToolResult(; content::Vector{Dict{String,Any}}, is_error::Bool=false,\n               structured_content=nothing) <: ResponseResult\n\nResult returned from a tool invocation.\n\nFields\n\ncontent::Vector{Dict{String,Any}}: Content produced by the tool\nis_error::Bool: Whether the tool execution resulted in an error\nstructured_content::Union{Nothing,AbstractDict}: Optional structured result (a JSON object per the MCP spec), serialized as structuredContent and omitted when nothing. Pair it with the tool's output_schema so clients can validate and consume it programmatically. Per the spec, a tool returning structured content SHOULD also include a human-readable serialization (e.g. the JSON as text) in content for clients that don't consume structured output.\n_meta::Union{Nothing,AbstractDict}: Optional result metadata for protocol extensions, serialized as _meta and omitted when nothing\n\n\n\n\n\n","title":"ModelContextProtocol.CallToolResult"},{"category":"type","location":"api/#ModelContextProtocol.Transport","page":"API Reference","text":"Transport\n\nAbstract base type for all MCP transport implementations. Defines the interface for reading and writing messages between client and server.\n\n\n\n\n\n","title":"ModelContextProtocol.Transport"},{"category":"type","location":"api/#ModelContextProtocol.StdioTransport","page":"API Reference","text":"StdioTransport(; input::IO=stdin, output::IO=stdout)\n\nTransport implementation using standard input/output streams. This is the default transport for local MCP server processes.\n\nFields\n\ninput::IO: Input stream for reading messages (default: stdin)\noutput::IO: Output stream for writing messages (default: stdout)\nconnected::Bool: Connection status (always true for stdio)\nwrite_lock::ReentrantLock: Serializes output writes (responses and notifications share stdout, and background task executions may write concurrently with the loop)\n\n\n\n\n\n","title":"ModelContextProtocol.StdioTransport"},{"category":"type","location":"api/#ModelContextProtocol.HttpTransport","page":"API Reference","text":"HttpTransport(; host::String=\"127.0.0.1\", port::Int=8080, endpoint::String=\"/\",\n              allowed_origins::Vector{String}=String[], allowed_hosts::Vector{String}=String[])\n\nTransport implementation following the MCP Streamable HTTP specification (2025-06-18). Supports Server-Sent Events (SSE) for streaming and session management. A request's response is a single JSON object, or — when request-scoped notifications (progress, log messages) are emitted during handling — an SSE stream scoped to that request.\n\nWhen bound to a loopback host without bearer auth, requests whose Host or Origin header is neither local nor allowlisted are rejected with 403 (DNS-rebinding protection); a deployment behind a reverse proxy adds its public hostname to allowed_hosts (or enables auth, which disables the guard).\n\nFields\n\nhost::String: Host address to bind to (default: \"127.0.0.1\")\nport::Int: Port number to listen on (default: 8080)\nendpoint::String: HTTP endpoint path (default: \"/\")\nserver::Union{HTTP.Server,Nothing}: HTTP server instance\nconnected::Bool: Connection status\nserver_task::Union{Task,Nothing}: Server task handle\nactive_streams::Dict{String,HTTP.Stream}: Active streaming connections\nrequest_queue::Channel{QueuedHttpRequest}: Queue for incoming requests (id, body, auth user)\nresponse_channels::Dict{String,Channel{Tuple{Symbol,String}}}: Per-request response routes carrying (:notification, json) entries followed by one (:response, json)\nallowed_origins::Vector{String}: Origins accepted by the DNS-rebinding guard (exact match)\nallowed_hosts::Vector{String}: Extra Host-header hostnames accepted by the DNS-rebinding guard\n\n\n\n\n\n","title":"ModelContextProtocol.HttpTransport"},{"category":"function","location":"api/#ModelContextProtocol.connect","page":"API Reference","text":"connect(transport::Transport) -> Nothing\n\nEstablish the transport connection. Default implementation does nothing (for transports that are always connected).\n\nArguments\n\ntransport::Transport: The transport instance to connect\n\nReturns\n\nNothing\n\nThrows\n\nTransportError: If connection cannot be established\n\n\n\n\n\nconnect(transport::HttpTransport) -> Nothing\n\nStart the HTTP server and begin listening for connections.\n\nArguments\n\ntransport::HttpTransport: The transport instance\n\nReturns\n\nNothing\n\nThrows\n\nTransportError: If the server cannot be started\n\n\n\n\n\n","title":"ModelContextProtocol.connect"},{"category":"type","location":"api/#ModelContextProtocol.Server","page":"API Reference","text":"Server(config::ServerConfig; transport::Union{Transport,Nothing}=nothing)\n\nRepresent a running MCP server instance that manages resources, tools, and prompts.\n\nFields\n\nconfig::ServerConfig: Server configuration settings\ntransport::Union{Transport,Nothing}: Transport implementation for client-server communication\nresources::Vector{Resource}: Available resources\ntools::Vector{Tool}: Available tools\nprompts::Vector{MCPPrompt}: Available prompts\nresource_templates::Vector{ResourceTemplate}: Available resource templates\nsubscriptions::DefaultDict{String,Vector{Subscription}}: Resource subscription registry\nprogress_trackers::Dict{Union{String,Int},Progress}: Progress tracking for operations\ntasks::TaskStore: Registry of server-side tasks (MCP Tasks, experimental)\nlisten_subscriptions::SubscriptionRegistry: Active modern-era subscriptions/listen streams\nactive::Bool: Whether the server is currently active\ntask_notification_queue::Union{Channel{Any},Nothing}: notifications/tasks dispatch FIFO (set by install_task_notifications!, closed by stop!)\ntool_header_paths::Dict{String,Vector{Tuple{Vector{String},String}}}: per-tool SEP-2243 mirror tables — derived from the normalized schema by validate_tool_headers and persisted by register!, read by the handle_modern_request preflight\n\nConstructor\n\nServer(config::ServerConfig; transport=nothing): Creates a new server with the specified configuration\n\n\n\n\n\n","title":"ModelContextProtocol.Server"},{"category":"function","location":"api/#ModelContextProtocol.subscribe!","page":"API Reference","text":"subscribe!(server::Server, uri::String, callback::Function) -> Server\n\nSubscribe to updates for a specific resource identified by URI.\n\nThe callback is an in-process observer: notify_resource_updated invokes it as callback(uri) whenever that URI is announced as changed. It runs on the announcing task, its errors are logged and swallowed, and it sends nothing to clients on its own (client delivery is notify_resource_updated's job).\n\nArguments\n\nserver::Server: The server instance\nuri::String: The resource URI to subscribe to\ncallback::Function: Called as callback(uri::String) on each announced update\n\nReturns\n\nServer: The server instance for method chaining\n\n\n\n\n\n","title":"ModelContextProtocol.subscribe!"},{"category":"function","location":"api/#ModelContextProtocol.unsubscribe!","page":"API Reference","text":"unsubscribe!(server::Server, uri::String, callback::Function) -> Server\n\nRemove a subscription for a specific resource URI and callback function.\n\nArguments\n\nserver::Server: The server instance\nuri::String: The resource URI to unsubscribe from\ncallback::Function: The callback function to remove\n\nReturns\n\nServer: The server instance for method chaining\n\n\n\n\n\n","title":"ModelContextProtocol.unsubscribe!"},{"category":"function","location":"api/#ModelContextProtocol.notify_list_changed","page":"API Reference","text":"notify_list_changed(server::Server, kind::Symbol) -> Int\n\nAnnounce that a list of server components changed, delivering notifications/{kind}/list_changed to every subscriptions/listen stream that subscribed to it and to the connected legacy session (when the corresponding listChanged capability is declared). Call this after mutating server.tools, server.prompts, or server.resources at runtime.\n\nOnly streams open at the time of the change are notified (there is no backlog).\n\nArguments\n\nserver::Server: The server whose lists changed\nkind::Symbol: One of :tools, :prompts, :resources\n\nReturns\n\nInt: How many delivery targets the notification was handed to — subscriptions/listen streams plus the legacy session. Counts enqueue to the transport, not client receipt (a disconnected HTTP peer drops silently).\n\n\n\n\n\n","title":"ModelContextProtocol.notify_list_changed"},{"category":"function","location":"api/#ModelContextProtocol.notify_resource_updated","page":"API Reference","text":"notify_resource_updated(server::Server, uri::AbstractString) -> Int\n\nAnnounce that a resource's contents changed, delivering notifications/resources/updated to every subscriptions/listen stream that subscribed to that URI and to the connected legacy session when it subscribed via resources/subscribe. In-process callbacks registered with subscribe! for the URI are also invoked (with the URI as their only argument); callback errors are logged and do not affect client delivery or the returned count.\n\nArguments\n\nserver::Server: The server holding the resource\nuri::AbstractString: The resource URI that changed\n\nReturns\n\nInt: How many delivery targets the notification was handed to — subscriptions/listen streams plus the legacy session. Counts enqueue to the transport, not client receipt (a disconnected HTTP peer drops silently).\n\n\n\n\n\n","title":"ModelContextProtocol.notify_resource_updated"},{"category":"function","location":"api/#ModelContextProtocol.send_progress","page":"API Reference","text":"send_progress(ctx::RequestContext, progress::Real;\n              total::Union{Real,Nothing}=nothing,\n              message::Union{String,Nothing}=nothing) -> Bool\n\nEmit an MCP notifications/progress for the current request. A tool handler that accepts the RequestContext (its second argument) can call this during a long operation to report progress.\n\nReturns false (a no-op) when the client did not supply a progressToken, no transport is connected, or the call is a detachable modern-era task call (tasks extension: notifications/progress is not supported on tasks, and the spawned handler has no request stream to route them to), so it is always safe to call. Send an increasing progress; include total for a determinate bar and message for a status line.\n\n\n\n\n\n","title":"ModelContextProtocol.send_progress"},{"category":"function","location":"api/#ModelContextProtocol.task_cancelled","page":"API Reference","text":"task_cancelled(ctx) -> Bool\n\nCheck whether the current task-augmented execution has been cancelled by the client (via tasks/cancel). Long-running, context-aware tool handlers can poll this to stop work early; the discarded result is never delivered (cancelled tasks stay cancelled). Always false for ordinary (non-task) calls, so it is safe to call unconditionally.\n\nhandler = (args, ctx) -> begin\n    for chunk in work_chunks\n        task_cancelled(ctx) && return TextContent(text = \"aborted\")\n        process(chunk)\n    end\n    TextContent(text = \"done\")\nend\n\n\n\n\n\n","title":"ModelContextProtocol.task_cancelled"},{"category":"type","location":"api/#ModelContextProtocol.InputRequired","page":"API Reference","text":"InputRequired(requests::AbstractDict; state=nothing)\n\nThe value a handler returns when it needs client input to finish (MRTR, 2026-07-28): the request is answered with an InputRequiredResult carrying these input requests, and the client retries with responses under the same keys. On the retry the handler runs AGAIN from the top — read the responses with input_responses(ctx) and any carried-over state with input_state(ctx).\n\nOnly meaningful on modern-era tools/call (legacy sessions have no wire shape for it and get an error). If a needed response is still missing on the retry, return another InputRequired — the spec says re-issue, not error.\n\nA valid requestState can be replayed within its TTL (one-time semantics would require server-side shared state, which the stateless design avoids), so handlers should stay idempotent across retries.\n\nFields\n\nrequests::LittleDict{String,InputRequest}: server-assigned unique keys → input requests\nstate::Any: JSON-serializable handler state to carry to the retry, delivered inside the integrity-protected requestState (do NOT put secrets here: it is signed, not encrypted)\n\n\n\n\n\n","title":"ModelContextProtocol.InputRequired"},{"category":"function","location":"api/#ModelContextProtocol.elicit_request","page":"API Reference","text":"elicit_request(message::String; requested_schema=nothing) -> InputRequest\n\nBuild a form-mode elicitation input request (elicitation/create): ask the user a question, constraining the answer with a requested schema. The schema is REQUIRED on the wire (ElicitRequestFormParams.requestedSchema, an object schema with properties), so when none is given a minimal empty-object schema is synthesized — a schema-validating client would reject the request otherwise.\n\nArguments\n\nmessage::String: The message to present to the user\nrequested_schema: JSON schema (Dict) for the expected response content; must be an object schema. Defaults to {\"type\": \"object\", \"properties\": {}}\n\nReturns\n\nInputRequest: The request for an InputRequired return\n\n\n\n\n\n","title":"ModelContextProtocol.elicit_request"},{"category":"function","location":"api/#ModelContextProtocol.sampling_request","page":"API Reference","text":"sampling_request(params::AbstractDict) -> InputRequest\n\nBuild a sampling input request (sampling/createMessage): ask the client's LLM for a completion. params is the CreateMessageRequest params object (e.g. messages, maxTokens).\n\nArguments\n\nparams::AbstractDict: The sampling/createMessage params\n\nReturns\n\nInputRequest: The request for an InputRequired return\n\n\n\n\n\n","title":"ModelContextProtocol.sampling_request"},{"category":"function","location":"api/#ModelContextProtocol.roots_request","page":"API Reference","text":"roots_request() -> InputRequest\n\nBuild a roots input request (roots/list): ask the client for its filesystem roots.\n\nReturns\n\nInputRequest: The request for an InputRequired return\n\n\n\n\n\n","title":"ModelContextProtocol.roots_request"},{"category":"function","location":"api/#ModelContextProtocol.input_responses","page":"API Reference","text":"input_responses(ctx) -> Dict{String,Any}\n\nThe client's inputResponses from an MRTR retry (2026-07-28), keyed like the inputRequests the server issued. Empty on a first (non-retry) request — a handler asks for input by returning InputRequired when the response it needs is absent.\n\nArguments\n\nctx: The request context passed to a ctx-aware handler\n\nReturns\n\nDict{String,Any}: The responses (empty when none)\n\n\n\n\n\n","title":"ModelContextProtocol.input_responses"},{"category":"function","location":"api/#ModelContextProtocol.input_state","page":"API Reference","text":"input_state(ctx) -> Any\n\nThe handler state carried through the verified requestState of an MRTR retry (whatever the handler put in InputRequired(...; state=...)), or nothing on a first request.\n\nArguments\n\nctx: The request context passed to a ctx-aware handler\n\nReturns\n\nThe carried state, or nothing\n\n\n\n\n\n","title":"ModelContextProtocol.input_state"},{"category":"function","location":"api/#ModelContextProtocol.task_detach","page":"API Reference","text":"task_detach(ctx; ttl_ms=nothing, status_message=nothing) -> Bool\n\nHand the current tool call off to background task execution (MCP Tasks extension, SEP-2663). Callable from a ctx-aware tool handler: when the request is modern-era, the tool is task-capable (task_support :optional or :required), and the client declared the io.modelcontextprotocol/tasks extension, this durably creates the task and immediately delivers the CreateTaskResult (resultType:\"task\") as the call's response — the handler keeps running in the background, and its eventual return value (or thrown error) becomes the task's terminal state, observable via tasks/get.\n\nReturns false — and the handler simply continues as an ordinary synchronous call — when any precondition is absent (legacy-era request, extension not declared, tool not task-capable). Idempotent: a second call returns true without creating another task.\n\nArguments\n\nctx: The request context passed to a ctx-aware handler\nttl_ms::Union{Int,Nothing}: Requested retention duration (clamped to the store's maximum); nothing for the server default\nstatus_message::Union{String,Nothing}: Optional initial statusMessage\n\nReturns\n\nBool: true when the call is (now) task-detached\n\n\n\n\n\n","title":"ModelContextProtocol.task_detach"},{"category":"function","location":"api/#ModelContextProtocol.task_await_input","page":"API Reference","text":"task_await_input(ctx, request::InputRequest) -> Any\ntask_await_input(ctx, requests::AbstractVector{InputRequest}) -> Vector{Any}\n\nAsk the client for input MID-TASK and block the handler until it answers (tasks extension, SEP-2663). Callable from a handler that has already detached via task_detach: the request(s) are registered under server-minted keys (unique over the task's lifetime), the task's status flips to input_required, and tasks/get surfaces the outstanding requests in inputRequests. The client answers with tasks/update inputResponses; each response value is returned to the waiting handler (the vector form returns responses in request order, and only once ALL of them have arrived — a partial tasks/update is accepted, the task simply stays input_required until the rest arrive). Once nothing is pending the status returns to working.\n\nBuild requests with elicit_request, sampling_request, or roots_request — the same constructors the MRTR flow uses. A server MUST NOT send input requests the creating request's client capabilities did not declare, so an undeclared capability throws (failing the task).\n\nThrows TaskCancelledException when the task goes terminal while (or before) waiting. An unanswered request blocks until the client answers or cancels, or until the task's ttl elapses — a per-wait deadline timer fails an expired task still parked on client input and unwinds its waiters, so abandoned tasks cannot pin handler state forever. To distinguish expiry from a client cancel, check task_cancelled(ctx) — true only for the latter.\n\nThe registered requests are frozen: each request's params are normalized into an owned plain-JSON snapshot at registration, so a key's content can never change across polls (the spec's key-stability rule) and the capability check binds to exactly what will be surfaced. Params must therefore be plain JSON data — nested dicts/vectors/tuples/sets of strings, numbers, booleans, and nothing (what elicit_request and friends naturally produce); anything else is rejected with an ArgumentError.\n\nBefore detachment there is no inputRequests surface: a handler that needs input to DECIDE (e.g. whether to proceed at all) returns InputRequired for the MRTR round on the original request instead.\n\nArguments\n\nctx: The request context passed to a ctx-aware handler\nrequest/requests: The input request(s) to surface to the client\n\nReturns\n\nThe client's response value (single form), or a Vector{Any} of response values in request order (vector form)\n\n\n\n\n\n","title":"ModelContextProtocol.task_await_input"},{"category":"type","location":"api/#ModelContextProtocol.TaskCancelledException","page":"API Reference","text":"TaskCancelledException()\n\nThrown by task_await_input when the task reaches a terminal state while the handler is waiting (or had already reached one when it asked) — cancelled by the client via tasks/cancel, or failed by the server because its ttl elapsed while parked on client input. Handlers that need to distinguish the two can check task_cancelled(ctx), which is true only for a client cancellation. Catch it for cleanup; otherwise it unwinds the handler, whose discarded outcome never overwrites the terminal status.\n\n\n\n\n\n","title":"ModelContextProtocol.TaskCancelledException"},{"category":"constant","location":"api/#ModelContextProtocol.LATEST_PROTOCOL_VERSION","page":"API Reference","text":"Latest LEGACY (initialize-handshake) MCP protocol version supported by this implementation. Modern-era versions live in MODERN_PROTOCOL_VERSIONS.\n\n\n\n\n\n","title":"ModelContextProtocol.LATEST_PROTOCOL_VERSION"},{"category":"constant","location":"api/#ModelContextProtocol.SUPPORTED_PROTOCOL_VERSIONS","page":"API Reference","text":"All LEGACY (initialize-handshake) MCP protocol versions supported by this implementation, newest first. These are the versions initialize negotiates; modern-era versions (per-request _meta, no handshake) live in MODERN_PROTOCOL_VERSIONS.\n\n\n\n\n\n","title":"ModelContextProtocol.SUPPORTED_PROTOCOL_VERSIONS"},{"category":"constant","location":"api/#ModelContextProtocol.FEATURE_VERSIONS","page":"API Reference","text":"Minimum protocol version required for each feature.\n\nFeatures are identified by Symbol keys. The value is the minimum protocol version that introduced the feature. Use supports(version, feature) to check availability.\n\nFeatures\n\n:tasks - Experimental task tracking (SEP-1686)\n:sse_priming_events - SSE priming for stream resumability (SEP-1699)\n:icon_metadata - Icon metadata for tools/resources/prompts (SEP-973)\n:tool_calling_in_sampling - Tool calling in sampling requests (SEP-1577)\n:oauth_openid_connect - OpenID Connect discovery for OAuth (PR #797)\n:oauth_incremental_scope - Incremental scope consent (SEP-835)\n:url_elicitation - URL mode elicitation requests (SEP-1036)\n:streamable_http - Streamable HTTP transport\n:resource_links - ResourceLink content type\n\n\n\n\n\n","title":"ModelContextProtocol.FEATURE_VERSIONS"},{"category":"function","location":"api/#ModelContextProtocol.negotiate_version","page":"API Reference","text":"negotiate_version(client_version::Union{AbstractString, Nothing}) -> String\n\nNegotiate the protocol version with a client per the MCP specification.\n\nIf the client requests a version we support, return that version. Otherwise, return our latest version and let the client decide whether it can proceed.\n\nArguments\n\nclient_version::Union{AbstractString, Nothing}: Protocol version requested by the client, or nothing\n\nReturns\n\nString: The negotiated protocol version\n\n\n\n\n\n","title":"ModelContextProtocol.negotiate_version"},{"category":"function","location":"api/#ModelContextProtocol.supports","page":"API Reference","text":"supports(version::AbstractString, feature::Symbol) -> Bool\n\nCheck whether a protocol version supports a specific feature.\n\nArguments\n\nversion::AbstractString: The negotiated protocol version\nfeature::Symbol: Feature identifier (see FEATURE_VERSIONS for available features)\n\nReturns\n\nBool: true if the version supports the feature\n\nExample\n\nif supports(negotiated_version, :tasks)\n    # Include task tracking in response\nend\n\n\n\n\n\n","title":"ModelContextProtocol.supports"},{"category":"function","location":"api/#ModelContextProtocol.is_supported_version","page":"API Reference","text":"is_supported_version(version::AbstractString) -> Bool\n\nCheck whether a protocol version is in our supported versions list.\n\nArguments\n\nversion::AbstractString: Protocol version string to check\n\nReturns\n\nBool: true if we support this version\n\n\n\n\n\n","title":"ModelContextProtocol.is_supported_version"},{"category":"type","location":"api/#ModelContextProtocol.OAuthConfig","page":"API Reference","text":"OAuthConfig(; issuer::String, audience::String,\n             required_scopes::Vector{String}=String[],\n             jwks_uri::Union{String,Nothing}=nothing,\n             introspection_endpoint::Union{String,Nothing}=nothing)\n\nConfigure OAuth 2.0 token validation for an MCP server.\n\nFields\n\nissuer::String: Expected token issuer (iss claim)\naudience::String: Expected audience (aud claim) - typically your server's URL\nrequired_scopes::Vector{String}: Scopes required to access the server\njwks_uri::Union{String,Nothing}: URL to fetch JSON Web Key Set for JWT validation\nintrospection_endpoint::Union{String,Nothing}: Token introspection endpoint URL\n\n\n\n\n\n","title":"ModelContextProtocol.OAuthConfig"},{"category":"type","location":"api/#ModelContextProtocol.AuthMiddleware","page":"API Reference","text":"AuthMiddleware(; config::OAuthConfig,\n                validator::TokenValidator,\n                allowlist::Union{Set{String},Nothing}=nothing,\n                case_insensitive_allowlist::Bool=true,\n                enabled::Bool=true)\n\nMiddleware for authenticating HTTP requests to an MCP server.\n\nFields\n\nconfig::OAuthConfig: OAuth configuration\nvalidator::TokenValidator: Token validation strategy\nallowlist::Union{Set{String},Nothing}: Optional set of allowed usernames/subjects\ncase_insensitive_allowlist::Bool: When true (default), allowlist username matching is case-insensitive (identity providers vary/normalize case — e.g. Keycloak lowercases GitHub logins); the opaque OAuth subject is always matched exactly. Set false for exact username matching.\nenabled::Bool: Whether authentication is enabled (for development/testing)\n\n\n\n\n\n","title":"ModelContextProtocol.AuthMiddleware"},{"category":"type","location":"api/#ModelContextProtocol.AuthResult","page":"API Reference","text":"AuthResult\n\nResult of an authentication attempt.\n\n\n\n\n\n","title":"ModelContextProtocol.AuthResult"},{"category":"type","location":"api/#ModelContextProtocol.AuthenticatedUser","page":"API Reference","text":"AuthenticatedUser(; subject::String, provider::String,\n                  username::Union{String,Nothing}=nothing,\n                  scopes::Vector{String}=String[],\n                  claims::Dict{String,Any}=Dict{String,Any}())\n\nRepresent an authenticated user after successful token validation.\n\nFields\n\nsubject::String: Unique user identifier (sub claim from token)\nprovider::String: Authentication provider name (e.g., \"github\", \"google\")\nusername::Union{String,Nothing}: Human-readable username if available\nscopes::Vector{String}: Granted OAuth scopes\nclaims::Dict{String,Any}: Raw claims from the token\n\n\n\n\n\n","title":"ModelContextProtocol.AuthenticatedUser"},{"category":"function","location":"api/#ModelContextProtocol.create_auth_middleware","page":"API Reference","text":"create_auth_middleware(config::OAuthConfig;\n                      validator::TokenValidator,\n                      allowlist::Union{Set{String},Nothing}=nothing,\n                      enabled::Bool=true) -> AuthMiddleware\n\nCreate an authentication middleware for the HTTP transport.\n\nArguments\n\nconfig::OAuthConfig: OAuth configuration\nvalidator::TokenValidator: Token validation strategy (REQUIRED — no default, so an unsafe validator is never selected implicitly. Note JWTValidator does not verify signatures; prefer IntrospectionValidator or GitHubOAuthValidator for tokens from external issuers.)\nallowlist::Union{Set{String},Nothing}: Optional allowlist of usernames/subjects\nenabled::Bool: Whether auth is enabled (default: true)\n\nExample\n\nauth = create_auth_middleware(\n    OAuthConfig(\n        issuer = \"https://github.com\",\n        audience = \"my-mcp-server\"\n    ),\n    validator = IntrospectionValidator(client_id = \"id\", client_secret = \"secret\"),\n    allowlist = Set([\"user1\", \"user2\"])\n)\n\n\n\n\n\n","title":"ModelContextProtocol.create_auth_middleware"},{"category":"function","location":"api/#ModelContextProtocol.create_simple_auth","page":"API Reference","text":"create_simple_auth(tokens::Dict{String,String};\n                  allowlist::Union{Set{String},Nothing}=nothing) -> AuthMiddleware\n\nCreate a simple API key-based authentication middleware.\n\nArguments\n\ntokens::Dict{String,String}: Map of API keys to usernames\nallowlist::Union{Set{String},Nothing}: Optional additional allowlist\n\nExample\n\nauth = create_simple_auth(Dict(\n    \"sk-abc123\" => \"user1\",\n    \"sk-def456\" => \"user2\"\n))\n\n\n\n\n\n","title":"ModelContextProtocol.create_simple_auth"},{"category":"function","location":"api/#ModelContextProtocol.disable_auth","page":"API Reference","text":"disable_auth() -> AuthMiddleware\n\nCreate a disabled auth middleware (for development/testing). All requests will be allowed with an anonymous user.\n\n\n\n\n\n","title":"ModelContextProtocol.disable_auth"},{"category":"function","location":"api/#ModelContextProtocol.is_auth_enabled","page":"API Reference","text":"is_auth_enabled(transport::HttpTransport) -> Bool\n\nReturn whether OAuth Resource Server token validation is enabled on this transport.\n\nArguments\n\ntransport::HttpTransport: The transport instance\n\nReturns\n\nBool: true if an enabled AuthMiddleware is configured\n\n\n\n\n\n","title":"ModelContextProtocol.is_auth_enabled"},{"category":"type","location":"api/#ModelContextProtocol.AuthProvider","page":"API Reference","text":"AuthProvider\n\nAbstract type for authentication providers (GitHub, Google, custom OAuth servers).\n\n\n\n\n\n","title":"ModelContextProtocol.AuthProvider"},{"category":"type","location":"api/#ModelContextProtocol.TokenValidator","page":"API Reference","text":"TokenValidator\n\nAbstract type for token validation strategies (JWT, introspection).\n\n\n\n\n\n","title":"ModelContextProtocol.TokenValidator"},{"category":"type","location":"api/#ModelContextProtocol.SimpleTokenValidator","page":"API Reference","text":"SimpleTokenValidator(; tokens::Dict{String,AuthenticatedUser})\n\nSimple token validator using a static mapping of tokens to users. Useful for API keys and development/testing.\n\nnote: Development use\nLookups are plain dictionary comparisons (not constant-time) and tokens are held in memory in plaintext. Intended for development and trusted static API keys, not as a general-purpose production token store.\n\nFields\n\ntokens::Dict{String,AuthenticatedUser}: Map of valid tokens to user info\n\n\n\n\n\n","title":"ModelContextProtocol.SimpleTokenValidator"},{"category":"type","location":"api/#ModelContextProtocol.JWTValidator","page":"API Reference","text":"JWTValidator(; insecure_skip_signature_verification::Bool, clock_skew_seconds::Int=60)\n\nJWT validator that checks claims (iss, aud, exp, nbf, scope) but does not verify the token's cryptographic signature.\n\ndanger: No signature verification — explicit opt-in required\nThis validator decodes and validates JWT claims but does not verify the token's cryptographic signature, so any caller can forge the issuer, audience, and scopes. It therefore refuses to construct unless you pass insecure_skip_signature_verification = true.For tokens from an authorization server, use JWKSValidator (signature verification via JWKS — the recommended path) or IntrospectionValidator (RFC 7662). Choose JWTValidator only when this server sits behind a gateway that has already verified the signature.\n\nFields\n\njwks_cache::Dict{String,Any}: Unused (retained for struct-layout compatibility)\nclock_skew_seconds::Int: Allowed clock skew for exp/nbf validation\n\n\n\n\n\n","title":"ModelContextProtocol.JWTValidator"},{"category":"type","location":"api/#ModelContextProtocol.JWKSValidator","page":"API Reference","text":"JWKSValidator(jwks_uri::String; allowed_algs=[\"RS256\", \"RS384\", \"RS512\"],\n              clock_skew_seconds=60, refresh_interval_seconds=300,\n              allow_insecure_http=false)\nJWKSValidator(keyset::JWTs.JWKSet; ...)\n\nJWT validator with cryptographic signature verification against a JSON Web Key Set (RFC 7517), plus the same claims validation as JWTValidator (iss, aud, exp, nbf, scopes — all fail-closed). This is the recommended validator for JWTs from external authorization servers (Keycloak, Auth0, GitHub Apps, etc.).\n\nKeys are fetched lazily: construction never touches the network, so a server can start while its authorization server is down (requests fail closed until keys load). An unknown kid triggers a JWKS re-fetch (key rotation) at most once per refresh_interval_seconds — rate-limited so attacker-supplied kid values cannot hammer the JWKS endpoint. Fetches use bounded HTTP timeouts and a response size cap (MAX_JWKS_BYTES), and never hold the validator lock during network I/O.\n\nfile:// URIs are supported for local key sets. An http:// URL is rejected at construction unless allow_insecure_http=true (a plaintext JWKS lets an on-path attacker swap in their own signing key — use only for localhost/testing). The second constructor accepts a pre-built JWTs.JWKSet (e.g. static keys) directly; with no URL it never refreshes.\n\nnote: Algorithm allowlist\nTokens whose header alg is not in allowed_algs are rejected before any cryptography runs (this also rejects alg=none). The default allows the RSA family only; do not add HMAC algorithms (HS*) for keys published in a public JWKS document.\n\nFields\n\nkeyset::JWTs.JWKSet: Key set (url-backed or static) holding keys by kid\nallowed_algs::Vector{String}: Permitted JWT signature algorithms\nclock_skew_seconds::Int: Allowed clock skew for exp/nbf validation\nrefresh_interval_seconds::Float64: Minimum seconds between JWKS fetch attempts (>= 0)\n\n\n\n\n\n","title":"ModelContextProtocol.JWKSValidator"},{"category":"type","location":"api/#ModelContextProtocol.IntrospectionValidator","page":"API Reference","text":"IntrospectionValidator(; client_id=nothing, client_secret=nothing)\n\nToken validator using OAuth 2.0 Token Introspection (RFC 7662). Used for opaque tokens that cannot be validated locally.\n\nFields\n\nclient_id::Union{String,Nothing}: Client ID for introspection auth\nclient_secret::Union{String,Nothing}: Client secret for introspection auth\n\n\n\n\n\n","title":"ModelContextProtocol.IntrospectionValidator"},{"category":"type","location":"api/#ModelContextProtocol.GitHubOAuthValidator","page":"API Reference","text":"GitHubOAuthValidator(; cache_ttl_seconds::Int=300)\n\nToken validator for GitHub OAuth access tokens. Validates tokens by calling GitHub's /user API endpoint.\n\nFields\n\ncache_ttl_seconds::Int: How long to cache user info (default: 5 minutes)\nuser_cache::Dict{String,Tuple{AuthenticatedUser,DateTime}}: Cache of validated tokens\ncache_lock::ReentrantLock: Lock for thread-safe cache access\n\n\n\n\n\n","title":"ModelContextProtocol.GitHubOAuthValidator"},{"category":"function","location":"api/#ModelContextProtocol.create_github_auth","page":"API Reference","text":"create_github_auth(; allowed_users::Union{Vector{String},Set{String}}=String[],\n                    required_org::Union{String,Nothing}=nothing,\n                    cache_ttl_seconds::Int=300) -> AuthMiddleware\n\nCreate an authentication middleware configured for GitHub OAuth.\n\nArguments\n\nallowed_users: List of GitHub usernames allowed to access the server\nrequired_org: Optionally require membership in a GitHub organization\ncache_ttl_seconds: How long to cache validated tokens (default: 5 minutes)\n\nReturns\n\nAuthMiddleware configured for GitHub OAuth validation.\n\nExample\n\n# Allow specific users\nauth = create_github_auth(\n    allowed_users = [\"user1\", \"user2\", \"user3\"]\n)\n\n# Require organization membership\nauth = create_github_auth(\n    required_org = \"JuliaSMLM\"\n)\n\n# Both user allowlist and org requirement\nauth = create_github_auth(\n    allowed_users = [\"user1\", \"user2\"],\n    required_org = \"LidkeLab\"\n)\n\n\n\n\n\n","title":"ModelContextProtocol.create_github_auth"},{"category":"function","location":"api/#ModelContextProtocol.clear_cache!","page":"API Reference","text":"clear_cache!(validator::GitHubOAuthValidator)\n\nClear the token validation cache.\n\n\n\n\n\n","title":"ModelContextProtocol.clear_cache!"},{"category":"type","location":"api/#ModelContextProtocol.ProtectedResourceMetadata","page":"API Reference","text":"ProtectedResourceMetadata(; resource::String,\n                           authorization_servers::Vector{String},\n                           scopes_supported::Vector{String}=String[],\n                           bearer_methods_supported::Vector{String}=[\"header\"])\n\nMCP Protected Resource Metadata per RFC 9728. Served at .well-known/oauth-protected-resource.\n\nFields\n\nresource::String: The protected resource identifier (your server URL)\nauthorization_servers::Vector{String}: URLs of authorization servers that can issue tokens\nscopes_supported::Vector{String}: OAuth scopes the resource understands\nbearer_methods_supported::Vector{String}: How tokens can be sent (header, body, query)\n\n\n\n\n\n","title":"ModelContextProtocol.ProtectedResourceMetadata"},{"category":"function","location":"api/#ModelContextProtocol.create_protected_resource_metadata","page":"API Reference","text":"create_protected_resource_metadata(resource_url::String,\n                                   authorization_servers::Vector{String};\n                                   scopes::Vector{String}=String[]) -> ProtectedResourceMetadata\n\nCreate Protected Resource Metadata for the MCP server.\n\nArguments\n\nresource_url::String: The URL of your MCP server (the protected resource)\nauthorization_servers::Vector{String}: URLs of OAuth authorization servers\nscopes::Vector{String}: OAuth scopes the server understands\n\nExample\n\nmetadata = create_protected_resource_metadata(\n    \"https://mcp.example.com\",\n    [\"https://github.com/login/oauth\"],\n    scopes = [\"read:user\", \"repo\"]\n)\n\n\n\n\n\n","title":"ModelContextProtocol.create_protected_resource_metadata"},{"category":"function","location":"api/#ModelContextProtocol.create_github_resource_metadata","page":"API Reference","text":"create_github_resource_metadata(resource_url::String;\n                               scopes::Vector{String}=[\"read:user\"]) -> ProtectedResourceMetadata\n\nCreate Protected Resource Metadata configured for GitHub OAuth.\n\nArguments\n\nresource_url::String: Your MCP server URL\nscopes::Vector{String}: GitHub OAuth scopes to request (default: [\"read:user\"])\n\nExample\n\nmetadata = create_github_resource_metadata(\n    \"https://mcp.lidkelab.org\",\n    scopes = [\"read:user\", \"read:org\"]\n)\n\n\n\n\n\n","title":"ModelContextProtocol.create_github_resource_metadata"},{"category":"function","location":"api/#ModelContextProtocol.authenticate_request","page":"API Reference","text":"authenticate_request(middleware::AuthMiddleware, authorization_header::Union{String,Nothing}) -> AuthResult\n\nAuthenticate an HTTP request using the auth middleware.\n\nArguments\n\nmiddleware::AuthMiddleware: The authentication middleware\nauthorization_header::Union{String,Nothing}: The Authorization header value\n\nReturns\n\nAuthResult indicating success with user info, or failure with error details.\n\n\n\n\n\n","title":"ModelContextProtocol.authenticate_request"},{"category":"function","location":"api/#ModelContextProtocol.validate_token","page":"API Reference","text":"validate_token(validator::TokenValidator, token::AbstractString, config::OAuthConfig) -> AuthResult\n\nValidate an OAuth token using the specified validator.\n\nArguments\n\nvalidator::TokenValidator: The validation strategy to use\ntoken::AbstractString: The token to validate\nconfig::OAuthConfig: OAuth configuration with expected issuer, audience, etc.\n\nReturns\n\nAuthResult: Success with user info, or failure with error details\n\n\n\n\n\n","title":"ModelContextProtocol.validate_token"},{"category":"function","location":"api/#ModelContextProtocol.extract_bearer_token","page":"API Reference","text":"extract_bearer_token(authorization_header::String) -> Union{String,Nothing}\n\nExtract Bearer token from Authorization header.\n\nArguments\n\nauthorization_header::String: The Authorization header value\n\nReturns\n\nThe token if present and valid format, nothing otherwise.\n\n\n\n\n\n","title":"ModelContextProtocol.extract_bearer_token"},{"category":"function","location":"api/#ModelContextProtocol.content2dict","page":"API Reference","text":"content2dict(content::Content) -> Dict{String,Any}\n\nConvert a Content object to its dictionary representation for JSON serialization.\n\nArguments\n\ncontent::Content: The content object to convert\n\nReturns\n\nDict{String,Any}: Dictionary representation of the content\n\nExamples\n\ntext_content = TextContent(text=\"Hello\", type=\"text\")\ndict = content2dict(text_content)\n# Returns: Dict(\"type\" => \"text\", \"text\" => \"Hello\", \"annotations\" => Dict())\n\n\n\n\n\n","title":"ModelContextProtocol.content2dict"},{"category":"type","location":"api/#ModelContextProtocol.CallToolParams","page":"API Reference","text":"CallToolParams(; name::String, arguments::Union{Dict{String,Any},Nothing}=nothing) <: RequestParams\n\nParameters for invoking a specific tool on an MCP server.\n\nFields\n\nname::String: Name of the tool to call\narguments::Union{Dict{String,Any},Nothing}: Optional arguments to pass to the tool\ntask::Union{Dict{String,Any},Nothing}: When present, the caller requests task-augmented execution (MCP Tasks, experimental); may carry a requested \"ttl\" in milliseconds\n\n\n\n\n\n","title":"ModelContextProtocol.CallToolParams"},{"category":"type","location":"api/#ModelContextProtocol.CancelTaskParams","page":"API Reference","text":"CancelTaskParams(; taskId::String) <: RequestParams\n\nParameters for a tasks/cancel request.\n\nFields\n\ntaskId::String: The task identifier to cancel\n\n\n\n\n\n","title":"ModelContextProtocol.CancelTaskParams"},{"category":"type","location":"api/#ModelContextProtocol.ClientCapabilities","page":"API Reference","text":"ClientCapabilities(; experimental::Union{Dict{String,Dict{String,Any}},Nothing}=nothing,\n                roots::Union{Dict{String,Bool},Nothing}=nothing,\n                sampling::Union{Dict{String,Any},Nothing}=nothing)\n\nCapabilities reported by an MCP client during initialization.\n\nFields\n\nexperimental::Union{Dict{String,Dict{String,Any}},Nothing}: Experimental features supported\nroots::Union{Dict{String,Bool},Nothing}: Root directories client has access to\nsampling::Union{Dict{String,Any},Nothing}: Sampling capabilities for model generation\n\n\n\n\n\n","title":"ModelContextProtocol.ClientCapabilities"},{"category":"type","location":"api/#ModelContextProtocol.CompleteParams","page":"API Reference","text":"CompleteParams(; ref::CompletionReference, argument::CompletionArgument,\n               context::Union{Dict{String,Any},Nothing}=nothing) <: RequestParams\n\nParameters for a completion/complete request.\n\nFields\n\nref::CompletionReference: The prompt or resource template being completed\nargument::CompletionArgument: The argument name and partial value\ncontext::Union{Dict{String,Any},Nothing}: Optional context; its arguments entry carries already-resolved argument values\n\n\n\n\n\n","title":"ModelContextProtocol.CompleteParams"},{"category":"type","location":"api/#ModelContextProtocol.CompletionArgument","page":"API Reference","text":"CompletionArgument(; name::String, value::String)\n\nName the argument being completed and the partial value typed so far.\n\nFields\n\nname::String: The argument (or template variable) name\nvalue::String: The partial value to complete\n\n\n\n\n\n","title":"ModelContextProtocol.CompletionArgument"},{"category":"type","location":"api/#ModelContextProtocol.CompletionReference","page":"API Reference","text":"CompletionReference(; type::String, name::Union{String,Nothing}=nothing,\n                    uri::Union{String,Nothing}=nothing)\n\nIdentify what a completion/complete request targets: a prompt (type of ref/prompt with name) or a resource template (type of ref/resource with uri naming the URI template).\n\nFields\n\ntype::String: \"ref/prompt\" or \"ref/resource\"\nname::Union{String,Nothing}: The prompt name (for ref/prompt)\nuri::Union{String,Nothing}: The URI template (for ref/resource)\n\n\n\n\n\n","title":"ModelContextProtocol.CompletionReference"},{"category":"type","location":"api/#ModelContextProtocol.ErrorInfo","page":"API Reference","text":"ErrorInfo(; code::Int, message::String, data::Union{Dict{String,Any},Nothing}=nothing)\n\nError information structure for JSON-RPC error responses.\n\nFields\n\ncode::Int: Numeric error code (predefined in ErrorCodes module)\nmessage::String: Human-readable error description\ndata::Union{Dict{String,Any},Nothing}: Optional additional error details\n\n\n\n\n\n","title":"ModelContextProtocol.ErrorInfo"},{"category":"type","location":"api/#ModelContextProtocol.GetPromptParams","page":"API Reference","text":"GetPromptParams(; name::String, arguments::Union{Dict{String,String},Nothing}=nothing) <: RequestParams\n\nParameters for requesting a specific prompt from an MCP server.\n\nFields\n\nname::String: Name of the prompt to retrieve\narguments::Union{Dict{String,String},Nothing}: Optional arguments to apply to the prompt template\n\n\n\n\n\n","title":"ModelContextProtocol.GetPromptParams"},{"category":"type","location":"api/#ModelContextProtocol.GetPromptResult","page":"API Reference","text":"GetPromptResult(; description::String, messages::Vector{PromptMessage}) <: ResponseResult\n\nResult returned from a get prompt request.\n\nFields\n\ndescription::String: Description of the prompt\nmessages::Vector{PromptMessage}: The prompt messages with template variables replaced\n\n\n\n\n\n","title":"ModelContextProtocol.GetPromptResult"},{"category":"type","location":"api/#ModelContextProtocol.GetTaskParams","page":"API Reference","text":"GetTaskParams(; taskId::String) <: RequestParams\n\nParameters for a tasks/get status poll.\n\nFields\n\ntaskId::String: The task identifier to query\n\n\n\n\n\n","title":"ModelContextProtocol.GetTaskParams"},{"category":"type","location":"api/#ModelContextProtocol.Implementation","page":"API Reference","text":"Implementation(; name::String=\"default-client\", version::String=\"1.0.0\",\n               title::Union{String,Nothing}=nothing,\n               description::Union{String,Nothing}=nothing)\n\nInformation about a client or server implementation of the MCP protocol.\n\nFields\n\nname::String: Name of the implementation\nversion::String: Version string of the implementation\ntitle::Union{String,Nothing}: Optional human-readable display name\ndescription::Union{String,Nothing}: Optional human-readable description (MCP 2025-11-25)\n\n\n\n\n\n","title":"ModelContextProtocol.Implementation"},{"category":"type","location":"api/#ModelContextProtocol.InitializeParams","page":"API Reference","text":"InitializeParams(; capabilities::ClientCapabilities=ClientCapabilities(),\n               clientInfo::Implementation=Implementation(),\n               protocolVersion::Union{String,Nothing}=nothing) <: RequestParams\n\nParameters for MCP protocol initialization requests.\n\nFields\n\ncapabilities::ClientCapabilities: Client capabilities being reported\nclientInfo::Implementation: Information about the client implementation  \nprotocolVersion::Union{String,Nothing}: Version of the MCP protocol requested by the client. The server negotiates against SUPPORTED_PROTOCOL_VERSIONS (latest: LATEST_PROTOCOL_VERSION); see negotiate_version.\n\n\n\n\n\n","title":"ModelContextProtocol.InitializeParams"},{"category":"type","location":"api/#ModelContextProtocol.InitializeResult","page":"API Reference","text":"InitializeResult(; serverInfo::Dict{String,Any}, capabilities::Dict{String,Any},\n               protocolVersion::String, instructions::String=\"\") <: ResponseResult\n\nResult returned in response to MCP protocol initialization.\n\nFields\n\nserverInfo::Dict{String,Any}: Information about the server implementation\ncapabilities::Dict{String,Any}: Server capabilities being reported\nprotocolVersion::String: Version of the MCP protocol being used\ninstructions::String: Optional usage instructions for clients\n\n\n\n\n\n","title":"ModelContextProtocol.InitializeResult"},{"category":"type","location":"api/#ModelContextProtocol.JSONRPCError","page":"API Reference","text":"JSONRPCError(; id::Union{RequestId,Nothing}, error::ErrorInfo) <: Response\n\nJSON-RPC error response message returned when requests fail.\n\nFields\n\nid::Union{RequestId,Nothing}: Identifier matching the request this is responding to, or null\nerror::ErrorInfo: Information about the error that occurred\n\n\n\n\n\n","title":"ModelContextProtocol.JSONRPCError"},{"category":"type","location":"api/#ModelContextProtocol.JSONRPCNotification","page":"API Reference","text":"JSONRPCNotification(; method::String, \n                   params::Union{RequestParams,Dict{String,Any}}) <: Notification\n\nJSON-RPC notification message that does not expect a response.\n\nFields\n\nmethod::String: Name of the notification method\nparams::Union{RequestParams,Dict{String,Any}}: Parameters for the notification\n\n\n\n\n\n","title":"ModelContextProtocol.JSONRPCNotification"},{"category":"type","location":"api/#ModelContextProtocol.JSONRPCRequest","page":"API Reference","text":"JSONRPCRequest(; id::RequestId, method::String, \n             params::Union{RequestParams, Nothing}, \n             meta::RequestMeta=RequestMeta()) <: Request\n\nJSON-RPC request message used to invoke methods on the server.\n\nFields\n\nid::RequestId: Unique identifier for the request\nmethod::String: Name of the method to invoke\nparams::Union{RequestParams, Nothing}: Parameters for the method\nmeta::RequestMeta: Additional metadata for the request\n\n\n\n\n\n","title":"ModelContextProtocol.JSONRPCRequest"},{"category":"type","location":"api/#ModelContextProtocol.JSONRPCResponse","page":"API Reference","text":"JSONRPCResponse(; id::RequestId, result::Union{ResponseResult,AbstractDict{String,Any}}) <: Response\n\nJSON-RPC response message returned for successful requests.\n\nFields\n\nid::RequestId: Identifier matching the request this is responding to\nresult::Union{ResponseResult,AbstractDict{String,Any}}: Results of the method execution\n\n\n\n\n\n","title":"ModelContextProtocol.JSONRPCResponse"},{"category":"type","location":"api/#ModelContextProtocol.ListPromptsParams","page":"API Reference","text":"ListPromptsParams(; cursor::Union{String,Nothing}=nothing) <: RequestParams\n\nParameters for requesting a list of available prompts from an MCP server.\n\nFields\n\ncursor::Union{String,Nothing}: Optional pagination cursor for long prompt lists\n\n\n\n\n\n","title":"ModelContextProtocol.ListPromptsParams"},{"category":"type","location":"api/#ModelContextProtocol.ListPromptsResult","page":"API Reference","text":"ListPromptsResult(; prompts::Vector{Dict{String,Any}}, \n                nextCursor::Union{String,Nothing}=nothing) <: ResponseResult\n\nResult returned from a list prompts request.\n\nFields\n\nprompts::Vector{Dict{String,Any}}: List of available prompts with their metadata\nnextCursor::Union{String,Nothing}: Optional pagination cursor for fetching more prompts\n\n\n\n\n\n","title":"ModelContextProtocol.ListPromptsResult"},{"category":"type","location":"api/#ModelContextProtocol.ListResourceTemplatesParams","page":"API Reference","text":"ListResourceTemplatesParams(; cursor::Union{String,Nothing}=nothing) <: RequestParams\n\nParameters for a resources/templates/list request.\n\nFields\n\ncursor::Union{String,Nothing}: Optional pagination cursor for long template lists\n\n\n\n\n\n","title":"ModelContextProtocol.ListResourceTemplatesParams"},{"category":"type","location":"api/#ModelContextProtocol.ListResourcesParams","page":"API Reference","text":"ListResourcesParams(; cursor::Union{String,Nothing}=nothing) <: RequestParams\n\nParameters for requesting a list of available resources from an MCP server.\n\nFields\n\ncursor::Union{String,Nothing}: Optional pagination cursor for long resource lists\n\n\n\n\n\n","title":"ModelContextProtocol.ListResourcesParams"},{"category":"type","location":"api/#ModelContextProtocol.ListResourcesResult","page":"API Reference","text":"ListResourcesResult(; resources::Vector{Dict{String,Any}}, \n                  nextCursor::Union{String,Nothing}=nothing) <: ResponseResult\n\nResult returned from a list resources request.\n\nFields\n\nresources::Vector{Dict{String,Any}}: List of available resources with their metadata\nnextCursor::Union{String,Nothing}: Optional pagination cursor for fetching more resources\n\n\n\n\n\n","title":"ModelContextProtocol.ListResourcesResult"},{"category":"type","location":"api/#ModelContextProtocol.ListTasksParams","page":"API Reference","text":"ListTasksParams(; cursor::Union{String,Nothing}=nothing) <: RequestParams\n\nParameters for a paginated tasks/list request.\n\nFields\n\ncursor::Union{String,Nothing}: Opaque pagination cursor from a previous response\n\n\n\n\n\n","title":"ModelContextProtocol.ListTasksParams"},{"category":"type","location":"api/#ModelContextProtocol.ListToolsParams","page":"API Reference","text":"ListToolsParams(; cursor::Union{String,Nothing}=nothing) <: RequestParams\n\nParameters for requesting a list of available tools from an MCP server.\n\nFields\n\ncursor::Union{String,Nothing}: Optional pagination cursor for long tool lists\n\n\n\n\n\n","title":"ModelContextProtocol.ListToolsParams"},{"category":"type","location":"api/#ModelContextProtocol.ListToolsResult","page":"API Reference","text":"ListToolsResult(; tools::Vector{Dict{String,Any}}, \n              nextCursor::Union{String,Nothing}=nothing) <: ResponseResult\n\nResult returned from a list tools request.\n\nFields\n\ntools::Vector{Dict{String,Any}}: List of available tools with their metadata\nnextCursor::Union{String,Nothing}: Optional pagination cursor for fetching more tools\n\n\n\n\n\n","title":"ModelContextProtocol.ListToolsResult"},{"category":"type","location":"api/#ModelContextProtocol.ProgressParams","page":"API Reference","text":"ProgressParams(; progress_token::ProgressToken, progress::Float64,\n             total::Union{Float64,Nothing}=nothing) <: RequestParams\n\nParameters for progress notifications during long-running operations.\n\nFields\n\nprogress_token::ProgressToken: Token identifying the operation being reported on\nprogress::Float64: Current progress value\ntotal::Union{Float64,Nothing}: Optional total expected value\n\n\n\n\n\n","title":"ModelContextProtocol.ProgressParams"},{"category":"type","location":"api/#ModelContextProtocol.ReadResourceParams","page":"API Reference","text":"ReadResourceParams(; uri::String) <: RequestParams\n\nParameters for requesting the contents of a specific resource.\n\nFields\n\nuri::String: URI identifier of the resource to read\n\n\n\n\n\n","title":"ModelContextProtocol.ReadResourceParams"},{"category":"type","location":"api/#ModelContextProtocol.ReadResourceResult","page":"API Reference","text":"ReadResourceResult(; contents::Vector{Dict{String,Any}}) <: ResponseResult\n\nResult returned from a read resource request.\n\nFields\n\ncontents::Vector{Dict{String,Any}}: The contents of the requested resource\n\n\n\n\n\n","title":"ModelContextProtocol.ReadResourceResult"},{"category":"type","location":"api/#ModelContextProtocol.RequestMeta","page":"API Reference","text":"RequestMeta(; progress_token::Union{ProgressToken,Nothing}=nothing,\n            protocol_version::Union{String,Nothing}=nothing,\n            client_capabilities::Union{Dict{String,Any},Nothing}=nothing,\n            client_info::Union{Dict{String,Any},Nothing}=nothing)\n\nMetadata for MCP protocol requests: progress tracking, plus the modern-era (2026-07-28+) per-request protocol fields carried in _meta under io.modelcontextprotocol/* keys. A non-nothing protocol_version marks the request as modern-era (stateless, no initialize handshake) and routes it through handle_modern_request.\n\nFields\n\nprogress_token::Union{ProgressToken,Nothing}: Optional token for tracking request progress\nprotocol_version::Union{String,Nothing}: io.modelcontextprotocol/protocolVersion (required on every modern-era request; absent on legacy requests). Only string values are honored — a non-string value is treated as absent.\nclient_capabilities::Any: io.modelcontextprotocol/clientCapabilities (required on every modern-era request), kept as the raw parsed JSON object — no re-encoding copy is made\nclient_info::Any: io.modelcontextprotocol/clientInfo (optional), kept as the raw parsed JSON object\nlog_level::Union{String,Nothing}: io.modelcontextprotocol/logLevel (optional) — the client's per-request opt-in to notifications/message delivery. Only string values are stored; validation against the recognized levels happens in handle_modern_request\nhas_log_level::Bool: Whether the logLevel key was present at all. Presence is tracked separately from the typed value so a present-but-invalid level is REJECTED (-32602 per the spec), never silently treated as absent\n\n\n\n\n\n","title":"ModelContextProtocol.RequestMeta"},{"category":"type","location":"api/#ModelContextProtocol.SetLevelParams","page":"API Reference","text":"SetLevelParams(; level::String) <: RequestParams\n\nParameters for logging/setLevel requests.\n\nFields\n\nlevel::String: Minimum log level the client wants to receive (one of the MCP/RFC-5424 levels: \"debug\", \"info\", \"notice\", \"warning\", \"error\", \"critical\", \"alert\", \"emergency\")\n\n\n\n\n\n","title":"ModelContextProtocol.SetLevelParams"},{"category":"type","location":"api/#ModelContextProtocol.SubscribeParams","page":"API Reference","text":"SubscribeParams(; uri::String) <: RequestParams\n\nParameters for resources/subscribe requests.\n\nFields\n\nuri::String: URI of the resource the client wants update notifications for\n\n\n\n\n\n","title":"ModelContextProtocol.SubscribeParams"},{"category":"type","location":"api/#ModelContextProtocol.SubscriptionsListenParams","page":"API Reference","text":"SubscriptionsListenParams(; notifications::Union{Nothing,Dict{String,Any}}=nothing) <: RequestParams\n\nParameters for subscriptions/listen requests (modern era).\n\nFields\n\nnotifications::Union{Nothing,Dict{String,Any}}: The notification-type filter — toolsListChanged/promptsListChanged/resourcesListChanged booleans and a resourceSubscriptions array of resource URIs. Required: an absent or malformed filter is rejected with -32602 by the handler (see parse_subscription_filter); nothing here only represents the not-yet-validated wire state.\n\n\n\n\n\n","title":"ModelContextProtocol.SubscriptionsListenParams"},{"category":"type","location":"api/#ModelContextProtocol.TaskResultParams","page":"API Reference","text":"TaskResultParams(; taskId::String) <: RequestParams\n\nParameters for a tasks/result payload retrieval. The response blocks until the task reaches a terminal status and then matches the original request's result type.\n\nFields\n\ntaskId::String: The task identifier to retrieve results for\n\n\n\n\n\n","title":"ModelContextProtocol.TaskResultParams"},{"category":"type","location":"api/#ModelContextProtocol.UnsubscribeParams","page":"API Reference","text":"UnsubscribeParams(; uri::String) <: RequestParams\n\nParameters for resources/unsubscribe requests.\n\nFields\n\nuri::String: URI of the resource to stop receiving update notifications for\n\n\n\n\n\n","title":"ModelContextProtocol.UnsubscribeParams"},{"category":"type","location":"api/#ModelContextProtocol.UpdateTaskParams","page":"API Reference","text":"UpdateTaskParams(; taskId::String, inputResponses::Dict{String,Any}) <: RequestParams\n\nParameters for a tasks/update request (tasks extension, SEP-2663): the client's responses to outstanding inputRequests surfaced by tasks/get on an input_required task. Both fields are required — a request missing either fails typed parsing and is rejected with -32602.\n\nFields\n\ntaskId::String: The task identifier to update\ninputResponses::Dict{String,Any}: Responses keyed to match the server-issued inputRequests keys\n\n\n\n\n\n","title":"ModelContextProtocol.UpdateTaskParams"},{"category":"method","location":"api/#ModelContextProtocol.get_params_type-Tuple{String}","page":"API Reference","text":"get_params_type(method::String) -> Union{Type,Nothing}\n\nGet the appropriate parameter type for a given JSON-RPC method name.\n\nArguments\n\nmethod::String: The JSON-RPC method name\n\nReturns\n\nUnion{Type,Nothing}: The Julia type to use for parsing parameters, or nothing if no specific type is defined\n\n\n\n\n\n","title":"ModelContextProtocol.get_params_type"},{"category":"method","location":"api/#ModelContextProtocol.get_result_type-Tuple{Union{Int64, String}}","page":"API Reference","text":"get_result_type(id::RequestId) -> Union{Type{<:ResponseResult},Nothing}\n\nGet the expected result type for a response based on the request ID.\n\nArguments\n\nid::RequestId: The request ID to look up\n\nReturns\n\nUnion{Type{<:ResponseResult},Nothing}: The expected response result type, or nothing if not known\n\nNote: This is a placeholder that needs to be implemented with request tracking.\n\n\n\n\n\n","title":"ModelContextProtocol.get_result_type"},{"category":"method","location":"api/#ModelContextProtocol.parse_error_response-Tuple{JSON3.Object}","page":"API Reference","text":"parse_error_response(raw::JSON3.Object) -> Response\n\nParse a JSON-RPC error response object into a typed Response struct.\n\nArguments\n\nraw::JSON3.Object: The parsed JSON object representing an error response\n\nReturns\n\nResponse: A JSONRPCError with properly typed error information\n\n\n\n\n\n","title":"ModelContextProtocol.parse_error_response"},{"category":"method","location":"api/#ModelContextProtocol.parse_message-Tuple{String}","page":"API Reference","text":"parse_message(json::String) -> MCPMessage\n\nParse a JSON-RPC message string into the appropriate typed message object.\n\nArguments\n\njson::String: The raw JSON-RPC message string\n\nReturns\n\nMCPMessage: A typed MCPMessage subtype (JSONRPCRequest, JSONRPCResponse, JSONRPCNotification, or JSONRPCError)\n\n\n\n\n\n","title":"ModelContextProtocol.parse_message"},{"category":"method","location":"api/#ModelContextProtocol.parse_notification-Tuple{JSON3.Object}","page":"API Reference","text":"parse_notification(raw::JSON3.Object) -> Notification\n\nParse a JSON-RPC notification object into a typed Notification struct.\n\nArguments\n\nraw::JSON3.Object: The parsed JSON object representing a notification\n\nReturns\n\nNotification: A JSONRPCNotification with properly typed parameters if possible\n\n\n\n\n\n","title":"ModelContextProtocol.parse_notification"},{"category":"method","location":"api/#ModelContextProtocol.parse_request-Tuple{JSON3.Object}","page":"API Reference","text":"parse_request(raw::JSON3.Object) -> Request\n\nParse a JSON-RPC request object into a typed Request struct.\n\nArguments\n\nraw::JSON3.Object: The parsed JSON object representing a request\n\nReturns\n\nRequest: A JSONRPCRequest with properly typed parameters based on the method\n\n\n\n\n\n","title":"ModelContextProtocol.parse_request"},{"category":"method","location":"api/#ModelContextProtocol.parse_success_response-Tuple{JSON3.Object}","page":"API Reference","text":"parse_success_response(raw::JSON3.Object) -> Response\n\nParse a successful JSON-RPC response object into a typed Response struct.\n\nArguments\n\nraw::JSON3.Object: The parsed JSON object representing a successful response\n\nReturns\n\nResponse: A JSONRPCResponse with properly typed result if possible, or JSONRPCError if parsing fails\n\n\n\n\n\n","title":"ModelContextProtocol.parse_success_response"},{"category":"method","location":"api/#ModelContextProtocol.serialize_message-Tuple{ModelContextProtocol.MCPMessage}","page":"API Reference","text":"serialize_message(msg::MCPMessage) -> String\n\nSerialize an MCP message object into a JSON-RPC compliant string.\n\nArguments\n\nmsg::MCPMessage: The message object to serialize (Request, Response, Notification, or Error)\n\nReturns\n\nString: A JSON string representation of the message following the JSON-RPC 2.0 specification\n\n\n\n\n\n","title":"ModelContextProtocol.serialize_message"},{"category":"type","location":"api/#ModelContextProtocol.HandlerResult","page":"API Reference","text":"HandlerResult(; response::Union{Response,Nothing}=nothing,\n            error::Union{ErrorInfo,Nothing}=nothing)\n\nRepresent the result of handling a request.\n\nFields\n\nresponse::Union{Response,Nothing}: The response to send (if successful)\nerror::Union{ErrorInfo,Nothing}: Error information (if request failed)\ndeferred::Bool: When true, neither field is set and the response will be delivered out-of-loop via deliver_response (used by the blocking tasks/result)\n\nA HandlerResult must contain either a response, an error, or be deferred.\n\n\n\n\n\n","title":"ModelContextProtocol.HandlerResult"},{"category":"type","location":"api/#ModelContextProtocol.RequestContext","page":"API Reference","text":"RequestContext(; server::Server, state::ServerState=ServerState(),\n               request_id::Union{RequestId,Nothing}=nothing,\n               progress_token::Union{ProgressToken,Nothing}=nothing)\n\nStore the current request context for MCP protocol handlers.\n\nFields\n\nserver::Server: The MCP server instance handling the request\nstate::ServerState: The persistent server state, carrying the negotiated protocol version for feature gating (see supports)\nrequest_id::Union{RequestId,Nothing}: The ID of the current request (if any)\nprogress_token::Union{ProgressToken,Nothing}: Optional token for progress reporting\n\n\n\n\n\n","title":"ModelContextProtocol.RequestContext"},{"category":"type","location":"api/#ModelContextProtocol.RequestHandler","page":"API Reference","text":"RequestHandler\n\nDefine base type for all request handlers.\n\n\n\n\n\n","title":"ModelContextProtocol.RequestHandler"},{"category":"method","location":"api/#ModelContextProtocol._json_integer_value-Tuple{AbstractString}","page":"API Reference","text":"_json_integer_value(s::AbstractString) -> Union{BigInt,Nothing}\n\nEvaluate a JSON-number token EXACTLY, returning its value when it denotes an integer: \"42\", \"42.0\", \"4.2e1\", and \"-0.0\" all evaluate (to 42, 42, 42, and 0), while non-numbers, non-integral values, and tokens with unreasonably large exponents (a \"1e999999\" mirror must not allocate a gigadigit BigInt) return nothing. Never goes through Float64, so distinct integers beyond 2^53 stay distinct.\n\n\n\n\n\n","title":"ModelContextProtocol._json_integer_value"},{"category":"method","location":"api/#ModelContextProtocol.completion_values-Tuple{Any, String, String, Union{Nothing, Dict{String, String}}}","page":"API Reference","text":"completion_values(sources, arg_name::String, value::String,\n                  context_args::Union{Nothing,Dict{String,String}}) -> Vector{String}\n\nResolve the suggestion list for one completion request from a component's completions sources: a Vector source is served filtered by prefix against the partial value; a Function source is called as f(value) — or f(value, context_args) when applicable, where context_args is the request context's validated arguments map (already-resolved argument values) or nothing. Missing sources (or a component without any) resolve to no suggestions. Source misconfiguration — a non-Vector/non-Function source, a non-string element, or a function returning a lone string or non-strings — throws ArgumentError (surfaced by the handler as an internal error naming the cause) rather than silently coercing.\n\nArguments\n\nsources: The component's completions field (a Dict or nothing)\narg_name::String: The argument (or template variable) being completed\nvalue::String: The partial value typed so far\ncontext_args: The request context's validated arguments, or nothing\n\nReturns\n\nVector{String}: The full (uncapped) suggestion list\n\n\n\n\n\n","title":"ModelContextProtocol.completion_values"},{"category":"method","location":"api/#ModelContextProtocol.convert_to_content_type-Tuple{Any}","page":"API Reference","text":"convert_to_content_type(result::Any) -> Any\n\nApply the documented convenience conversions for tool handler return values: a Dict becomes JSON wrapped in TextContent, a String becomes TextContent, and a Tuple{Vector{UInt8},String} becomes ImageContent.\n\nThese conversions are independent of the tool's declared return_type (the caller validates the converted result against return_type afterwards), so the documented behavior holds for the default return_type = Vector{Content} too.\n\nArguments\n\nresult::Any: The raw value returned by a tool handler\n\nReturns\n\nAny: A Content object for the convenience cases above; otherwise result unchanged\n\n\n\n\n\n","title":"ModelContextProtocol.convert_to_content_type"},{"category":"method","location":"api/#ModelContextProtocol.execute_tool_call-Tuple{MCPTool, AbstractDict, ModelContextProtocol.RequestContext}","page":"API Reference","text":"execute_tool_call(tool::MCPTool, args::AbstractDict, ctx::RequestContext)\n    -> Union{CallToolResult,ErrorInfo}\n\nRun a tool handler and normalize its return value to a CallToolResult (applying the documented convenience conversions and return_type validation), an InputRequired (passed through verbatim for the MRTR layer to convert), or an ErrorInfo when execution throws. Shared by the synchronous tools/call path and background task-augmented executions.\n\n\n\n\n\n","title":"ModelContextProtocol.execute_tool_call"},{"category":"method","location":"api/#ModelContextProtocol.handle_call_tool-Tuple{ModelContextProtocol.RequestContext, ModelContextProtocol.CallToolParams}","page":"API Reference","text":"handle_call_tool(ctx::RequestContext, params::CallToolParams) -> HandlerResult\n\nHandle requests to call a specific tool with the provided parameters.\n\nArguments\n\nctx::RequestContext: The current request context\nparams::CallToolParams: Parameters containing the tool name and arguments\n\nReturns\n\nHandlerResult: Contains either the tool execution results or an error if the tool is not found or execution fails\n\n\n\n\n\n","title":"ModelContextProtocol.handle_call_tool"},{"category":"method","location":"api/#ModelContextProtocol.handle_cancel_task-Tuple{ModelContextProtocol.RequestContext, ModelContextProtocol.CancelTaskParams}","page":"API Reference","text":"handle_cancel_task(ctx::RequestContext, params::CancelTaskParams) -> HandlerResult\n\nHandle a tasks/cancel: transition a non-terminal task to \"cancelled\" (waking any blocked tasks/result requests) and return the task state. Cancelling a task already in a terminal status is rejected with -32602 per spec.\n\n\n\n\n\n","title":"ModelContextProtocol.handle_cancel_task"},{"category":"method","location":"api/#ModelContextProtocol.handle_complete-Tuple{ModelContextProtocol.RequestContext, ModelContextProtocol.CompleteParams}","page":"API Reference","text":"handle_complete(ctx::RequestContext, params::CompleteParams) -> HandlerResult\n\nHandle a completion/complete request: suggest values for a prompt argument (ref/prompt, resolved by prompt name) or a resource-template variable (ref/resource, resolved by URI template). Suggestions come from the matched component's completions sources (see completion_values); the response caps values at the spec's 100, reporting the full count as total and hasMore accordingly. An unknown ref type, prompt name, or template URI is -32602, as is a context.arguments that is not an object of strings (the schema's shape).\n\n\n\n\n\n","title":"ModelContextProtocol.handle_complete"},{"category":"method","location":"api/#ModelContextProtocol.handle_get_task-Tuple{ModelContextProtocol.RequestContext, ModelContextProtocol.GetTaskParams}","page":"API Reference","text":"handle_get_task(ctx::RequestContext, params::GetTaskParams) -> HandlerResult\n\nHandle a tasks/get poll: return the task's current state, flattened into the result per the spec (GetTaskResult = Result & Task).\n\n\n\n\n\n","title":"ModelContextProtocol.handle_get_task"},{"category":"method","location":"api/#ModelContextProtocol.handle_initialize-Tuple{ModelContextProtocol.RequestContext, ModelContextProtocol.InitializeParams}","page":"API Reference","text":"handle_initialize(ctx::RequestContext, params::InitializeParams) -> HandlerResult\n\nHandle MCP protocol initialization requests by setting up the server and returning capabilities.\n\nArguments\n\nctx::RequestContext: The current request context\nparams::InitializeParams: The initialization parameters from the client\n\nReturns\n\nHandlerResult: Contains the server's capabilities and configuration\n\n\n\n\n\n","title":"ModelContextProtocol.handle_initialize"},{"category":"method","location":"api/#ModelContextProtocol.handle_list_prompts-Tuple{ModelContextProtocol.RequestContext, ModelContextProtocol.ListPromptsParams}","page":"API Reference","text":"handle_list_prompts(ctx::RequestContext, params::ListPromptsParams) -> HandlerResult\n\nHandle requests to list available prompts on the MCP server.\n\nArguments\n\nctx::RequestContext: The current request context\nparams::ListPromptsParams: Parameters for the list request (including optional cursor)\n\nReturns\n\nHandlerResult: Contains information about all available prompts\n\n\n\n\n\n","title":"ModelContextProtocol.handle_list_prompts"},{"category":"method","location":"api/#ModelContextProtocol.handle_list_resource_templates-Tuple{ModelContextProtocol.RequestContext, ModelContextProtocol.ListResourceTemplatesParams}","page":"API Reference","text":"handle_list_resource_templates(ctx::RequestContext, params::ListResourceTemplatesParams)\n    -> HandlerResult\n\nHandle a resources/templates/list request: advertise the server's resource templates in the spec wire shape (resourceTemplates entries with uriTemplate, name, and optional description/mimeType/title/icons/_meta).\n\n\n\n\n\n","title":"ModelContextProtocol.handle_list_resource_templates"},{"category":"method","location":"api/#ModelContextProtocol.handle_list_resources-Tuple{ModelContextProtocol.RequestContext, ModelContextProtocol.ListResourcesParams}","page":"API Reference","text":"handle_list_resources(ctx::RequestContext, params::ListResourcesParams) -> HandlerResult\n\nHandle requests to list all available resources on the MCP server.\n\nArguments\n\nctx::RequestContext: The current request context\nparams::ListResourcesParams: Parameters for the list request (including optional cursor)\n\nReturns\n\nHandlerResult: Contains information about all registered resources\n\n\n\n\n\n","title":"ModelContextProtocol.handle_list_resources"},{"category":"method","location":"api/#ModelContextProtocol.handle_list_tasks-Tuple{ModelContextProtocol.RequestContext, ModelContextProtocol.ListTasksParams}","page":"API Reference","text":"handle_list_tasks(ctx::RequestContext, params::ListTasksParams) -> HandlerResult\n\nHandle a paginated tasks/list, restricted to the requestor's authorization context. Not offered (-32601) when the server cannot identify requestors (HTTP without auth).\n\n\n\n\n\n","title":"ModelContextProtocol.handle_list_tasks"},{"category":"method","location":"api/#ModelContextProtocol.handle_list_tools-Tuple{ModelContextProtocol.RequestContext, ModelContextProtocol.ListToolsParams}","page":"API Reference","text":"handle_list_tools(ctx::RequestContext, params::ListToolsParams) -> HandlerResult\n\nHandle requests to list all available tools on the MCP server.\n\nArguments\n\nctx::RequestContext: The current request context\nparams::ListToolsParams: Parameters for the list request (including optional cursor)\n\nReturns\n\nHandlerResult: Contains information about all registered tools\n\n\n\n\n\n","title":"ModelContextProtocol.handle_list_tools"},{"category":"method","location":"api/#ModelContextProtocol.handle_notification-Tuple{ModelContextProtocol.RequestContext, ModelContextProtocol.JSONRPCNotification}","page":"API Reference","text":"handle_notification(ctx::RequestContext, notification::JSONRPCNotification) -> Nothing\n\nProcess notification messages from clients that don't require responses.\n\nArguments\n\nctx::RequestContext: The current request context\nnotification::JSONRPCNotification: The notification to process\n\nReturns\n\nNothing: Notifications don't generate responses\n\n\n\n\n\n","title":"ModelContextProtocol.handle_notification"},{"category":"method","location":"api/#ModelContextProtocol.handle_ping-Tuple{ModelContextProtocol.RequestContext, Nothing}","page":"API Reference","text":"handle_ping(ctx::RequestContext, params::Nothing) -> HandlerResult\n\nHandle MCP protocol ping requests.\n\nArguments\n\nctx::RequestContext: The current request context\nparams::Nothing: The ping parameters does not contain any data\n\nReturns\n\nHandlerResult: Ping returns an empty response payload\n\n\n\n\n\n","title":"ModelContextProtocol.handle_ping"},{"category":"method","location":"api/#ModelContextProtocol.handle_read_resource-Tuple{ModelContextProtocol.RequestContext, ModelContextProtocol.ReadResourceParams}","page":"API Reference","text":"handle_read_resource(ctx::RequestContext, params::ReadResourceParams) -> HandlerResult\n\nHandle requests to read content from a specific resource by URI.\n\nArguments\n\nctx::RequestContext: The current request context\nparams::ReadResourceParams: Parameters containing the URI of the resource to read\n\nReturns\n\nHandlerResult: Contains either the resource contents or an error if the resource  is not found or cannot be read\n\n\n\n\n\n","title":"ModelContextProtocol.handle_read_resource"},{"category":"method","location":"api/#ModelContextProtocol.handle_request-Tuple{Server, ModelContextProtocol.ServerState, ModelContextProtocol.Request}","page":"API Reference","text":"handle_request(server::Server, state::ServerState, request::Request) -> Response\n\nProcess an MCP protocol request and route it to the appropriate handler based on the request method.\n\nArguments\n\nserver::Server: The MCP server instance handling the request\nstate::ServerState: The persistent server state, threaded into the request context (carries the negotiated protocol version)\nrequest::Request: The parsed JSON-RPC request to process\n\nBehavior\n\nThis function creates a request context, then dispatches the request to the appropriate handler based on the request method. Supported methods include:\n\ninitialize: Server initialization\nresources/list: List available resources\nresources/read: Read a specific resource\ntools/list: List available tools\ntools/call: Invoke a specific tool\nprompts/list: List available prompts\nprompts/get: Get a specific prompt\n\nIf an unknown method is received, a METHODNOTFOUND error is returned. Any exceptions thrown during processing are caught and converted to INTERNAL_ERROR responses.\n\nReturns\n\nResponse: Either a successful response or an error response depending on the handler result\n\n\n\n\n\n","title":"ModelContextProtocol.handle_request"},{"category":"method","location":"api/#ModelContextProtocol.handle_set_level-Tuple{ModelContextProtocol.RequestContext, ModelContextProtocol.SetLevelParams}","page":"API Reference","text":"handle_set_level(ctx::RequestContext, params::SetLevelParams) -> HandlerResult\n\nHandle logging/setLevel requests by adjusting the installed MCPLogger's minimum level.\n\nAccepts the MCP/RFC-5424 levels (MCP_LOG_LEVELS) and maps them to Julia LogLevels. Setting \"debug\" also enables the per-request lifecycle log lines emitted by handle_request. When the global logger is not an MCPLogger the request still succeeds (the preference simply has nothing to apply to).\n\nArguments\n\nctx::RequestContext: The current request context\nparams::SetLevelParams: The requested minimum level\n\nReturns\n\nHandlerResult: Empty result on success; INVALID_PARAMS error for unknown levels\n\n\n\n\n\n","title":"ModelContextProtocol.handle_set_level"},{"category":"method","location":"api/#ModelContextProtocol.handle_subscribe_resource-Tuple{ModelContextProtocol.RequestContext, ModelContextProtocol.SubscribeParams}","page":"API Reference","text":"handle_subscribe_resource(ctx::RequestContext, params::SubscribeParams) -> HandlerResult\n\nHandle resources/subscribe requests by recording the URI in the session's wire-subscription set. Returns an empty result per spec. Idempotent: repeat subscriptions to the same URI are accepted.\n\nArguments\n\nctx::RequestContext: The current request context\nparams::SubscribeParams: Parameters containing the resource URI\n\nReturns\n\nHandlerResult: An empty result acknowledging the subscription\n\n\n\n\n\n","title":"ModelContextProtocol.handle_subscribe_resource"},{"category":"method","location":"api/#ModelContextProtocol.handle_task_result-Tuple{ModelContextProtocol.RequestContext, ModelContextProtocol.TaskResultParams}","page":"API Reference","text":"handle_task_result(ctx::RequestContext, params::TaskResultParams) -> HandlerResult\n\nHandle a tasks/result retrieval. For a terminal task, respond immediately with the underlying call's result or error. For a non-terminal task the spec requires blocking until terminal — the response route is detached from the (serial) server loop and a waiter task delivers the response when the task finishes, so the loop stays free to process tasks/get polls and the tasks/cancel that may be what unblocks this very request.\n\n\n\n\n\n","title":"ModelContextProtocol.handle_task_result"},{"category":"method","location":"api/#ModelContextProtocol.handle_unsubscribe_resource-Tuple{ModelContextProtocol.RequestContext, ModelContextProtocol.UnsubscribeParams}","page":"API Reference","text":"handle_unsubscribe_resource(ctx::RequestContext, params::UnsubscribeParams) -> HandlerResult\n\nHandle resources/unsubscribe requests by removing the URI from the session's wire-subscription set. Returns an empty result per spec. Idempotent: unsubscribing a URI that was never subscribed is accepted.\n\nArguments\n\nctx::RequestContext: The current request context\nparams::UnsubscribeParams: Parameters containing the resource URI\n\nReturns\n\nHandlerResult: An empty result acknowledging the unsubscription\n\n\n\n\n\n","title":"ModelContextProtocol.handle_unsubscribe_resource"},{"category":"method","location":"api/#ModelContextProtocol.match_uri_template-Tuple{String, String}","page":"API Reference","text":"match_uri_template(template::String, uri::String) -> Union{Nothing,Dict{String,String}}\n\nMatch uri against an RFC 6570 level-1 URI template. Each {var} placeholder matches one path segment (one or more characters excluding /). Returns the extracted variables on a full match, nothing otherwise.\n\nDeliberate subset of RFC 6570: variable names are [A-Za-z0-9_]+ (no dotted or pct-encoded names); adjacent placeholders with no literal between them ({a}{b}) are ambiguous and never match; a repeated variable name must capture the same value in every position.\n\n\n\n\n\n","title":"ModelContextProtocol.match_uri_template"},{"category":"method","location":"api/#ModelContextProtocol.normalize_read_contents-Tuple{Any, String, String}","page":"API Reference","text":"normalize_read_contents(data, fallback_uri::String, fallback_mime::String)\n    -> Vector{LittleDict{String,Any}}\n\nNormalize a resource provider's return value into spec resources/read contents entries: TextResourceContents/BlobResourceContents (or a vector of them) serialize directly — the only path that can produce binary blob contents; a String becomes the text verbatim; anything else (including custom ResourceContents subtypes) is JSON-encoded into a text entry carrying fallback_uri/fallback_mime.\n\n\n\n\n\n","title":"ModelContextProtocol.normalize_read_contents"},{"category":"method","location":"api/#ModelContextProtocol.notify_task_status-Tuple{Server, ModelContextProtocol.TaskRecord}","page":"API Reference","text":"notify_task_status(server::Server, record::TaskRecord) -> Nothing\n\nSend an optional notifications/tasks/status with the task's full wire state. Best-effort: failures are logged at debug level and never propagate (requestors must not rely on these notifications per spec).\n\n\n\n\n\n","title":"ModelContextProtocol.notify_task_status"},{"category":"method","location":"api/#ModelContextProtocol.param_header_violation-Tuple{Vector{Tuple{Vector{String}, String}}, Any, Dict{String, Any}}","page":"API Reference","text":"param_header_violation(annotated::Vector{Tuple{Vector{String},String}},\n                       arguments, headers::Dict{String,Any})\n    -> Union{String,Nothing}\n\nValidate a modern-era HTTP tools/call's Mcp-Param-* headers against its body (SEP-2243 custom-header mirroring), driven by the tool's MIRROR TABLE — the (property path, suffix) pairs validate_tool_headers derived from the NORMALIZED schema at registration, so enforcement matches advertisement exactly. For every annotated path PRESENT in the request's arguments with a non-null value, the mirrored header must exist, must not be duplicated or unsafe, must decode (a =?base64?...?= sentinel is validated STRICTLY; anything else is a literal), and must match the body value — strings compare exactly, booleans through true/false, integers exactly through the full JSON number grammar, and non-integral numbers numerically.\n\nSkipped per spec: absent paths (nothing to mirror), explicit JSON null values (clients omit the header for null, servers must not expect it), headers whose path is not in the body, and Mcp-Param-* headers matching no annotation (forward compatibility). Returns the violation description, or nothing.\n\n\n\n\n\n","title":"ModelContextProtocol.param_header_violation"},{"category":"method","location":"api/#ModelContextProtocol.request_protocol_version-Tuple{ModelContextProtocol.RequestContext}","page":"API Reference","text":"request_protocol_version(ctx::RequestContext) -> Union{String,Nothing}\n\nThe protocol version in effect for THIS request: the per-request version carried in a modern-era request's _meta when present, otherwise the session version negotiated by initialize (legacy era). nothing before any negotiation.\n\n\n\n\n\n","title":"ModelContextProtocol.request_protocol_version"},{"category":"method","location":"api/#ModelContextProtocol.serialize_resource_contents-Tuple{ResourceContents}","page":"API Reference","text":"serialize_resource_contents(resource::ResourceContents) -> LittleDict{String,Any}\n\nSerialize resource contents to the spec wire shape: {uri, text, mimeType} for TextResourceContents, {uri, blob, mimeType} (base64) for BlobResourceContents.\n\nArguments\n\nresource::ResourceContents: The resource contents to serialize\n\nReturns\n\nLittleDict{String,Any}: The serialized resource contents entry\n\n\n\n\n\n","title":"ModelContextProtocol.serialize_resource_contents"},{"category":"method","location":"api/#ModelContextProtocol.spawn_task_execution!-Tuple{ModelContextProtocol.RequestContext, MCPTool, AbstractDict, ModelContextProtocol.TaskRecord}","page":"API Reference","text":"spawn_task_execution!(ctx::RequestContext, tool::MCPTool, args::AbstractDict,\n                      record::TaskRecord) -> Nothing\n\nRun a tool call in a background Julia task, recording the outcome into record and emitting a notifications/tasks/status on the terminal transition. The execution context carries the original request's progress token (valid for the task lifetime per spec) and the task record (for task_cancelled(ctx)). If the task was cancelled while running, the outcome is discarded.\n\n\n\n\n\n","title":"ModelContextProtocol.spawn_task_execution!"},{"category":"method","location":"api/#ModelContextProtocol.task_principal-Tuple{ModelContextProtocol.RequestContext}","page":"API Reference","text":"task_principal(ctx::RequestContext) -> Union{String,Nothing}\n\nThe authorization principal tasks are bound to: the authenticated subject when HTTP auth is enabled, otherwise nothing (single-user transports like stdio).\n\n\n\n\n\n","title":"ModelContextProtocol.task_principal"},{"category":"method","location":"api/#ModelContextProtocol.task_terminal_response-Tuple{Any, ModelContextProtocol.TaskRecord}","page":"API Reference","text":"task_terminal_response(request_id, record::TaskRecord) -> Response\n\nBuild the tasks/result response for a terminal task: exactly what the underlying request would have returned — its CallToolResult (with the related-task _meta added) or its JSON-RPC error. A task cancelled before completion has no underlying result, so it answers with an error. Caller must hold the store lock.\n\n\n\n\n\n","title":"ModelContextProtocol.task_terminal_response"},{"category":"method","location":"api/#ModelContextProtocol.tasks_cancel_offered-Tuple{Server}","page":"API Reference","text":"tasks_cancel_offered(server::Server) -> Bool\n\nWhether tasks/cancel is offered (a TaskCapability with cancel=true), matching what the capability advertises.\n\n\n\n\n\n","title":"ModelContextProtocol.tasks_cancel_offered"},{"category":"method","location":"api/#ModelContextProtocol.tasks_list_offered-Tuple{Server}","page":"API Reference","text":"tasks_list_offered(server::Server) -> Bool\n\nWhether tasks/list is offered: requires a TaskCapability with list=true, and is withheld on an HTTP transport without authentication (the server cannot identify requestors there, so listing would expose task metadata across clients).\n\n\n\n\n\n","title":"ModelContextProtocol.tasks_list_offered"},{"category":"method","location":"api/#ModelContextProtocol.tasks_supported-Tuple{ModelContextProtocol.RequestContext}","page":"API Reference","text":"tasks_supported(ctx::RequestContext) -> Bool\n\nWhether the tasks capability is in effect for THIS session: the server is configured with a TaskCapability AND the client negotiated a protocol version with task support (2025-11-25+). When false, task metadata on requests is ignored (per spec) and the tasks/* methods do not exist.\n\n\n\n\n\n","title":"ModelContextProtocol.tasks_supported"},{"category":"method","location":"api/#ModelContextProtocol.validate_tool_headers-Tuple{MCPTool}","page":"API Reference","text":"validate_tool_headers(tool::MCPTool) -> Vector{Tuple{Vector{String},String}}\n\nReject invalid SEP-2243 x-mcp-header annotations at registration and return the tool's MIRROR TABLE — the (property path, header suffix) pairs runtime enforcement uses. Rules: every annotation value must be a string, every suffix a nonempty HTTP token (it becomes part of the Mcp-Param-* header NAME), suffixes case-insensitively unique within the tool (header names are case-insensitive, so \"Route\" and \"route\" would collapse onto one mirrored header), the annotated property must declare an annotatable type (string, integer, or boolean — the spec excludes number, objects, and arrays), and annotations are only valid on statically reachable properties (never under array items, composition branches, or definitions). Advertising an invalid annotation would force conforming clients to discard the tool — better to refuse it server-side with a clear error. Throws ArgumentError on violation.\n\nThe table is derived from the NORMALIZED schema — the same JSON tree tools/list advertises — so validation, advertisement, and enforcement can never diverge (a NamedTuple-shaped raw schema serializes to ordinary JSON objects and is honored exactly as advertised).\n\n\n\n\n\n","title":"ModelContextProtocol.validate_tool_headers"},{"category":"method","location":"api/#ModelContextProtocol.with_related_task_meta-Tuple{CallToolResult, String}","page":"API Reference","text":"with_related_task_meta(result::CallToolResult, task_id::String) -> CallToolResult\n\nReturn a copy of result whose _meta carries the spec-required io.modelcontextprotocol/related-task association for tasks/result responses.\n\n\n\n\n\n","title":"ModelContextProtocol.with_related_task_meta"},{"category":"constant","location":"api/#ModelContextProtocol.LATEST_MODERN_PROTOCOL_VERSION","page":"API Reference","text":"Latest modern-era protocol version.\n\n\n\n\n\n","title":"ModelContextProtocol.LATEST_MODERN_PROTOCOL_VERSION"},{"category":"constant","location":"api/#ModelContextProtocol.MODERN_PROTOCOL_VERSIONS","page":"API Reference","text":"Modern-era (stateless, per-request _meta) MCP protocol versions supported by this implementation, newest first. A request carrying io.modelcontextprotocol/protocolVersion in _meta is served statelessly under one of these versions; initialize selects legacy semantics from SUPPORTED_PROTOCOL_VERSIONS. Both eras are served concurrently (dual-era server, per the 2026-07-28 spec's backward-compatibility model).\n\n\n\n\n\n","title":"ModelContextProtocol.MODERN_PROTOCOL_VERSIONS"},{"category":"constant","location":"api/#ModelContextProtocol.MODERN_CACHEABLE_METHODS","page":"API Reference","text":"Modern methods whose complete results are CacheableResults: the spec REQUIRES ttlMs and cacheScope on them.\n\n\n\n\n\n","title":"ModelContextProtocol.MODERN_CACHEABLE_METHODS"},{"category":"constant","location":"api/#ModelContextProtocol.MODERN_METHODS","page":"API Reference","text":"Methods served in the modern era, each gated on the capability that provides it (nothing = always available). Methods REMOVED from the modern era — ping, logging/setLevel, resources/subscribe/unsubscribe (replaced by subscriptions/listen), and the legacy core tasks methods tasks/result and tasks/list — return METHODNOTFOUND on modern requests even though the legacy era serves them. tasks/get, tasks/update, and tasks/cancel belong to the io.modelcontextprotocol/tasks EXTENSION (SEP-2663): always dispatchable, but gated in-handler on the request's declared extension capability (-32021 for non-declaring clients, per the extension's error rules — not -32601). A method whose capability the server does not declare is equally METHODNOTFOUND: the advertised capability set and the callable surface must agree.\n\n\n\n\n\n","title":"ModelContextProtocol.MODERN_METHODS"},{"category":"method","location":"api/#ModelContextProtocol.dispatch_modern-Tuple{ModelContextProtocol.RequestContext, ModelContextProtocol.Request}","page":"API Reference","text":"dispatch_modern(ctx::RequestContext, request::Request) -> HandlerResult\n\nRoute a validated modern-era request to its handler. Shared feature handlers (tools, resources, prompts) are reused from the legacy era; server/discover is modern-only. Requests whose typed params failed to parse get -32602 rather than an internal error.\n\nArguments\n\nctx::RequestContext: The request context (carries the per-request protocol version)\nrequest::Request: The parsed request\n\nReturns\n\nHandlerResult: The handler's result\n\n\n\n\n\n","title":"ModelContextProtocol.dispatch_modern"},{"category":"method","location":"api/#ModelContextProtocol.handle_discover-Tuple{ModelContextProtocol.RequestContext}","page":"API Reference","text":"handle_discover(ctx::RequestContext) -> HandlerResult\n\nHandle server/discover (a server MUST in the modern era): advertise the protocol versions this server supports across both eras, its modern capability surface, and optional instructions, with the CacheableResult fields (ttlMs, cacheScope) the spec requires on discovery results. resultType and serverInfo are attached by the modern envelope.\n\nCapabilities carry only what the modern era actually serves: the features present, their listChanged/subscribe flags (delivered via subscriptions/listen), logging when declared (per-request logLevel opt-in — a MUST for servers that emit notifications/message), and no legacy-only tasks — and never the legacy builder's embedded resource listings, which would leak resource metadata into shared caches and do not match the modern ServerCapabilities shape.\n\nArguments\n\nctx::RequestContext: The current request context\n\nReturns\n\nHandlerResult: The discovery result\n\n\n\n\n\n","title":"ModelContextProtocol.handle_discover"},{"category":"method","location":"api/#ModelContextProtocol.handle_modern_request-Tuple{Server, ModelContextProtocol.ServerState, ModelContextProtocol.Request}","page":"API Reference","text":"handle_modern_request(server::Server, state::ServerState, request::Request;\n                      authenticated_user=nothing) -> Union{Response,Nothing}\n\nServe a modern-era (2026-07-28+) request statelessly: validate the per-request _meta protocol fields, dispatch to the shared feature handlers, and apply the modern result envelope plus the CacheableResult fields where required. The handler context gets a FRESH ServerState — modern requests are self-contained, and a ctx-aware tool handler must not be able to mutate the legacy session's state.\n\nValidation, in order (per the spec):\n\nmissing protocolVersion (only reachable for server/discover, which routes modern unconditionally) → -32602 Invalid params\nunsupported protocolVersion → UnsupportedProtocolVersionError (-32022) with data.supported/data.requested\nmissing clientCapabilities → -32602 Invalid params (a required _meta field)\npresent but unrecognized logLevel → -32602 Invalid params (the spec's SHOULD; presence-aware — a mistyped value is rejected, never treated as absent)\nmethod not on the modern surface (or its capability undeclared) → -32601\n\nnotifications/message follows the per-request opt-in: a request that set a valid io.modelcontextprotocol/logLevel (and passed all validation) gets records at or above that level on its own response stream, provided the server declares the logging capability; every other modern request gets none — the suppression flag the loop armed stays set through the iteration, covering the loop's own post-dispatch lifecycle records too. subscriptions/listen never honors the opt-in: its response stream IS the subscription stream, and the spec forbids notifications/message there.\n\nArguments\n\nserver::Server: The MCP server instance\nstate::ServerState: The legacy session state (NOT given to handlers; see above)\nrequest::Request: The parsed request (with meta.protocol_version set)\nauthenticated_user: Per-request identity from HTTP auth, else nothing\n\nReturns\n\nUnion{Response,Nothing}: The response to send\n\n\n\n\n\n","title":"ModelContextProtocol.handle_modern_request"},{"category":"method","location":"api/#ModelContextProtocol.modern_cache_scope-Tuple{Server}","page":"API Reference","text":"modern_cache_scope(server::Server) -> String\n\nThe cacheScope for this server's cacheable results: \"private\" when bearer auth is enabled (responses are served inside an authorization context and MUST NOT be shared across contexts by intermediaries), \"public\" otherwise.\n\nArguments\n\nserver::Server: The server instance\n\nReturns\n\nString: \"public\" or \"private\"\n\n\n\n\n\n","title":"ModelContextProtocol.modern_cache_scope"},{"category":"method","location":"api/#ModelContextProtocol.modern_input_required_response-Tuple{Server, InputRequired, Any, String, Any, Any, String}","page":"API Reference","text":"modern_input_required_response(server::Server, ir::InputRequired, id,\n                               method::String, client_capabilities,\n                               params_digest, principal::String) -> Response\n\nBuild the modern-era response for a handler's InputRequired (MRTR): the -32021 MissingRequiredClientCapability error when the request did not declare a capability the input requests need, otherwise the enveloped InputRequiredResult (resultType input_required + inputRequests + a minted requestState). Shared by the in-loop serve path and off-loop detachable tool calls (tasks extension), so both build byte-identical responses.\n\nArguments\n\nserver::Server: The MCP server instance\nir::InputRequired: The handler's input-required value\nid: The request id to respond with\nmethod::String: The request method (bound into the requestState)\nclient_capabilities: The request's declared _meta client capabilities\nparams_digest: The request's canonical params digest\nprincipal::String: The MRTR principal (see mrtr_principal)\n\nReturns\n\nResponse: The error or enveloped result response\n\n\n\n\n\n","title":"ModelContextProtocol.modern_input_required_response"},{"category":"method","location":"api/#ModelContextProtocol.modern_invalid_params-Tuple{ModelContextProtocol.RequestContext, String}","page":"API Reference","text":"modern_invalid_params(ctx::RequestContext, method::String) -> HandlerResult\n\nBuild the -32602 Invalid params result for a modern request whose typed parameters are missing or malformed.\n\n\n\n\n\n","title":"ModelContextProtocol.modern_invalid_params"},{"category":"method","location":"api/#ModelContextProtocol.modern_method_available-Tuple{Server, String}","page":"API Reference","text":"modern_method_available(server::Server, method::String) -> Bool\n\nWhether method exists on this server's modern surface: it must be a modern-era method AND its providing capability must be declared — the advertised capability set and the callable surface must agree.\n\nArguments\n\nserver::Server: The server instance\nmethod::String: The request method\n\nReturns\n\nBool: true when the method is callable in the modern era\n\n\n\n\n\n","title":"ModelContextProtocol.modern_method_available"},{"category":"method","location":"api/#ModelContextProtocol.modern_result_envelope-Tuple{ModelContextProtocol.JSONRPCResponse, Server}","page":"API Reference","text":"modern_result_envelope(response::JSONRPCResponse, server::Server) -> JSONRPCResponse\n\nApply the modern-era result envelope: every result MUST carry resultType (\"complete\" unless the handler set one) and SHOULD identify the server via io.modelcontextprotocol/serverInfo in the result's _meta (merged into any handler-provided _meta).\n\nThe known handler result types are re-shaped WITHOUT a JSON round-trip, so values travel by reference — no numeric precision loss (Int64 above 2^53 survives) and no extra copies of base64 payloads. Only unknown result types fall back to an untyped JSON3 round-trip (which preserves Int64, unlike a Dict{String,Any}-typed read).\n\nArguments\n\nresponse::JSONRPCResponse: The handler's response\nserver::Server: The server (for serverInfo)\n\nReturns\n\nJSONRPCResponse: A response whose result carries the modern envelope fields\n\n\n\n\n\n","title":"ModelContextProtocol.modern_result_envelope"},{"category":"method","location":"api/#ModelContextProtocol.modernize_error_code-Tuple{Int64}","page":"API Reference","text":"modernize_error_code(code::Int) -> Int\n\nMap legacy-range error codes to their modern-era replacements. The 2026-07-28 allocation policy grandfathers -32000..-32019 for legacy responses but modern responses must use spec codes: not-found conditions for resources (-32000), tools (-32001), URIs (-32002, reserved — MUST NOT be emitted), and prompts (-32003) are all -32602 Invalid params in the modern era.\n\nArguments\n\ncode::Int: A legacy-era error code\n\nReturns\n\nInt: The code to emit on a modern-era response\n\n\n\n\n\n","title":"ModelContextProtocol.modernize_error_code"},{"category":"method","location":"api/#ModelContextProtocol.serve_modern-Tuple{Server, ModelContextProtocol.Request, String, Any, Union{Nothing, AuthenticatedUser}}","page":"API Reference","text":"serve_modern(server::Server, request::Request, version::String, mrtr_state,\n             authenticated_user) -> Union{Response,Nothing}\n\nDispatch a fully validated modern-era request and build its response (envelope, CacheableResult fields, MRTR inputrequired handling). Split out of `handlemodern_request` so the per-request log opt-in can re-scope the logger around the whole serve path.\n\nArguments\n\nserver::Server: The MCP server instance\nrequest::Request: The validated request\nversion::String: The request's protocol version\nmrtr_state: The verified MRTR state payload, or nothing\nauthenticated_user: Per-request identity from HTTP auth, else nothing\n\nReturns\n\nUnion{Response,Nothing}: The response to send (nothing when deferred)\n\n\n\n\n\n","title":"ModelContextProtocol.serve_modern"},{"category":"type","location":"api/#ModelContextProtocol.InputRequest","page":"API Reference","text":"InputRequest(method::String, params)\n\nOne entry of an InputRequiredResult's inputRequests map: a request the client should fulfill and answer in its retry's inputResponses. Only the three spec-blessed request types are valid — construct via elicit_request, sampling_request, or roots_request.\n\nFields\n\nmethod::String: The request method (elicitation/create, sampling/createMessage, roots/list)\nparams::Any: The request's params (a Dict, or nothing for none)\n\n\n\n\n\n","title":"ModelContextProtocol.InputRequest"},{"category":"method","location":"api/#ModelContextProtocol.canonical_json_digest-Tuple{Any}","page":"API Reference","text":"canonical_json_digest(x) -> String\n\nSHA-256 hex digest of the canonical (sorted-key) JSON serialization of x. Used to bind an issued requestState to the exact original params it may resume.\n\nArguments\n\nx: Any parsed JSON value\n\nReturns\n\nString: The hex digest\n\n\n\n\n\n","title":"ModelContextProtocol.canonical_json_digest"},{"category":"method","location":"api/#ModelContextProtocol.canonicalize_json-Tuple{Any}","page":"API Reference","text":"canonicalize_json(x) -> Any\n\nRebuild a JSON value with all object keys sorted (recursively), producing a serialization-stable structure for digesting. Non-container values pass through.\n\nArguments\n\nx: Any parsed JSON value\n\nReturns\n\nThe canonical structure\n\n\n\n\n\n","title":"ModelContextProtocol.canonicalize_json"},{"category":"method","location":"api/#ModelContextProtocol.capability_satisfied-Tuple{Any, ModelContextProtocol.InputRequest}","page":"API Reference","text":"capability_satisfied(client_capabilities, r::InputRequest) -> Bool\n\nWhether this request's declared _meta client capabilities cover input request r. Declaration means the capability VALUE is an object (a null or scalar value declares nothing — including MODE values: {\"elicitation\": {\"form\": null}} declares no form support), and subfeatures are honored:\n\nform-mode elicitation: form declared with an object value covers it, and ONLY the bare empty elicitation object implies it — any other declared mode set (url, or modes from a future revision) excludes form\ntool-enabled sampling (params.tools OR params.toolChoice present): requires sampling.tools declared as an object, not just sampling\n\nArguments\n\nclient_capabilities: The request's declared client capabilities (any JSON value)\nr::InputRequest: The input request to check\n\nReturns\n\nBool: true when r may be sent to this client\n\n\n\n\n\n","title":"ModelContextProtocol.capability_satisfied"},{"category":"method","location":"api/#ModelContextProtocol.input_required_envelope-Tuple{Server, InputRequired, String, Any, String}","page":"API Reference","text":"input_required_envelope(server::Server, ir::InputRequired, method::String,\n                        params_digest, principal::String) -> LittleDict{String,Any}\n\nBuild the InputRequiredResult wire shape for a handler's InputRequired value: resultType: \"input_required\", the inputRequests map (each entry {method, params?}), and a freshly minted requestState bound to this method, principal, and params digest. Capability checking is the caller's job (undeclared_capability_requirements) — it must reject with -32021 BEFORE building this envelope.\n\nArguments\n\nserver::Server: The server\nir::InputRequired: The handler's value\nmethod::String: The request method (bound into the state token)\nparams_digest: The request's canonical params digest\nprincipal::String: The requesting principal\n\nReturns\n\nLittleDict{String,Any}: The result object\n\n\n\n\n\n","title":"ModelContextProtocol.input_required_envelope"},{"category":"method","location":"api/#ModelContextProtocol.issue_request_state-Tuple{Server, String, Any, String, Any}","page":"API Reference","text":"issue_request_state(server::Server, method::String, params_digest,\n                    principal::String, handler_state) -> String\n\nMint an integrity-protected requestState token: an HMAC-SHA256-signed payload embedding issue time + TTL, the principal, the ORIGINATING METHOD, the canonical digest of the original request params, and the handler's carried state. Signed with the per-server key, so a token survives neither tampering nor (with the default ephemeral key) a server restart. Binding the method prevents cross-method state confusion: tools/call and prompts/get params can be structurally identical, and a token issued for one must not resume the other.\n\nArguments\n\nserver::Server: The server (holds the signing key)\nmethod::String: The request method the state was issued for\nparams_digest: The request's canonical params digest (from RequestMeta)\nprincipal::String: The requesting principal (mrtr_principal)\nhandler_state: The handler's JSON-serializable state (may be nothing)\n\nReturns\n\nString: The token (base64(payload).base64(mac))\n\n\n\n\n\n","title":"ModelContextProtocol.issue_request_state"},{"category":"method","location":"api/#ModelContextProtocol.mrtr_principal-Tuple{Union{Nothing, AuthenticatedUser}}","page":"API Reference","text":"mrtr_principal(user::Union{AuthenticatedUser,Nothing}) -> String\n\nThe principal string bound into a requestState: the canonical provider-qualified identity (see principal_identity), or \"\" when the request is unauthenticated. A state issued to one principal must not be redeemable by another — including a DIFFERENT provider's token that happens to carry the same subject.\n\nArguments\n\nuser::Union{AuthenticatedUser,Nothing}: The request's authenticated user\n\nReturns\n\nString: The principal\n\n\n\n\n\n","title":"ModelContextProtocol.mrtr_principal"},{"category":"method","location":"api/#ModelContextProtocol.principal_identity-Tuple{AuthenticatedUser}","page":"API Reference","text":"principal_identity(user::AuthenticatedUser) -> String\n\nThe canonical, collision-free identity string for an authenticated principal: the JSON array [provider, subject]. Two identity providers can issue the same sub, so the subject alone must never identify a principal; JSON encoding (not delimiter concatenation) makes the pair unambiguous — (\"a\", \"b\\x1fc\") and (\"a\\x1fb\", \"c\") cannot collide. username is display-oriented and mutable, so it never participates.\n\n\n\n\n\n","title":"ModelContextProtocol.principal_identity"},{"category":"method","location":"api/#ModelContextProtocol.undeclared_capability_requirements-Tuple{InputRequired, Any}","page":"API Reference","text":"undeclared_capability_requirements(ir::InputRequired, client_capabilities)\n    -> LittleDict{String,Any}\n\nThe requiredCapabilities object for the capabilities ir's input requests need but this request did not declare — a ClientCapabilities-shaped object precise to the subfeature: {\"elicitation\": {\"form\": {}}} for a form elicitation, {\"sampling\": {}} for plain sampling, {\"sampling\": {\"tools\": {}}} for tool-enabled sampling. A server MUST NOT send inputRequests the client did not declare support for: a non-empty return means the request must be rejected with -32021 carrying this object as error.data.requiredCapabilities.\n\nArguments\n\nir::InputRequired: The handler's input-required value\nclient_capabilities: The request's declared client capabilities (any JSON value)\n\nReturns\n\nLittleDict{String,Any}: The missing requirements (empty when all declared)\n\n\n\n\n\n","title":"ModelContextProtocol.undeclared_capability_requirements"},{"category":"method","location":"api/#ModelContextProtocol.verify_request_state-Tuple{Server, AbstractString, String, Any, String}","page":"API Reference","text":"verify_request_state(server::Server, token::AbstractString, method::String,\n                     params_digest, principal::String) -> Tuple{Bool,Any}\n\nVerify a retry's requestState BEFORE any handler runs: signature (constant-time), TTL, principal, originating method, and original-params digest must all hold — the token is attacker-controlled input. Returns (true, handler_state) on success or (false, reason) on any failure.\n\nArguments\n\nserver::Server: The server (holds the signing key)\ntoken::AbstractString: The offered requestState\nmethod::String: THIS request's method (must equal the issuing method)\nparams_digest: THIS request's canonical params digest (must equal the original's)\nprincipal::String: THIS request's principal\n\nReturns\n\nTuple{Bool,Any}: (ok, handler_state_or_reason)\n\n\n\n\n\n","title":"ModelContextProtocol.verify_request_state"},{"category":"method","location":"api/#ModelContextProtocol.call_tool_result_wire-Tuple{CallToolResult}","page":"API Reference","text":"call_tool_result_wire(result::CallToolResult) -> LittleDict{String,Any}\n\nSerialize a CallToolResult to the plain result object inlined on a completed task (tasks/get result field): content/isError plus structuredContent and _meta when present. No resultType (it is a nested result, not a response envelope) and no legacy related-task _meta (the taskId is already at the response root, and SEP-2663 forbids the redundant key).\n\n\n\n\n\n","title":"ModelContextProtocol.call_tool_result_wire"},{"category":"method","location":"api/#ModelContextProtocol.ensure_task_notifications!-Tuple{Server}","page":"API Reference","text":"ensure_task_notifications!(server::Server) -> Nothing\n\n(Re)install the notifications/tasks dispatch pipeline when it is absent or its queue has been closed — stop! and the server-loop shutdown both close the queue (ending the dispatcher so it cannot pin the server past its lifetime), so a server started again needs a fresh queue and dispatcher. A live pipeline is left untouched.\n\n\n\n\n\n","title":"ModelContextProtocol.ensure_task_notifications!"},{"category":"method","location":"api/#ModelContextProtocol.ext_task_authorized-Tuple{ModelContextProtocol.RequestContext, ModelContextProtocol.TaskRecord}","page":"API Reference","text":"ext_task_authorized(ctx::RequestContext, record::TaskRecord) -> Bool\n\nRe-authorize a task-related request against the task's recorded requirements (SEP-2663: authorization checks on EVERY task request, not only at creation): when the request is authenticated, the current token must carry every scope the originating tool required. A failure is reported as task-not-found by the caller, indistinguishable from an unknown id.\n\n\n\n\n\n","title":"ModelContextProtocol.ext_task_authorized"},{"category":"method","location":"api/#ModelContextProtocol.ext_task_principal-Tuple{ModelContextProtocol.RequestContext}","page":"API Reference","text":"ext_task_principal(ctx::RequestContext) -> Union{String,Nothing}\n\nThe authorization principal extension tasks are bound to. Unlike the legacy task_principal (subject only), this binds provider AND subject via the canonical collision-free principal_identity — two identity providers can issue the same sub, and a token from the wrong provider must not resolve another caller's task. nothing when auth is not enabled (single-user transports). Shares its encoding with the MRTR requestState principal, so the composition flow (MRTR round, then task creation) is bound to one identity end-to-end.\n\n\n\n\n\n","title":"ModelContextProtocol.ext_task_principal"},{"category":"method","location":"api/#ModelContextProtocol.ext_task_wire-Tuple{ModelContextProtocol.TaskRecord}","page":"API Reference","text":"ext_task_wire(record::TaskRecord; detailed::Bool=false) -> LittleDict{String,Any}\n\nSerialize a task record to the SEP-2663 wire shape: taskId, status, optional statusMessage, createdAt/lastUpdatedAt (ISO 8601 UTC), ttlMs (always present; null = unlimited), and pollIntervalMs — integer milliseconds, and never the legacy ttl/pollInterval keys. With detailed=true the status-specific DetailedTask payload is inlined: result for \"completed\", error for \"failed\", inputRequests for \"input_required\". Caller must hold the store lock.\n\n\n\n\n\n","title":"ModelContextProtocol.ext_task_wire"},{"category":"method","location":"api/#ModelContextProtocol.handle_ext_cancel_task-Tuple{ModelContextProtocol.RequestContext, ModelContextProtocol.CancelTaskParams}","page":"API Reference","text":"handle_ext_cancel_task(ctx::RequestContext, params::CancelTaskParams) -> HandlerResult\n\nHandle a modern-era tasks/cancel (tasks extension): signal cancellation intent and acknowledge with an empty result. Cancellation is cooperative and eventually consistent — a running handler observes it via task_cancelled(ctx), and the ack is idempotent: cancelling an already-terminal task acks identically (-32602 is reserved for unknown taskIds, a deliberate delta from the legacy era's terminal-cancel rejection). Gated on the extension declaration (-32021).\n\n\n\n\n\n","title":"ModelContextProtocol.handle_ext_cancel_task"},{"category":"method","location":"api/#ModelContextProtocol.handle_ext_get_task-Tuple{ModelContextProtocol.RequestContext, ModelContextProtocol.GetTaskParams}","page":"API Reference","text":"handle_ext_get_task(ctx::RequestContext, params::GetTaskParams) -> HandlerResult\n\nHandle a modern-era tasks/get poll (tasks extension): return the DetailedTask for the task's current status, with the terminal result/error inlined. Gated on the extension declaration (-32021); an unknown, expired, cross-principal, or legacy-era taskId is -32602.\n\n\n\n\n\n","title":"ModelContextProtocol.handle_ext_get_task"},{"category":"method","location":"api/#ModelContextProtocol.handle_ext_update_task-Tuple{ModelContextProtocol.RequestContext, ModelContextProtocol.UpdateTaskParams}","page":"API Reference","text":"handle_ext_update_task(ctx::RequestContext, params::UpdateTaskParams) -> HandlerResult\n\nHandle a modern-era tasks/update (tasks extension): deliver the client's inputResponses to the task's outstanding input requests, acknowledging with an empty result. Responses keyed to requests that are not currently outstanding — never issued, already answered, or superseded — are ignored per spec, and a partial set (a strict subset of the outstanding keys) is accepted: answered keys are removed and the task stays input_required until the rest arrive; once nothing is pending the status returns to working. The ack is eventually consistent — delivery happens before the ack here, but the resumed handler runs concurrently, so the observable status is whatever tasks/get sees next. Gated on the extension declaration (-32021); an unknown taskId is -32602.\n\n\n\n\n\n","title":"ModelContextProtocol.handle_ext_update_task"},{"category":"method","location":"api/#ModelContextProtocol.install_task_notifications!-Tuple{Server}","page":"API Reference","text":"install_task_notifications!(server::Server) -> Nothing\n\nWire the tasks extension's notifications/tasks status updates: install the store's status-change hook so every extension-era transition (parking on input, resuming, completing, failing, cancelling, expiring) pushes the task's complete DetailedTask — identical to what tasks/get would return at that moment — to the subscriptions/listen streams that subscribed to the task's id.\n\nThe hook runs under the store lock (which the wire snapshot requires) but does NO transport I/O there: it enqueues the snapshot — plus the task's transition-time principal and required_scopes, re-checked per stream at delivery — onto a bounded FIFO consumed by a single dispatcher task, which broadcasts OUTSIDE the store lock. A stalled client can therefore never wedge the task store (a synchronous stdio write under the lock would block every tasks/* request once the client's pipe fills), the single consumer preserves transition order, and a full queue load-sheds (best-effort pushes; polling stays authoritative). Installed by mcp_server; stop! closes the queue, ending the dispatcher.\n\n\n\n\n\n","title":"ModelContextProtocol.install_task_notifications!"},{"category":"method","location":"api/#ModelContextProtocol.safe_error_message-Tuple{String, Any}","page":"API Reference","text":"safe_error_message(prefix::String, e) -> String\n\nFormat an exception for an error message WITHOUT trusting its show/showerror methods: an exception whose display itself throws must not escape the error path it is being reported on (it would defeat every catch that interpolates $(e) and, on the task-worker path, leave a delivered task non-terminal forever). Falls back to the exception's type name, then to a static string.\n\n\n\n\n\n","title":"ModelContextProtocol.safe_error_message"},{"category":"method","location":"api/#ModelContextProtocol.spawn_detachable_tool_call!-Tuple{ModelContextProtocol.RequestContext, MCPTool, AbstractDict, Vector{String}}","page":"API Reference","text":"spawn_detachable_tool_call!(ctx::RequestContext, tool::MCPTool,\n                            args::AbstractDict,\n                            tool_scopes::Vector{String}) -> HandlerResult\n\nRun a task-capable modern-era tools/call off the serial server loop: capture the request's response route, spawn the handler on a background Julia task, and defer. tool_scopes is the caller's snapshot of the tool's required_scopes — the exact set this request was authorized against, recorded onto a minted task for per-request re-authorization. The handler decides the response shape by what it does first:\n\nreturns without detaching → ordinary synchronous result (the immediate-result shortcut), delivered on the captured route\nreturns InputRequired without detaching → the MRTR round (capability check, input_required envelope) — this is what lets MRTR exchanges resolve BEFORE task creation, per the SEP-2663 composition rule\ncalls task_detach(ctx) → the CreateTaskResult was already delivered; the eventual outcome is recorded into the task (a CallToolResult completes it, a thrown error fails it with the JSON-RPC error inlined)\n\nThe worker guarantees exactly one outcome on every path: any throw between the handler's return and the response delivery — including one raised by a diagnostic logger — lands in a last-resort catch that answers -32603 (or fails the already-created task).\n\n\n\n\n\n","title":"ModelContextProtocol.spawn_detachable_tool_call!"},{"category":"method","location":"api/#ModelContextProtocol.tasks_extension_declared-Tuple{Any}","page":"API Reference","text":"tasks_extension_declared(client_capabilities) -> Bool\n\nWhether this request declared the io.modelcontextprotocol/tasks extension in its _meta clientCapabilities. Declaration means the extension key is present under extensions with an OBJECT value (the spec's \"an empty object indicates support\"; a null or scalar value declares nothing, matching the MRTR capability rules).\n\n\n\n\n\n","title":"ModelContextProtocol.tasks_extension_declared"},{"category":"method","location":"api/#ModelContextProtocol.tasks_extension_required_error-Tuple{}","page":"API Reference","text":"tasks_extension_required_error() -> ErrorInfo\n\nThe -32021 MissingRequiredClientCapability error for task-surface requests from a client that did not declare the tasks extension, carrying the spec's requiredCapabilities object naming the extension.\n\n\n\n\n\n","title":"ModelContextProtocol.tasks_extension_required_error"},{"category":"method","location":"api/#ModelContextProtocol.broadcast_subscription_notification-Tuple{Server, String}","page":"API Reference","text":"broadcast_subscription_notification(server::Server, method::String;\n                                    params::AbstractDict=LittleDict{String,Any}(),\n                                    uri::Union{String,Nothing}=nothing,\n                                    task_id::Union{String,Nothing}=nothing) -> Int\n\nDeliver a notification to every subscriptions/listen stream that opted into it, tagged with each stream's own subscription id. Every record — not just the filter matches — is swept for liveness (route_alive), so a disconnected client whose filter never matches anything still gets pruned. Returns the number of streams the notification reached.\n\nDelivery happens while holding the registry lock (every delivery path enqueues without blocking), so a broadcast cannot interleave with a stream's registration or graceful closure; see the SubscriptionRegistry docstring for the lock ordering.\n\nArguments\n\nserver::Server: The server whose subscriptions to notify\nmethod::String: The notification method\nparams::AbstractDict: Additional notification params\nuri::Union{String,Nothing}: For notifications/resources/updated, the resource URI\ntask_id::Union{String,Nothing}: For notifications/tasks, the task id\nauthorize: Optional per-stream predicate record::SubscriptionRecord -> Bool checked after the filter match — used by notifications/tasks to re-authorize each delivery against the stream's stored principal/scopes WITHOUT taking the task-store lock here (the ordering store → registry must never reverse)\n\nReturns\n\nInt: How many streams received the notification\n\n\n\n\n\n","title":"ModelContextProtocol.broadcast_subscription_notification"},{"category":"method","location":"api/#ModelContextProtocol.cancel_subscription!-Tuple{Server, Any}","page":"API Reference","text":"cancel_subscription!(server::Server, request_id) -> Bool\n\nCancel an active subscriptions/listen stream by its originating request id — the notifications/cancelled path, which is how a stdio client (which cannot close a response stream) ends a subscription. The record is removed so no further messages carry its id; per JSON-RPC cancellation semantics no response is sent.\n\nOnly ROUTELESS records (stream transports like stdio, where the single shared channel means the cancellation can only come from the stream's own client) are cancellable this way. A route-bound (HTTP) stream is cancelled by closing the response stream itself — an in-band cancellation message must never be honored for it, because on HTTP the notification can arrive on ANY connection: honoring it would let one client end another client's stream by guessing its id.\n\nArguments\n\nserver::Server: The server holding the subscription\nrequest_id: The listen request's JSON-RPC id (from the notification's requestId)\n\nReturns\n\nBool: true when an active subscription was cancelled\n\n\n\n\n\n","title":"ModelContextProtocol.cancel_subscription!"},{"category":"method","location":"api/#ModelContextProtocol.close_subscriptions!-Tuple{Server}","page":"API Reference","text":"close_subscriptions!(server::Server) -> Nothing\n\nEnd all subscriptions/listen streams gracefully: each open stream receives the JSON-RPC response to its originating listen request (an empty complete result tagged with its subscription id and carrying serverInfo like every other modern result), which is how a client distinguishes an orderly server shutdown from an abrupt transport drop. Called during server shutdown.\n\nThe closing results are delivered while holding the registry lock, so a concurrent broadcast can never write on a stream AFTER its graceful result: the broadcast either completes first or finds an empty registry.\n\nArguments\n\nserver::Server: The server whose subscriptions to close\n\nReturns\n\nNothing\n\n\n\n\n\n","title":"ModelContextProtocol.close_subscriptions!"},{"category":"method","location":"api/#ModelContextProtocol.handle_subscriptions_listen-Tuple{ModelContextProtocol.RequestContext, ModelContextProtocol.SubscriptionsListenParams}","page":"API Reference","text":"handle_subscriptions_listen(ctx::RequestContext, params::SubscriptionsListenParams) -> HandlerResult\n\nOpen a subscriptions/listen stream: validate the filter (a malformed one is rejected with -32602, not silently emptied), send notifications/subscriptions/acknowledged as the stream's FIRST message (carrying the honored subset of the requested filter), register the subscription, and defer the request's response — the stream stays open until the client closes it, cancels it (notifications/cancelled on stdio), or the server shuts down (close_subscriptions!).\n\nAck-then-register happens under the registry lock, so a concurrent broadcast can neither enqueue a change notification ahead of the acknowledgment nor observe a half-registered stream — and a stream whose ack cannot be delivered is never registered at all.\n\nNotification types the server cannot deliver are omitted from the acknowledged filter rather than silently accepted, so a client can tell what it will actually receive.\n\nArguments\n\nctx::RequestContext: The current request context\nparams::SubscriptionsListenParams: The request's notification filter\n\nReturns\n\nHandlerResult: A deferred result (the response, if any, arrives at closure)\n\n\n\n\n\n","title":"ModelContextProtocol.handle_subscriptions_listen"},{"category":"method","location":"api/#ModelContextProtocol.legacy_session_notification-Tuple{ModelContextProtocol.ServerState, Any, String}","page":"API Reference","text":"legacy_session_notification(state::ServerState, transport, method::String;\n                            params::AbstractDict = LittleDict{String,Any}()) -> Bool\n\nDeliver a plain JSON-RPC notification to the connected legacy session, if there is one. The caller passes the session state and transport it already fetched (one snapshot per announcement — a concurrent restart cannot authorize against one session and deliver through another). The session must have completed a real initialize handshake: initialized alone is not proof (a bare notifications/initialized sets it), so a negotiated protocol_version is required too — a server that only ever served modern requests is never armed for legacy delivery.\n\nDelivery is transport-polymorphic via send_notification — stdout on stdio, the standalone GET SSE stream on Streamable HTTP. The ambient request route is explicitly bypassed for the send: when an announcement happens inside a request handler (legacy or modern), the legacy notification must go out-of-band to the legacy session's own stream, never onto the calling request's response channel (which on a modern request would leak it to a different — possibly differently authenticated — client).\n\nArguments\n\nstate::ServerState: The legacy session state snapshot\ntransport: The transport snapshot (Transport or nothing)\nmethod::String: The notification method\nparams::AbstractDict: The notification's params\n\nReturns\n\nBool: true when the notification was handed to the transport (enqueued — not proof of client receipt); false when there is no handshaken legacy session, no transport, or the send failed\n\n\n\n\n\n","title":"ModelContextProtocol.legacy_session_notification"},{"category":"method","location":"api/#ModelContextProtocol.list_changed_declared-Tuple{Server, Symbol}","page":"API Reference","text":"list_changed_declared(server::Server, kind::Symbol) -> Bool\n\nReport whether the server declares the listChanged capability for kind — the gate for sending notifications/{kind}/list_changed to the legacy session (modern subscriptions/listen streams are gated per-stream at ack time instead, via the honored filter subset).\n\nArguments\n\nserver::Server: The server whose declared capabilities to check\nkind::Symbol: One of :tools, :prompts, :resources\n\nReturns\n\nBool: Whether the matching capability declares list_changed = true\n\n\n\n\n\n","title":"ModelContextProtocol.list_changed_declared"},{"category":"method","location":"api/#ModelContextProtocol.subscription_notification-Tuple{String, AbstractDict, Any}","page":"API Reference","text":"subscription_notification(method::String, params::AbstractDict, sub_id) -> String\n\nSerialize a notification for delivery on a subscriptions/listen stream, tagging it with the subscription id the spec requires on every stream message.\n\nArguments\n\nmethod::String: The notification method\nparams::AbstractDict: The notification's params (the _meta tag is added here)\nsub_id: The subscription id (the listen request's JSON-RPC id)\n\nReturns\n\nString: The serialized JSON-RPC notification\n\n\n\n\n\n","title":"ModelContextProtocol.subscription_notification"},{"category":"method","location":"api/#ModelContextProtocol.process_message-Tuple{Server, ModelContextProtocol.ServerState, String}","page":"API Reference","text":"process_message(server::Server, state::ServerState, message::String) -> Union{String,Nothing}\n\nProcess an incoming JSON-RPC message and generate an appropriate response.\n\nArguments\n\nserver::Server: The MCP server instance\nstate::ServerState: Current server state\nmessage::String: Raw JSON-RPC message to process\n\nReturns\n\nUnion{String,Nothing}: A serialized response string or nothing for notifications\n\n\n\n\n\n","title":"ModelContextProtocol.process_message"},{"category":"method","location":"api/#ModelContextProtocol.run_server_loop-Tuple{Server, ModelContextProtocol.ServerState}","page":"API Reference","text":"run_server_loop(server::Server, state::ServerState) -> Nothing\n\nExecute the main server loop that reads JSON-RPC messages from the transport and writes responses back. Implements optimized CPU usage by blocking on input rather than active polling.\n\nArguments\n\nserver::Server: The MCP server instance with configured transport\nstate::ServerState: The server state object to track running status\n\nReturns\n\nNothing: The function runs until interrupted or state.running becomes false\n\n\n\n\n\n","title":"ModelContextProtocol.run_server_loop"},{"category":"type","location":"api/#ModelContextProtocol.CapabilityResponse","page":"API Reference","text":"CapabilityResponse(; \n    listChanged::Bool=false, \n    subscribe::Union{Bool,Nothing}=nothing, \n    tools::Union{Dict{String,Any},Nothing}=nothing, \n    resources::Union{Vector{Dict{String,Any}},Nothing}=nothing)\n\nDefine response structure for capabilities including tool and resource listings.\n\nFields\n\nlistChanged::Bool: Whether listings can change during server lifetime.\nsubscribe::Union{Bool,Nothing}: Whether subscriptions are supported.\ntools::Union{Dict{String,Any},Nothing}: Tool definitions by name.\nresources::Union{Vector{Dict{String,Any}},Nothing}: Available resource listings.\n\n\n\n\n\n","title":"ModelContextProtocol.CapabilityResponse"},{"category":"type","location":"api/#ModelContextProtocol.CompletionCapability","page":"API Reference","text":"CompletionCapability()\n\nAdvertise argument-completion support (completion/complete): contextual value suggestions for prompt arguments and resource-template variables, declared as the completions capability. Sources are configured per component via the completions field on MCPPrompt / ResourceTemplate; components without sources serve empty suggestion lists.\n\n\n\n\n\n","title":"ModelContextProtocol.CompletionCapability"},{"category":"type","location":"api/#ModelContextProtocol.LoggingCapability","page":"API Reference","text":"LoggingCapability(; levels::Vector{String}=[\"info\", \"warn\", \"error\"])\n\nConfigure logging-related capabilities for an MCP server.\n\nFields\n\nlevels::Vector{String}: Supported logging levels.\n\n\n\n\n\n","title":"ModelContextProtocol.LoggingCapability"},{"category":"type","location":"api/#ModelContextProtocol.PromptCapability","page":"API Reference","text":"PromptCapability(; list_changed::Bool=false)\n\nConfigure prompt-related capabilities for an MCP server.\n\nFields\n\nlist_changed::Bool: Whether server supports notifications when prompt listings change.\n\n\n\n\n\n","title":"ModelContextProtocol.PromptCapability"},{"category":"type","location":"api/#ModelContextProtocol.ResourceCapability","page":"API Reference","text":"ResourceCapability(; list_changed::Bool=false, subscribe::Bool=false)\n\nConfigure resource-related capabilities for an MCP server.\n\nFields\n\nlist_changed::Bool: Whether server supports notifications when resource listings change.\nsubscribe::Bool: Whether server supports subscriptions to resource updates.\n\n\n\n\n\n","title":"ModelContextProtocol.ResourceCapability"},{"category":"type","location":"api/#ModelContextProtocol.TaskCapability","page":"API Reference","text":"TaskCapability(; list::Bool=true, cancel::Bool=true)\n\nConfigure MCP Tasks support (SEP-1686, experimental): task-augmented tools/call plus the tasks/get, tasks/result, and optionally tasks/list/tasks/cancel operations. Only advertised to clients that negotiated protocol 2025-11-25 or later.\n\nFields\n\nlist::Bool: Whether tasks/list is offered. Note: on an HTTP transport without authentication the server cannot identify requestors, so list is withheld from the advertised capability regardless of this setting (per the spec's security guidance).\ncancel::Bool: Whether tasks/cancel is offered.\n\n\n\n\n\n","title":"ModelContextProtocol.TaskCapability"},{"category":"type","location":"api/#ModelContextProtocol.ToolCapability","page":"API Reference","text":"ToolCapability(; list_changed::Bool=false)\n\nConfigure tool-related capabilities for an MCP server.\n\nFields\n\nlist_changed::Bool: Whether server supports notifications when tool listings change.\n\n\n\n\n\n","title":"ModelContextProtocol.ToolCapability"},{"category":"method","location":"api/#ModelContextProtocol.capabilities_to_protocol-Tuple{Vector{ModelContextProtocol.Capability}, Server}","page":"API Reference","text":"capabilities_to_protocol(capabilities::Vector{Capability}, server::Server) -> Dict{String,Any}\n\nConvert server capabilities to the initialization response format required by the MCP protocol.\n\nArguments\n\ncapabilities::Vector{Capability}: List of server capabilities.\nserver::Server: The server containing tools and resources.\n\nReturns\n\nDict{String,Any}: Protocol-formatted capabilities dictionary including available tools and resources.\n\n\n\n\n\n","title":"ModelContextProtocol.capabilities_to_protocol"},{"category":"method","location":"api/#ModelContextProtocol.create_init_response-Tuple{Server, String}","page":"API Reference","text":"create_init_response(server::Server, protocol_version::String) -> InitializeResult\n\nCreate the initialization response for an MCP server.\n\nArguments\n\nserver::Server: The server to create the response for.\nprotocol_version::String: MCP protocol version string.\n\nReturns\n\nInitializeResult: Initialization response including server capabilities and info.\n\n\n\n\n\n","title":"ModelContextProtocol.create_init_response"},{"category":"method","location":"api/#ModelContextProtocol.merge_capabilities-Tuple{Vector{ModelContextProtocol.Capability}, Vector{ModelContextProtocol.Capability}}","page":"API Reference","text":"merge_capabilities(base::Vector{Capability}, override::Vector{Capability}) -> Vector{Capability}\n\nMerge two sets of capabilities, with the override set taking precedence.\n\nArguments\n\nbase::Vector{Capability}: Base set of capabilities.\noverride::Vector{Capability}: Override capabilities that take precedence.\n\nReturns\n\nVector{Capability}: Merged set of capabilities.\n\n\n\n\n\n","title":"ModelContextProtocol.merge_capabilities"},{"category":"method","location":"api/#ModelContextProtocol.to_protocol_format-Tuple{ModelContextProtocol.ResourceCapability}","page":"API Reference","text":"to_protocol_format(cap::Capability) -> Dict{String,Any}\n\nConvert an MCP capability to the JSON format expected by the MCP protocol.\n\nArguments\n\ncap::Capability: The capability to convert.\n\nReturns\n\nDict{String,Any}: Protocol-formatted capability dictionary.\n\n\n\n\n\n","title":"ModelContextProtocol.to_protocol_format"},{"category":"method","location":"api/#ModelContextProtocol.auto_register!-Tuple{Server, AbstractString}","page":"API Reference","text":"auto_register!(server::Server, dir::AbstractString) -> Server\n\nAutomatically register MCP components found in the specified directory structure.\n\nArguments\n\nserver::Server: The server to register components with\ndir::AbstractString: Root directory containing component subdirectories\n\nDirectory Structure\n\ndir/tools/: Contains tool definition files\ndir/resources/: Contains resource definition files\ndir/prompts/: Contains prompt definition files\n\nEach subdirectory is optional. Files should be .jl files containing component definitions.\n\nComponent File Format\n\nComponent files must have the tool/resource/prompt as the last expression in the file. The return value of include() is used to obtain the component.\n\nExample:\n\n# tools/my_tool.jl\njulia_version_tool = MCPTool(\n    name = \"julia_version\",\n    description = \"Get Julia version\",\n    handler = params -> Dict(\"version\" => string(VERSION))\n)  # ← This must be the last expression\n\nReturns\n\nServer: The updated server instance for method chaining\n\n\n\n\n\n","title":"ModelContextProtocol.auto_register!"},{"category":"method","location":"api/#ModelContextProtocol.default_capabilities-Tuple{}","page":"API Reference","text":"default_capabilities() -> Vector{Capability}\n\nCreate the default set of server capabilities for an MCP server.\n\nReturns\n\nVector{Capability}: Default capabilities including resources, tools, and prompts\n\n\n\n\n\n","title":"ModelContextProtocol.default_capabilities"},{"category":"method","location":"api/#ModelContextProtocol.normalize_path-Tuple{String}","page":"API Reference","text":"normalize_path(path::String) -> String\n\nConvert paths to absolute form, resolving relative paths against the project root.\n\nArguments\n\npath::String: The path to normalize\n\nReturns\n\nString: Absolute, normalized path with all symbolic links resolved\n\n\n\n\n\n","title":"ModelContextProtocol.normalize_path"},{"category":"method","location":"api/#ModelContextProtocol.scan_mcp_components-Tuple{String}","page":"API Reference","text":"scan_mcp_components(dir::String) -> Dict{Symbol,Vector}\n\nScan a directory recursively for MCP component definitions (tools, resources, prompts).\n\nArguments\n\ndir::String: Directory path to scan for component definitions\n\nReturns\n\nDict{Symbol,Vector}: Dictionary of found components grouped by type\n\n\n\n\n\n","title":"ModelContextProtocol.scan_mcp_components"},{"category":"type","location":"api/#ModelContextProtocol.ProgressToken","page":"API Reference","text":"ProgressToken\n\nType alias for tokens used to track long-running operations.\n\nType\n\nUnion{String,Int} - Can be either a string or integer identifier\n\n\n\n\n\n","title":"ModelContextProtocol.ProgressToken"},{"category":"type","location":"api/#ModelContextProtocol.RequestId","page":"API Reference","text":"RequestId\n\nType alias for JSON-RPC request identifiers.\n\nType\n\nUnion{String,Int} - Can be either a string or integer identifier\n\n\n\n\n\n","title":"ModelContextProtocol.RequestId"},{"category":"type","location":"api/#ModelContextProtocol.Capability","page":"API Reference","text":"Capability\n\nAbstract base type for all MCP protocol capabilities. Capabilities represent protocol features that servers can support. Concrete implementations define configuration for specific feature sets.\n\n\n\n\n\n","title":"ModelContextProtocol.Capability"},{"category":"type","location":"api/#ModelContextProtocol.MCPMessage","page":"API Reference","text":"MCPMessage\n\nAbstract base type for all message types in the MCP protocol. Serves as the root type for requests, responses, and notifications.\n\n\n\n\n\n","title":"ModelContextProtocol.MCPMessage"},{"category":"type","location":"api/#ModelContextProtocol.Notification","page":"API Reference","text":"Notification <: MCPMessage\n\nAbstract base type for one-way notifications in the MCP protocol. Notification messages don't expect a corresponding response.\n\n\n\n\n\n","title":"ModelContextProtocol.Notification"},{"category":"type","location":"api/#ModelContextProtocol.Progress","page":"API Reference","text":"Progress(; token::Union{String,Int}, current::Float64, \n        total::Union{Float64,Nothing}=nothing, message::Union{String,Nothing}=nothing)\n\nTrack progress of long-running operations in the MCP protocol.\n\nFields\n\ntoken::Union{String,Int}: Unique identifier for the progress tracker\ncurrent::Float64: Current progress value\ntotal::Union{Float64,Nothing}: Optional total expected value\nmessage::Union{String,Nothing}: Optional status message\n\n\n\n\n\n","title":"ModelContextProtocol.Progress"},{"category":"type","location":"api/#ModelContextProtocol.Request","page":"API Reference","text":"Request <: MCPMessage\n\nAbstract base type for client-to-server requests in the MCP protocol. Request messages expect a corresponding response from the server.\n\n\n\n\n\n","title":"ModelContextProtocol.Request"},{"category":"type","location":"api/#ModelContextProtocol.RequestParams","page":"API Reference","text":"RequestParams\n\nAbstract base type for all parameter structures in MCP protocol requests. Concrete subtypes define parameters for specific request methods.\n\n\n\n\n\n","title":"ModelContextProtocol.RequestParams"},{"category":"type","location":"api/#ModelContextProtocol.Resource","page":"API Reference","text":"Resource\n\nAbstract base type for all MCP resources. Resources represent data that can be read by clients.\n\n\n\n\n\n","title":"ModelContextProtocol.Resource"},{"category":"type","location":"api/#ModelContextProtocol.Response","page":"API Reference","text":"Response <: MCPMessage\n\nAbstract base type for server-to-client responses in the MCP protocol. Response messages are sent from the server in reply to client requests.\n\n\n\n\n\n","title":"ModelContextProtocol.Response"},{"category":"type","location":"api/#ModelContextProtocol.ResponseResult","page":"API Reference","text":"ResponseResult\n\nAbstract base type for all result structures in MCP protocol responses. Concrete subtypes define result formats for specific response methods.\n\n\n\n\n\n","title":"ModelContextProtocol.ResponseResult"},{"category":"type","location":"api/#ModelContextProtocol.Role","page":"API Reference","text":"Role\n\nEnum representing roles in the MCP protocol.\n\nValues\n\nuser: Content or messages from the user\nassistant: Content or messages from the assistant\n\n\n\n\n\n","title":"ModelContextProtocol.Role"},{"category":"type","location":"api/#ModelContextProtocol.ServerConfig","page":"API Reference","text":"ServerConfig(; name::String, version::String=\"1.0.0\",\n           description::String=\"\", capabilities::Vector{Capability}=Capability[],\n           instructions::String=\"\",\n           title::Union{String,Nothing}=nothing,\n           icons::Union{Vector{MCPIcon},Nothing}=nothing)\n\nDefine configuration settings for an MCP server instance.\n\nFields\n\nname::String: The server name shown to clients\nversion::String: Server implementation version (e.g., \"1.0.0\", \"2.3.1\") - YOUR server's version, not the protocol version\ndescription::String: Human-readable server description\ncapabilities::Vector{Capability}: Protocol capabilities supported by the server\ninstructions::String: Usage instructions for clients\ntitle::Union{String,Nothing}: Optional human-friendly display name\nicons::Union{Vector{MCPIcon},Nothing}: Optional icons for UI display\n\n\n\n\n\n","title":"ModelContextProtocol.ServerConfig"},{"category":"type","location":"api/#ModelContextProtocol.ServerError","page":"API Reference","text":"ServerError(message::String) <: Exception\n\nException type for MCP server-specific errors.\n\nFields\n\nmessage::String: The error message describing what went wrong\n\n\n\n\n\n","title":"ModelContextProtocol.ServerError"},{"category":"type","location":"api/#ModelContextProtocol.ServerState","page":"API Reference","text":"ServerState()\n\nTrack the internal state of an MCP server during operation.\n\nFields\n\ninitialized::Bool: Whether the server has been initialized by a client\nrunning::Bool: Whether the server main loop is active\nlast_request_id::Int: Last used request ID for server-initiated requests\npending_requests::Dict{RequestId,String}: Map of request IDs to method names\nprotocol_version::Union{String,Nothing}: MCP protocol version negotiated during initialization (see negotiate_version), or nothing before the client initializes. Server-global, not data-raced: all handler execution is serialized through run_server_loop. As with session_id, the transport is single-session, so a repeat initialize overwrites this (\"last initialize wins\")\nwire_subscriptions::Set{String}: Resource URIs the client subscribed to via resources/subscribe\n\n\n\n\n\n","title":"ModelContextProtocol.ServerState"},{"category":"type","location":"api/#ModelContextProtocol.Subscription","page":"API Reference","text":"Subscription(; uri::String, callback::Function, created_at::DateTime=now())\n\nDefine a subscription to resource updates in the MCP protocol.\n\nFields\n\nuri::String: The URI of the subscribed resource\ncallback::Function: Function to call when the resource is updated\ncreated_at::DateTime: When the subscription was created\n\n\n\n\n\n","title":"ModelContextProtocol.Subscription"},{"category":"type","location":"api/#ModelContextProtocol.Tool","page":"API Reference","text":"Tool\n\nAbstract base type for all MCP tools. Tools represent operations that can be invoked by clients.\n\n\n\n\n\n","title":"ModelContextProtocol.Tool"},{"category":"method","location":"api/#Base.convert-Tuple{Type{URIs.URI}, String}","page":"API Reference","text":"convert(::Type{URI}, s::String) -> URI\n\nConvert a string to a URI object.\n\nArguments\n\ns::String: The string to convert\n\nReturns\n\nURI: The resulting URI object\n\n\n\n\n\n","title":"Base.convert"},{"category":"method","location":"api/#ModelContextProtocol.icon_to_dict-Tuple{MCPIcon}","page":"API Reference","text":"icon_to_dict(icon::MCPIcon) -> LittleDict{String,Any}\n\nSerialize an MCPIcon to a dictionary, omitting nothing fields.\n\n\n\n\n\n","title":"ModelContextProtocol.icon_to_dict"},{"category":"type","location":"api/#ModelContextProtocol.PendingTaskInput","page":"API Reference","text":"PendingTaskInput(method::String, params, channel::Channel{Any})\n\nOne outstanding mid-task input request on an extension-era task (SEP-2663): the request the client sees under the task's inputRequests (as {method, params?} on tasks/get), paired with the channel the blocked task_await_input waiter takes the client's tasks/update response from. The channel is buffered (size 1) and receives exactly one value — the key is removed from the pending map in the same critical section that delivers, so a key can never be answered twice.\n\nFields\n\nmethod::String: The input request's method (e.g. elicitation/create)\nparams::Any: The input request's params (a Dict, or nothing for none)\nchannel::Channel{Any}: Delivery channel to the blocked handler; closed (never fed) when the task is cancelled, unwinding the waiter\n\n\n\n\n\n","title":"ModelContextProtocol.PendingTaskInput"},{"category":"type","location":"api/#ModelContextProtocol.TaskDetachState","page":"API Reference","text":"TaskDetachState(transport, route)\n\nPer-call state for a detachable modern-era tools/call (tasks extension, SEP-2663). Created when the call is spawned off-loop with its response route captured; task_detach(ctx) uses it to mint the task and deliver the CreateTaskResult, and the spawn wrapper uses it to learn whether the handler detached. All access is under lock.\n\nFields\n\nlock::ReentrantLock: Guards record/closed against the detach/finish race\ntransport::Any: The serving transport at spawn time\nroute::Any: The captured response route (see capture_response_route)\nrequired_scopes::Vector{String}: The tool's required_scopes, recorded onto the minted task for per-request re-authorization of the extension surface\nrecord::Union{TaskRecord,Nothing}: The minted task, published the moment it is durably created — BEFORE any fallible wire building or delivery, so a failure in between can never orphan a hidden non-terminal record\nclosed::Bool: Set when the handler has returned — a late task_detach (e.g. from a stray child task) must fail rather than deliver a second response\ncreate_delivered::Bool: Set once the CreateTaskResult delivery was attempted; while false, the request is still owed a response (the spawn wrapper answers -32603 if the handoff died between task creation and delivery)\n\n\n\n\n\n","title":"ModelContextProtocol.TaskDetachState"},{"category":"type","location":"api/#ModelContextProtocol.TaskRecord","page":"API Reference","text":"TaskRecord\n\nMutable record of one server-side task (a task-augmented request execution).\n\nFields\n\ntask_id::String: Receiver-generated unique identifier (cryptographically random)\nstatus::String: One of \"working\", \"input_required\", \"completed\", \"failed\", \"cancelled\"\nstatus_message::Union{String,Nothing}: Optional human-readable status detail\ncreated_at::DateTime: UTC creation timestamp\nlast_updated_at::DateTime: UTC timestamp of the last status change\nttl_ms::Union{Int,Nothing}: Actual retention duration from creation; nothing = unlimited\npoll_interval_ms::Union{Int,Nothing}: Suggested client polling interval\nprincipal::Union{String,Nothing}: Authorization binding (authenticated subject), nothing when unauthenticated\nmethod::String: The originating request method (e.g. \"tools/call\")\nera::Symbol: :legacy for SEP-1686 experimental tasks (2025-11-25 sessions), :ext for the modern-era io.modelcontextprotocol/tasks extension (SEP-2663). The eras are wire-incompatible and never serve each other's records\nrequired_scopes::Vector{String}: The originating tool's required_scopes, re-checked on every extension-era task request (SEP-2663 requires authorization on each task-related request — a later token for the same subject but without the tool's scopes must not read the result). Empty when the tool declares none\nresult::Union{CallToolResult,Nothing}: Final result when the underlying call succeeded (or failed via isError)\nerror::Union{ErrorInfo,Nothing}: Final JSON-RPC error when the underlying call errored\ndone::Base.Event: Set exactly once when the task reaches a terminal status\ncancel_requested::Threads.Atomic{Bool}: Set once tasks/cancel is accepted; atomic so handlers can poll it lock-free from worker threads via task_cancelled(ctx)\npending_inputs::LittleDict{String,PendingTaskInput}: Outstanding mid-task input requests keyed by server-minted id (extension era only; insertion-ordered, so inputRequests serializes in issue order). Non-empty exactly while the task is input_required\ninput_key_counter::Int: Monotone counter minting pending_inputs keys — a key is never reused over the task's lifetime, even after its request is answered (SEP-2663 key-uniqueness rule)\n\nAll mutation goes through the owning TaskStore under its lock.\n\n\n\n\n\n","title":"ModelContextProtocol.TaskRecord"},{"category":"type","location":"api/#ModelContextProtocol.TaskStore","page":"API Reference","text":"TaskStore(; default_ttl_ms=TASK_DEFAULT_TTL_MS, max_ttl_ms=TASK_MAX_TTL_MS,\n          poll_interval_ms=TASK_POLL_INTERVAL_MS, page_size=TASKS_PAGE_SIZE)\n\nThread-safe registry of server-side tasks.\n\nFields\n\nlock::ReentrantLock: Guards all record access and mutation\ntasks::Dict{String,TaskRecord}: Records by task id\ndefault_ttl_ms::Int: ttl applied when the requestor does not ask for one\nmax_ttl_ms::Int: Upper bound applied to requested ttls\npoll_interval_ms::Int: Suggested polling interval included in task responses\npage_size::Int: Page size for tasks/list cursor pagination\non_status_change::Base.RefValue{Any}: Optional hook record -> Nothing fired after every status transition, while the store lock is held (see _fire_status_change); installed for the tasks extension's notifications/tasks. nothing disables it\nnotification_seq::Threads.Atomic{Int}: Monotone sequence stamped on every enqueued status notification. Atomic on purpose: it is written at transition sites (under the store lock) but read for registration thresholds under the unrelated registry lock — a plain Ref would be a data race on a threaded runtime. Subscriptions record the counter at registration and receive only later-stamped events, so a queued-but-undrained backlog never replays older states to a freshly registered stream\n\n\n\n\n\n","title":"ModelContextProtocol.TaskStore"},{"category":"method","location":"api/#ModelContextProtocol._fire_status_change-Tuple{ModelContextProtocol.TaskStore, ModelContextProtocol.TaskRecord}","page":"API Reference","text":"_fire_status_change(store::TaskStore, record::TaskRecord) -> Nothing\n\nFire the store's status-change hook for a record whose status just changed. Called at every transition site WHILE the store lock is held — the hook builds the task's wire snapshot (which requires the lock) and ENQUEUES it (no transport I/O happens under the store lock; a dedicated dispatcher task broadcasts from the queue, see install_task_notifications!). A throwing hook is swallowed: a notification must never break the transition that triggered it.\n\n\n\n\n\n","title":"ModelContextProtocol._fire_status_change"},{"category":"method","location":"api/#ModelContextProtocol.cancel_task!-Tuple{ModelContextProtocol.TaskStore, ModelContextProtocol.TaskRecord}","page":"API Reference","text":"cancel_task!(store::TaskStore, record::TaskRecord) -> Bool\n\nTransition a task to \"cancelled\". Returns false without mutating when the task is already terminal (the handler maps that to a -32602 error per spec). Sets cancel_requested so cooperative handlers can observe it via task_cancelled(ctx), and unblocks any task_await_input waiter by draining the pending input requests.\n\n\n\n\n\n","title":"ModelContextProtocol.cancel_task!"},{"category":"method","location":"api/#ModelContextProtocol.create_task!-Tuple{ModelContextProtocol.TaskStore, String}","page":"API Reference","text":"create_task!(store::TaskStore, method::String;\n             requested_ttl_ms=nothing, principal=nothing, era=:legacy,\n             status_message=nothing) -> TaskRecord\n\nCreate a new task record in \"working\" status with a cryptographically random task id. The requested ttl is clamped to the store's max_ttl_ms; when absent the store's default_ttl_ms applies.\n\n\n\n\n\n","title":"ModelContextProtocol.create_task!"},{"category":"method","location":"api/#ModelContextProtocol.drain_pending_inputs!-Tuple{ModelContextProtocol.TaskRecord}","page":"API Reference","text":"drain_pending_inputs!(record::TaskRecord) -> Nothing\n\nClose and clear every outstanding mid-task input request on a task entering a terminal status: a blocked task_await_input waiter observes its channel closing and unwinds (there is nothing left that could ever feed it). Caller must hold the store lock — which is also what makes close-vs-deliver atomic: tasks/update delivers under the same lock, so a channel is either fed exactly once or closed, never both.\n\n\n\n\n\n","title":"ModelContextProtocol.drain_pending_inputs!"},{"category":"method","location":"api/#ModelContextProtocol.expire_parked_input!-Tuple{ModelContextProtocol.TaskStore, ModelContextProtocol.TaskRecord, Dates.DateTime}","page":"API Reference","text":"expire_parked_input!(store::TaskStore, record::TaskRecord, now_utc::DateTime) -> Bool\n\nFail an extension-era task whose ttl elapsed while parked in input_required: a task past its ttl that is waiting on CLIENT input is abandoned — the client the server is waiting on can no longer use the result — so it is terminalized (status failed, error inlined) and its pending inputs drained, unwinding the blocked task_await_input waiter. A no-op (returning false) for any record not in exactly that state, so the deadline timer and the store sweep can both call it and whichever runs first wins. Caller must hold the store lock.\n\n\n\n\n\n","title":"ModelContextProtocol.expire_parked_input!"},{"category":"method","location":"api/#ModelContextProtocol.finish_task!-Tuple{ModelContextProtocol.TaskStore, ModelContextProtocol.TaskRecord, Union{CallToolResult, ModelContextProtocol.ErrorInfo}}","page":"API Reference","text":"finish_task!(store::TaskStore, record::TaskRecord,\n             outcome::Union{CallToolResult,ErrorInfo}) -> Bool\n\nTransition a task to its terminal status from a completed execution. An ErrorInfo (a JSON-RPC error) is always \"failed\". For a CallToolResult the eras diverge: legacy (SEP-1686) maps is_error to \"failed\", while the extension era (SEP-2663) requires \"completed\" for ANY result — \"failed\" is reserved for JSON-RPC errors, and a tool-level isError:true result completes with the result inlined. Returns false without mutating when the task is already terminal (e.g. cancelled while the work was still running — cancelled tasks MUST stay cancelled, so the outcome is discarded).\n\n\n\n\n\n","title":"ModelContextProtocol.finish_task!"},{"category":"method","location":"api/#ModelContextProtocol.get_task-Tuple{ModelContextProtocol.TaskStore, String, Union{Nothing, String}}","page":"API Reference","text":"get_task(store::TaskStore, task_id::String,\n         principal::Union{String,Nothing}; era=:legacy) -> Union{TaskRecord,Nothing}\n\nLook up a task by id, enforcing authorization-context binding: a record is only returned when its stored principal matches the requestor's AND it belongs to the requested era (legacy sessions cannot see extension tasks and vice versa — the wire shapes are incompatible). A mismatch returns nothing (indistinguishable from \"not found\", so task existence is not leaked).\n\n\n\n\n\n","title":"ModelContextProtocol.get_task"},{"category":"method","location":"api/#ModelContextProtocol.iso8601_utc-Tuple{Dates.DateTime}","page":"API Reference","text":"iso8601_utc(dt::DateTime) -> String\n\nFormat a UTC DateTime as an ISO 8601 / RFC 3339 timestamp with a Z suffix.\n\n\n\n\n\n","title":"ModelContextProtocol.iso8601_utc"},{"category":"method","location":"api/#ModelContextProtocol.list_tasks-Tuple{ModelContextProtocol.TaskStore, Union{Nothing, String}, Union{Nothing, String}}","page":"API Reference","text":"list_tasks(store::TaskStore, principal::Union{String,Nothing},\n           cursor::Union{String,Nothing}; era=:legacy)\n    -> Tuple{Vector{TaskRecord},Union{String,Nothing}}\n\nReturn one page of the requestor's tasks (oldest first) and the next-page cursor, or nothing for the cursor when no further pages exist. Only tasks bound to the same principal AND era are visible (tasks/list exists only in the legacy era; extension records must never leak into it — the extension deliberately has no list, so one caller's task ids cannot be exposed to another). Throws ArgumentError for an invalid cursor (mapped to -32602 by the handler).\n\n\n\n\n\n","title":"ModelContextProtocol.list_tasks"},{"category":"method","location":"api/#ModelContextProtocol.sweep_expired!-Tuple{ModelContextProtocol.TaskStore}","page":"API Reference","text":"sweep_expired!(store::TaskStore) -> Nothing\n\nDelete terminal task records whose ttl has elapsed, and fail expired extension-era tasks still parked in input_required (see expire_parked_input! — here as a backstop; the clock-driven per-wait deadline timer in task_await_input is what guarantees the transition without further store activity). Once failed, such a record is terminal AND expired, so the next sweep deletes it — a post-expiry poll observes either the failed record briefly (when its own sweep is the one that just terminalized it) or task-not-found, timing-dependent; neither is promised. Other non-terminal records are retained past their ttl (the spec permits but does not require deleting those, and their background work may still be running). Caller must hold store.lock.\n\n\n\n\n\n","title":"ModelContextProtocol.sweep_expired!"},{"category":"method","location":"api/#ModelContextProtocol.task_is_expired-Tuple{ModelContextProtocol.TaskRecord, Dates.DateTime}","page":"API Reference","text":"task_is_expired(record::TaskRecord, now_utc::DateTime) -> Bool\n\nReturn whether the task's ttl (counted from creation) has elapsed. Unlimited (ttl_ms === nothing) tasks never expire.\n\n\n\n\n\n","title":"ModelContextProtocol.task_is_expired"},{"category":"method","location":"api/#ModelContextProtocol.task_is_terminal-Tuple{ModelContextProtocol.TaskRecord}","page":"API Reference","text":"task_is_terminal(record::TaskRecord) -> Bool\n\nReturn whether the task is in a terminal status (\"completed\", \"failed\", or \"cancelled\").\n\n\n\n\n\n","title":"ModelContextProtocol.task_is_terminal"},{"category":"method","location":"api/#ModelContextProtocol.task_wire-Tuple{ModelContextProtocol.TaskRecord}","page":"API Reference","text":"task_wire(record::TaskRecord) -> LittleDict{String,Any}\n\nSerialize a task record to the spec wire shape: taskId, status, optional statusMessage, createdAt/lastUpdatedAt (ISO 8601 UTC), ttl (always present; null for unlimited), and optional pollInterval.\n\n\n\n\n\n","title":"ModelContextProtocol.task_wire"},{"category":"type","location":"api/#ModelContextProtocol.SubscriptionFilter","page":"API Reference","text":"SubscriptionFilter(; tools_list_changed=false, prompts_list_changed=false,\n                   resources_list_changed=false, resource_uris=Set{String}(),\n                   task_ids=Set{String}())\n\nThe notification types a subscriptions/listen stream opted into. A server MUST NOT send notification types the client did not explicitly request, so every delivery is checked against this filter.\n\nFields\n\ntools_list_changed::Bool: deliver notifications/tools/list_changed\nprompts_list_changed::Bool: deliver notifications/prompts/list_changed\nresources_list_changed::Bool: deliver notifications/resources/list_changed\nresource_uris::Set{String}: deliver notifications/resources/updated for these URIs (a set: membership is checked on every update broadcast)\ntask_ids::Set{String}: deliver notifications/tasks status updates for these task ids (tasks extension, SEP-2663; only ids the requestor could tasks/get are ever agreed to)\n\n\n\n\n\n","title":"ModelContextProtocol.SubscriptionFilter"},{"category":"type","location":"api/#ModelContextProtocol.SubscriptionRecord","page":"API Reference","text":"SubscriptionRecord(id, filter, route, transport,\n                   task_principal=nothing, task_scopes=Set{String}())\n\nOne active subscriptions/listen stream.\n\nFields\n\nid::Union{String,Int}: the listen request's JSON-RPC id — also the io.modelcontextprotocol/subscriptionId every message on the stream carries\nfilter::SubscriptionFilter: the notification types this stream opted into\nroute::Any: the transport route handle the notifications are delivered on (nothing for stream transports like stdio, which share one channel)\ntransport::Any: the transport to deliver over (Transport; typed Any to avoid include-order coupling)\ntask_principal::Union{String,Nothing}: the listen requestor's extension-task principal, re-checked at every notifications/tasks delivery\ntask_scopes::Set{String}: the listen requestor's token scopes, re-checked against the task's required_scopes at every notifications/tasks delivery — a task whose authorization requirements changed after the listen must not keep leaking status to a stream that could no longer tasks/get it\ntask_seq::Int: the task-notification sequence at registration; only events stamped LATER are delivered to this stream, so a queued-but-undrained backlog never replays states older than the stream's own initial snapshot\n\n\n\n\n\n","title":"ModelContextProtocol.SubscriptionRecord"},{"category":"type","location":"api/#ModelContextProtocol.SubscriptionRegistry","page":"API Reference","text":"SubscriptionRegistry()\n\nRegistry of active subscriptions/listen streams. Guarded by a lock: the single server loop registers streams while HTTP connection tasks and background task executions can broadcast notifications concurrently. Deliveries happen WHILE holding this lock (all delivery paths enqueue without blocking), which is what guarantees the acknowledged-first and nothing-after-the-closing-result orderings.\n\nLock ordering: registry.lock may be held when a delivery takes the HTTP transport's channels_lock, never the reverse — nothing takes registry.lock while holding channels_lock.\n\nFields\n\nsubs::Vector{SubscriptionRecord}: active subscriptions\nlock::ReentrantLock: guards subs\n\n\n\n\n\n","title":"ModelContextProtocol.SubscriptionRegistry"},{"category":"method","location":"api/#ModelContextProtocol.filter_to_wire-Tuple{ModelContextProtocol.SubscriptionFilter}","page":"API Reference","text":"filter_to_wire(f::SubscriptionFilter) -> LittleDict{String,Any}\n\nSerialize a filter to the wire shape used in the acknowledgment's notifications field, which reflects the subset the server agreed to honor. Types not subscribed are omitted, and the resource URIs are emitted sorted (the in-memory set has no stable order).\n\nArguments\n\nf::SubscriptionFilter: The filter\n\nReturns\n\nLittleDict{String,Any}: The honored subset\n\n\n\n\n\n","title":"ModelContextProtocol.filter_to_wire"},{"category":"function","location":"api/#ModelContextProtocol.filter_wants","page":"API Reference","text":"filter_wants(f::SubscriptionFilter, method::String,\n             uri::Union{String,Nothing}=nothing,\n             task_id::Union{String,Nothing}=nothing) -> Bool\n\nWhether a notification of method (for resource updates, of uri; for task status updates, of task_id) was opted into.\n\nArguments\n\nf::SubscriptionFilter: The subscription's filter\nmethod::String: The notification method\nuri::Union{String,Nothing}: The resource URI for notifications/resources/updated\ntask_id::Union{String,Nothing}: The task id for notifications/tasks\n\nReturns\n\nBool: true when the notification may be delivered on this stream\n\n\n\n\n\n","title":"ModelContextProtocol.filter_wants"},{"category":"method","location":"api/#ModelContextProtocol.parse_subscription_filter-Tuple{Any}","page":"API Reference","text":"parse_subscription_filter(notifications) -> Union{SubscriptionFilter,String}\n\nValidate and parse the notifications filter object of a subscriptions/listen request. Returns the parsed filter, or a violation message the caller must reject with -32602: the filter must be an object, the *ListChanged flags booleans, resourceSubscriptions an array of at most MAX_RESOURCE_SUBSCRIPTIONS strings, and at least one notification type must be requested — a malformed filter must not silently establish a long-lived stream that can never deliver anything.\n\nUnknown keys are ignored (forward compatibility), and requesting types this server cannot deliver is NOT a violation: the honored subset in the acknowledgment is how the server communicates what it will actually send (see handle_subscriptions_listen).\n\nArguments\n\nnotifications: The request's params.notifications value (any JSON value)\n\nReturns\n\nUnion{SubscriptionFilter,String}: The parsed filter, or the violation\n\n\n\n\n\n","title":"ModelContextProtocol.parse_subscription_filter"},{"category":"type","location":"api/#ModelContextProtocol.TransportError","page":"API Reference","text":"TransportError(message::String) <: Exception\n\nException type for transport-specific errors.\n\nFields\n\nmessage::String: The error message describing what went wrong\n\n\n\n\n\n","title":"ModelContextProtocol.TransportError"},{"category":"method","location":"api/#ModelContextProtocol.capture_response_route-Tuple{Transport}","page":"API Reference","text":"capture_response_route(transport::Transport) -> Any\n\nCapture a route handle for delivering the CURRENT request's response later, from outside the server loop (used by tasks/result, which must block until the task completes without blocking the loop). The returned handle is passed to deliver_response.\n\nThe default returns nothing — for stream transports like stdio, responses carry their correlation in the JSON-RPC id, so no per-request route exists. Transports that route responses per request (HTTP) override this to detach and return the current request's route so the loop can move on to the next request.\n\nArguments\n\ntransport::Transport: The transport instance\n\nReturns\n\nAn opaque route handle understood by deliver_response for this transport\n\n\n\n\n\n","title":"ModelContextProtocol.capture_response_route"},{"category":"function","location":"api/#ModelContextProtocol.close","page":"API Reference","text":"close(transport::Transport) -> Nothing\n\nClose the transport connection and clean up resources.\n\nArguments\n\ntransport::Transport: The transport instance to close\n\nReturns\n\nNothing\n\n\n\n\n\n","title":"ModelContextProtocol.close"},{"category":"method","location":"api/#ModelContextProtocol.deliver_log_notification-Tuple{Transport, Any, String}","page":"API Reference","text":"deliver_log_notification(transport::Transport, route::Any, message::String) -> Bool\n\nDeliver a request-scoped log notification (ModernRequestLogger) on a request's response route. Distinct from deliver_notification because the failure policy differs: a subscriptions/listen stream that cannot keep up is load-shed by CLOSING it, but a request's response channel must never be closed under log pressure — the final response still has to travel it. Implementations drop the record (returning false, which sends it to the logger's fallback stream) rather than sacrifice the channel.\n\nThe default delegates to deliver_notification (correct for stdio's shared stream, which has no per-request channel to protect).\n\nArguments\n\ntransport::Transport: The transport instance\nroute::Any: The request's captured notification route\nmessage::String: The serialized JSON-RPC notification\n\nReturns\n\nBool: true when the notification was delivered\n\n\n\n\n\n","title":"ModelContextProtocol.deliver_log_notification"},{"category":"method","location":"api/#ModelContextProtocol.deliver_notification-Tuple{Transport, Any, String}","page":"API Reference","text":"deliver_notification(transport::Transport, route::Any, message::String) -> Bool\n\nDeliver a notification on a route captured earlier with capture_response_route, used by subscriptions/listen streams (whose request never completes, so its route carries notifications instead). Returns whether delivery succeeded — false means the client is gone and the subscription should be pruned.\n\nThe default writes to the shared stream (correct for stdio, where all messages share one channel and the io.modelcontextprotocol/subscriptionId tag demultiplexes them). Transports that route per request (HTTP) override this to push onto that request's response stream.\n\nArguments\n\ntransport::Transport: The transport instance\nroute::Any: The handle returned by capture_response_route\nmessage::String: The serialized JSON-RPC notification\n\nReturns\n\nBool: true when the notification was delivered\n\n\n\n\n\n","title":"ModelContextProtocol.deliver_notification"},{"category":"method","location":"api/#ModelContextProtocol.deliver_response-Tuple{Transport, Any, String}","page":"API Reference","text":"deliver_response(transport::Transport, route::Any, message::String) -> Nothing\n\nDeliver a serialized JSON-RPC response for a request whose route was captured earlier with capture_response_route. Safe to call from a background task. The default ignores the route and writes to the shared stream (correct for stdio, where write_message is lock-serialized).\n\nArguments\n\ntransport::Transport: The transport instance\nroute::Any: The handle returned by capture_response_route\nmessage::String: The serialized JSON-RPC response\n\nReturns\n\nNothing\n\n\n\n\n\n","title":"ModelContextProtocol.deliver_response"},{"category":"method","location":"api/#ModelContextProtocol.flush-Tuple{Transport}","page":"API Reference","text":"flush(transport::Transport) -> Nothing\n\nFlush any buffered data in the transport. Default implementation does nothing.\n\nArguments\n\ntransport::Transport: The transport instance to flush\n\nReturns\n\nNothing\n\n\n\n\n\n","title":"ModelContextProtocol.flush"},{"category":"function","location":"api/#ModelContextProtocol.is_connected","page":"API Reference","text":"is_connected(transport::Transport) -> Bool\n\nCheck if the transport is currently connected and operational.\n\nArguments\n\ntransport::Transport: The transport instance to check\n\nReturns\n\nBool: true if connected and ready, false otherwise\n\n\n\n\n\n","title":"ModelContextProtocol.is_connected"},{"category":"method","location":"api/#ModelContextProtocol.notification_route-Tuple{Transport}","page":"API Reference","text":"notification_route(transport::Transport) -> Any\n\nReturn the route handle identifying the request the transport just handed to the server loop, for request-scoped notification delivery. The loop stores it in task-local storage around message processing so send_notification can route notifications emitted during synchronous handling onto that request's response stream. The default returns nothing — stdio has a single shared stream, so notifications need no routing. HTTP overrides this with the current request's ID.\n\nArguments\n\ntransport::Transport: The transport instance\n\nReturns\n\nAn opaque route handle, or nothing when the transport needs no routing\n\n\n\n\n\n","title":"ModelContextProtocol.notification_route"},{"category":"method","location":"api/#ModelContextProtocol.pending_auth_context-Tuple{Transport}","page":"API Reference","text":"pending_auth_context(transport::Transport) -> Union{Nothing,Any}\n\nReturn the authenticated user associated with the message most recently read from transport, or nothing when the transport has no per-request authentication (e.g. stdio). Transports that authenticate requests override this; the default is nothing.\n\n\n\n\n\n","title":"ModelContextProtocol.pending_auth_context"},{"category":"method","location":"api/#ModelContextProtocol.pending_param_headers-Tuple{Transport}","page":"API Reference","text":"pending_param_headers(transport::Transport) -> Union{Nothing,Dict{String,Any}}\n\nReturn the Mcp-Param-* custom headers (SEP-2243 parameter mirroring) of the message most recently read from transport: a Dict keyed by LOWERCASED header suffix, with String values (OWS-stripped) or :invalid for duplicated/unsafe headers — or nothing when the transport carries no HTTP headers (e.g. stdio), which disables the mirroring validation entirely. HTTP overrides this; the default is nothing.\n\n\n\n\n\n","title":"ModelContextProtocol.pending_param_headers"},{"category":"function","location":"api/#ModelContextProtocol.read_message","page":"API Reference","text":"read_message(transport::Transport) -> Union{String,Nothing}\n\nRead a single message from the transport.\n\nArguments\n\ntransport::Transport: The transport instance to read from\n\nReturns\n\nUnion{String,Nothing}: The message string if available, or nothing if no message or connection closed\n\n\n\n\n\n","title":"ModelContextProtocol.read_message"},{"category":"method","location":"api/#ModelContextProtocol.route_alive-Tuple{Transport, Any}","page":"API Reference","text":"route_alive(transport::Transport, route::Any) -> Bool\n\nWhether the client behind a captured response route is still reachable — used by subscription broadcasts to sweep dead streams without writing anything to them.\n\nThe default returns true: stream transports (stdio) have no per-request route whose death is observable here, and their delivery attempt reports failure itself. HTTP overrides this to check whether the route's channel still exists and is open.\n\nArguments\n\ntransport::Transport: The transport instance\nroute::Any: The handle returned by capture_response_route\n\nReturns\n\nBool: true when the route may still deliver\n\n\n\n\n\n","title":"ModelContextProtocol.route_alive"},{"category":"method","location":"api/#ModelContextProtocol.send_notification-Tuple{Transport, String}","page":"API Reference","text":"send_notification(transport::Transport, message::String) -> Nothing\n\nDeliver a server-to-client JSON-RPC notification over transport.\n\nThe default writes the message directly — correct for stdio, where notifications and responses share one stream. Transports that multiplex per-request responses (e.g. HTTP, where write_message routes to the calling request's response channel) override this to send notifications over a separate out-of-band channel, so a mid-request notification never corrupts that request's response.\n\nArguments\n\ntransport::Transport: The transport instance to send over\nmessage::String: The serialized JSON-RPC notification\n\nReturns\n\nNothing\n\n\n\n\n\n","title":"ModelContextProtocol.send_notification"},{"category":"method","location":"api/#ModelContextProtocol.set_negotiated_version!-Tuple{Transport, String}","page":"API Reference","text":"set_negotiated_version!(transport::Transport, version::String) -> Nothing\n\nInform the transport of the protocol version negotiated during initialize, so transports that advertise a version per response (e.g. the HTTP MCP-Protocol-Version header) echo the negotiated one rather than a static default. Default is a no-op (stdio carries no version metadata).\n\nArguments\n\ntransport::Transport: The transport instance to update\nversion::String: The negotiated MCP protocol version\n\nReturns\n\nNothing\n\n\n\n\n\n","title":"ModelContextProtocol.set_negotiated_version!"},{"category":"function","location":"api/#ModelContextProtocol.write_message","page":"API Reference","text":"write_message(transport::Transport, message::String) -> Nothing\n\nWrite a message to the transport.\n\nArguments\n\ntransport::Transport: The transport instance to write to\nmessage::String: The message to send\n\nReturns\n\nNothing\n\nThrows\n\nTransportError: If the message cannot be sent\n\n\n\n\n\n","title":"ModelContextProtocol.write_message"},{"category":"method","location":"api/#ModelContextProtocol.close-Tuple{StdioTransport}","page":"API Reference","text":"close(transport::StdioTransport) -> Nothing\n\nMark the transport as closed. Does not actually close stdin/stdout.\n\nArguments\n\ntransport::StdioTransport: The stdio transport instance\n\nReturns\n\nNothing\n\n\n\n\n\n","title":"ModelContextProtocol.close"},{"category":"method","location":"api/#ModelContextProtocol.flush-Tuple{StdioTransport}","page":"API Reference","text":"flush(transport::StdioTransport) -> Nothing\n\nFlush the output stream.\n\nArguments\n\ntransport::StdioTransport: The stdio transport instance\n\nReturns\n\nNothing\n\n\n\n\n\n","title":"ModelContextProtocol.flush"},{"category":"method","location":"api/#ModelContextProtocol.is_connected-Tuple{StdioTransport}","page":"API Reference","text":"is_connected(transport::StdioTransport) -> Bool\n\nCheck if the stdio transport is connected.\n\nArguments\n\ntransport::StdioTransport: The stdio transport instance\n\nReturns\n\nBool: Connection status\n\n\n\n\n\n","title":"ModelContextProtocol.is_connected"},{"category":"method","location":"api/#ModelContextProtocol.read_message-Tuple{StdioTransport}","page":"API Reference","text":"read_message(transport::StdioTransport) -> Union{String,Nothing}\n\nRead a line from the input stream.\n\nArguments\n\ntransport::StdioTransport: The stdio transport instance\n\nReturns\n\nUnion{String,Nothing}: The message string, or nothing if empty or EOF\n\n\n\n\n\n","title":"ModelContextProtocol.read_message"},{"category":"method","location":"api/#ModelContextProtocol.write_message-Tuple{StdioTransport, String}","page":"API Reference","text":"write_message(transport::StdioTransport, message::String) -> Nothing\n\nWrite a message to the output stream with a newline.\n\nArguments\n\ntransport::StdioTransport: The stdio transport instance\nmessage::String: The message to write\n\nReturns\n\nNothing\n\nThrows\n\nTransportError: If writing fails\n\n\n\n\n\n","title":"ModelContextProtocol.write_message"},{"category":"type","location":"api/#ModelContextProtocol.QueuedHttpRequest","page":"API Reference","text":"QueuedHttpRequest(id, body, user, param_headers=nothing)\n\nEnvelope for a request on the HTTP work queue. Carries the per-request authenticated user (or nothing) and the request's Mcp-Param-* custom headers (SEP-2243 parameter mirroring; nothing for messages that carry none) from the concurrent connection handler to the single server loop, so per-request context travels with the request rather than via shared transport state.\n\n\n\n\n\n","title":"ModelContextProtocol.QueuedHttpRequest"},{"category":"method","location":"api/#ModelContextProtocol.accepts_sse-Tuple{AbstractString}","page":"API Reference","text":"accepts_sse(accept_header::AbstractString) -> Bool\n\nWhether an Accept header admits a text/event-stream response, per media-range matching: comma-separated ranges, media types compared case-insensitively with parameters stripped, text/* and */* admitted, and q=0 a refusal. The MOST SPECIFIC matching range decides (RFC 9110 precedence): exact text/event-stream over text/* over */* — so text/event-stream;q=0, text/*;q=1 is a refusal even though a laxer range matches. A bare substring test gets all of this wrong.\n\nPass the COMBINED value when the request repeats the Accept field (repeated fields are equivalent to their comma-joined concatenation).\n\nArguments\n\naccept_header::AbstractString: The request's (combined) Accept header value\n\nReturns\n\nBool: true when the client can consume an SSE response\n\n\n\n\n\n","title":"ModelContextProtocol.accepts_sse"},{"category":"method","location":"api/#ModelContextProtocol.broadcast_to_sse-Tuple{HttpTransport, String}","page":"API Reference","text":"broadcast_to_sse(transport::HttpTransport, message::String; event::String=\"message\") -> Nothing\n\nBroadcast a message immediately to all SSE streams.\n\nArguments\n\ntransport::HttpTransport: The transport instance\nmessage::String: The message to broadcast\nevent::String: The event type (default: \"message\")\n\nReturns\n\nNothing\n\n\n\n\n\n","title":"ModelContextProtocol.broadcast_to_sse"},{"category":"method","location":"api/#ModelContextProtocol.capture_response_route-Tuple{HttpTransport}","page":"API Reference","text":"capture_response_route(transport::HttpTransport) -> Union{String,Nothing}\n\nDetach the current request's response route so its response can be delivered later from a background task (see deliver_response). Clears current_request_id so the loop's subsequent write_message calls cannot route into this request's channel.\n\n\n\n\n\n","title":"ModelContextProtocol.capture_response_route"},{"category":"method","location":"api/#ModelContextProtocol.close-Tuple{HttpTransport}","page":"API Reference","text":"close(transport::HttpTransport) -> Nothing\n\nStop the HTTP server and close all connections.\n\nArguments\n\ntransport::HttpTransport: The transport instance\n\nReturns\n\nNothing\n\n\n\n\n\n","title":"ModelContextProtocol.close"},{"category":"method","location":"api/#ModelContextProtocol.collect_param_headers-Tuple{Any}","page":"API Reference","text":"collect_param_headers(request) -> Dict{String,Any}\n\nCollect the request's Mcp-Param-* custom headers (SEP-2243 parameter mirroring) into a Dict keyed by LOWERCASED header suffix. Values are OWS-stripped Strings, or :invalid when the header is duplicated or carries unsafe bytes (the same rules mcp_standard_header applies) — the preflight in handle_modern_request rejects :invalid entries with -32020. Always returns a Dict (possibly empty) for HTTP requests: an EMPTY Dict still arms the missing-header-with-body-value check, which nothing (a transport with no headers at all, e.g. stdio) disables.\n\n\n\n\n\n","title":"ModelContextProtocol.collect_param_headers"},{"category":"method","location":"api/#ModelContextProtocol.decode_mcp_header_value-Tuple{AbstractString}","page":"API Reference","text":"decode_mcp_header_value(value::AbstractString) -> Union{String,Nothing}\n\nDecode a standard-header value per SEP-2243: a =?base64?...?= sentinel wraps a Base64-encoded UTF-8 payload (used when the raw value is not header-safe). The Base64 is validated STRICTLY — canonical alphabet, correct padding, length a multiple of four — because Julia's decoder is permissive (it ignores invalid characters and missing padding), and SEP-2243 requires such values to be rejected, not fuzzily matched. The decoded bytes must be valid UTF-8. Returns nothing when the value must cause a -32020 rejection.\n\nArguments\n\nvalue::AbstractString: The header value (already whitespace-stripped)\n\nReturns\n\nUnion{String,Nothing}: The decoded value, or nothing when malformed\n\n\n\n\n\n","title":"ModelContextProtocol.decode_mcp_header_value"},{"category":"method","location":"api/#ModelContextProtocol.deliver_log_notification-Tuple{HttpTransport, Any, String}","page":"API Reference","text":"deliver_log_notification(transport::HttpTransport, route, message::String) -> Bool\n\nPush a request-scoped log notification onto the request's response stream. Unlike deliver_notification, backlog pressure NEVER closes the channel — the request's final response still has to travel it, and load-shedding it away because a stalled client let log records pile up would destroy the response. Past the cap the record is simply dropped (false), sending it to the logger's fallback stream instead.\n\n\n\n\n\n","title":"ModelContextProtocol.deliver_log_notification"},{"category":"method","location":"api/#ModelContextProtocol.deliver_notification-Tuple{HttpTransport, Any, String}","page":"API Reference","text":"deliver_notification(transport::HttpTransport, route, message::String) -> Bool\n\nPush a notification onto the response stream of the request identified by route (captured via capture_response_route) — the delivery path for subscriptions/listen streams. Returns false when that route is gone (client disconnected), so the caller can prune the subscription.\n\nA listen stream lives arbitrarily long, so — unlike ordinary request channels, whose buffering is bounded by the request's own lifetime — its backlog must be capped: a reader that stopped draining would otherwise accumulate every notification forever. At LISTEN_BACKLOG_CAP pending items the stream is load-shed: its channel is closed (which ends the connection handler after it drains what is already buffered) and delivery is reported failed so the caller prunes the subscription.\n\n\n\n\n\n","title":"ModelContextProtocol.deliver_notification"},{"category":"method","location":"api/#ModelContextProtocol.deliver_response-Tuple{HttpTransport, Union{Nothing, String}, String}","page":"API Reference","text":"deliver_response(transport::HttpTransport, route::Union{String,Nothing}, message::String) -> Nothing\n\nDeliver a deferred response to the request identified by route (captured earlier via capture_response_route). The HTTP connection handler is still blocked on the request's response channel, so the POST stays open until this delivers — which is exactly the blocking behavior tasks/result requires. Dropped silently when the client has disconnected (its channel is gone or closed).\n\n\n\n\n\n","title":"ModelContextProtocol.deliver_response"},{"category":"method","location":"api/#ModelContextProtocol.end_response-Tuple{HttpTransport}","page":"API Reference","text":"end_response(transport::HttpTransport) -> Nothing\n\nDeprecated: No longer needed as HTTP transport now sends complete responses. This method exists for backward compatibility but does nothing.\n\nArguments\n\ntransport::HttpTransport: The transport instance\n\nReturns\n\nNothing\n\n\n\n\n\n","title":"ModelContextProtocol.end_response"},{"category":"method","location":"api/#ModelContextProtocol.format_sse_event-Tuple{String}","page":"API Reference","text":"format_sse_event(data::String; event::Union{String,Nothing}=nothing, id::Union{Int64,String,Nothing}=nothing) -> String\n\nFormat a message as a Server-Sent Event.\n\nArguments\n\ndata::String: The data to send\nevent::Union{String,Nothing}: Optional event type\nid::Union{Int64,String,Nothing}: Optional event ID\n\nReturns\n\nString: Formatted SSE event\n\n\n\n\n\n","title":"ModelContextProtocol.format_sse_event"},{"category":"method","location":"api/#ModelContextProtocol.generate_session_id-Tuple{}","page":"API Reference","text":"generate_session_id() -> String\n\nGenerate a cryptographically secure session ID that meets MCP requirements. Must contain only visible ASCII characters (0x21 to 0x7E).\n\nReturns\n\nString: A valid session ID\n\n\n\n\n\n","title":"ModelContextProtocol.generate_session_id"},{"category":"method","location":"api/#ModelContextProtocol.handle_request-Tuple{HttpTransport, HTTP.Streams.Stream}","page":"API Reference","text":"handle_request(transport::HttpTransport, stream::HTTP.Stream)\n\nHandle incoming HTTP requests following the Streamable HTTP specification. Returns a single JSON response per request.\n\nArguments\n\ntransport::HttpTransport: The transport instance\nstream::HTTP.Stream: The HTTP stream\n\nReturns\n\nNothing\n\n\n\n\n\n","title":"ModelContextProtocol.handle_request"},{"category":"method","location":"api/#ModelContextProtocol.handle_sse_stream-Tuple{HttpTransport, HTTP.Streams.Stream, String}","page":"API Reference","text":"handle_sse_stream(transport::HttpTransport, stream::HTTP.Stream, stream_id::String)\n\nHandle a Server-Sent Events connection for notifications and streaming responses.\n\nArguments\n\ntransport::HttpTransport: The transport instance\nstream::HTTP.Stream: The HTTP stream\nstream_id::String: Unique identifier for this SSE stream\n\nReturns\n\nNothing\n\n\n\n\n\n","title":"ModelContextProtocol.handle_sse_stream"},{"category":"method","location":"api/#ModelContextProtocol.is_connected-Tuple{HttpTransport}","page":"API Reference","text":"is_connected(transport::HttpTransport) -> Bool\n\nCheck if the HTTP server is running and accepting connections.\n\nArguments\n\ntransport::HttpTransport: The transport instance\n\nReturns\n\nBool: true if connected, false otherwise\n\n\n\n\n\n","title":"ModelContextProtocol.is_connected"},{"category":"method","location":"api/#ModelContextProtocol.is_loopback_host-Tuple{AbstractString}","page":"API Reference","text":"is_loopback_host(host::AbstractString) -> Bool\n\nDetermine whether a hostname refers to the local loopback: localhost (optionally with a trailing dot), any 127.0.0.0/8 IPv4 address, ::1, or an IPv4-mapped IPv6 loopback (::ffff:127.x.y.z). String-list matching is not enough here — binding or addressing via 127.0.0.2 is just as local as 127.0.0.1.\n\nArguments\n\nhost::AbstractString: Hostname, IP literal, or bracketed IPv6 literal\n\nReturns\n\nBool: true if the host is loopback\n\n\n\n\n\n","title":"ModelContextProtocol.is_loopback_host"},{"category":"method","location":"api/#ModelContextProtocol.is_valid_session_id-Tuple{String}","page":"API Reference","text":"is_valid_session_id(session_id::String) -> Bool\n\nValidate that a session ID contains only visible ASCII characters (0x21 to 0x7E).\n\nArguments\n\nsession_id::String: The session ID to validate\n\nReturns\n\nBool: true if valid, false otherwise\n\n\n\n\n\n","title":"ModelContextProtocol.is_valid_session_id"},{"category":"method","location":"api/#ModelContextProtocol.mcp_standard_header-Tuple{Any, String}","page":"API Reference","text":"mcp_standard_header(request, name::String) -> Union{String,Nothing,Symbol}\n\nFetch a SEP-2243 standard header strictly: header names are matched case-insensitively across ALL header instances; duplicates (a smuggling vector — a gateway may honor a different copy than the backend) and values containing non-visible-ASCII bytes (NUL/control/8-bit, which HTTP.jl's parser admits but intermediaries interpret divergently) are rejected. Returns the single whitespace-stripped value, nothing when absent, or :invalid when the header must cause a -32020 rejection.\n\nArguments\n\nrequest: The HTTP request\nname::String: The header name\n\nReturns\n\nUnion{String,Nothing,Symbol}: Value, nothing (absent), or :invalid\n\n\n\n\n\n","title":"ModelContextProtocol.mcp_standard_header"},{"category":"method","location":"api/#ModelContextProtocol.modern_error_http_status-Tuple{String}","page":"API Reference","text":"modern_error_http_status(payload::String) -> Int\n\nMap a modern-era JSON-RPC response to its HTTP status per the 2026-07-28 transport: -32601 (method not found) → 404; the protocol validation errors — -32020 HeaderMismatch, -32021 MissingRequiredClientCapability, -32022 UnsupportedProtocolVersion — → 400; everything else (results and application-level errors) → 200.\n\nArguments\n\npayload::String: The serialized JSON-RPC response\n\nReturns\n\nInt: The HTTP status code to use\n\n\n\n\n\n","title":"ModelContextProtocol.modern_error_http_status"},{"category":"method","location":"api/#ModelContextProtocol.modern_header_violation-Tuple{Any, Any, String}","page":"API Reference","text":"modern_header_violation(request, msg, body_version) -> Union{String,Nothing}\n\nValidate the SEP-2243 standard request headers of a modern-era POST against its body: MCP-Protocol-Version and Mcp-Method are required on every request and must match the body's _meta protocol version and method; Mcp-Name is required on tools/call/prompts/get (mirroring params.name) and resources/read (mirroring params.uri), with the Base64 sentinel decoded before comparison. Header names are case-insensitive, duplicates and unsafe bytes are rejected (mcp_standard_header), values are compared case-sensitively after whitespace stripping. (A request whose headers claim a modern era while the body carries no modern _meta never reaches this function — the caller rejects it as a missing required field, -32602.)\n\nArguments\n\nrequest: The HTTP request (for header access)\nmsg: The parsed JSON body (a JSON3.Object)\nbody_version: The body's _meta protocol version\n\nReturns\n\nUnion{String,Nothing}: A violation description for a -32020 HeaderMismatch, or nothing when the headers validate\n\n\n\n\n\n","title":"ModelContextProtocol.modern_header_violation"},{"category":"method","location":"api/#ModelContextProtocol.notification_route-Tuple{HttpTransport}","page":"API Reference","text":"notification_route(transport::HttpTransport) -> Union{String,Nothing}\n\nReturn the HTTP request ID of the message just read from the queue, used by the server loop as the request-scoped notification route (see the base notification_route docstring).\n\n\n\n\n\n","title":"ModelContextProtocol.notification_route"},{"category":"method","location":"api/#ModelContextProtocol.parse_host_header-Tuple{AbstractString}","page":"API Reference","text":"parse_host_header(hostport::AbstractString) -> Union{String,Nothing}\n\nParse a Host-header style host[:port] value strictly, returning the lowercased hostname or nothing when the value is malformed (userinfo, multiple colons on a non-bracketed host, junk after a bracketed IPv6 literal, non-numeric port). Malformed values must be REJECTED by the DNS-rebinding guard, not leniently truncated into something that looks local ([::1]evil.example must not read as [::1]).\n\nArguments\n\nhostport::AbstractString: A Host-header style value\n\nReturns\n\nUnion{String,Nothing}: The lowercased hostname (brackets kept for IPv6 literals), or nothing when malformed\n\n\n\n\n\n","title":"ModelContextProtocol.parse_host_header"},{"category":"method","location":"api/#ModelContextProtocol.pending_auth_context-Tuple{HttpTransport}","page":"API Reference","text":"pending_auth_context(transport::HttpTransport) -> Union{AuthenticatedUser,Nothing}\n\nReturn the authenticated user of the message most recently read from the queue (set in read_message, consumed immediately by the single server loop). This is how the per-request identity reaches RequestContext.authenticated_user; handlers should read it from the request context, not from the transport.\n\n\n\n\n\n","title":"ModelContextProtocol.pending_auth_context"},{"category":"method","location":"api/#ModelContextProtocol.pending_param_headers-Tuple{HttpTransport}","page":"API Reference","text":"pending_param_headers(transport::HttpTransport) -> Union{Nothing,Dict{String,Any}}\n\nReturn the Mcp-Param-* headers of the message most recently read from the queue (set in read_message, consumed immediately by the single server loop) — the SEP-2243 parameter-mirroring context validated by the handle_modern_request preflight.\n\n\n\n\n\n","title":"ModelContextProtocol.pending_param_headers"},{"category":"method","location":"api/#ModelContextProtocol.read_message-Tuple{HttpTransport}","page":"API Reference","text":"read_message(transport::HttpTransport) -> Union{String,Nothing}\n\nRead a message from the request queue. Waits a BOUNDED interval and returns nothing when no request arrives, so the server loop regularly re-checks its run condition: stop! must be able to end the loop while the transport is still open (graceful subscriptions/listen closure delivers on the open transport, then the transport is closed) — a blocking take! would pin the loop until the next request.\n\nArguments\n\ntransport::HttpTransport: The transport instance\n\nReturns\n\nUnion{String,Nothing}: The message string, or nothing when no request arrived (or the transport disconnected)\n\n\n\n\n\n","title":"ModelContextProtocol.read_message"},{"category":"method","location":"api/#ModelContextProtocol.rebinding_violation-Tuple{AbstractString, AbstractString, Vector{String}, Vector{String}}","page":"API Reference","text":"rebinding_violation(host_header, origin_header, allowed_hosts, allowed_origins) -> Union{String,Nothing}\n\nCheck Host and Origin headers against DNS-rebinding attacks on a loopback-bound server (GHSA-w48q-cv73-mx4w class): a malicious website resolves its own domain to 127.0.0.1 and drives the local server from the victim's browser. A legitimate local client sends a loopback Host; the attack necessarily carries the attacker's hostname. Malformed Host values are violations (see parse_host_header). allowed_hosts applies to the Host header only; Origin is admitted by loopback hostname or an exact allowed_origins match — a proxy hostname in allowed_hosts deliberately does NOT admit browser origins on that host.\n\nArguments\n\nhost_header: The request's Host header value (\"\" when absent)\norigin_header: The request's Origin header value (\"\" when absent)\nallowed_hosts: Extra hostnames accepted in Host (e.g. a reverse-proxy domain)\nallowed_origins: Full origins accepted in Origin (exact match, existing semantics)\n\nReturns\n\nUnion{String,Nothing}: A human-readable violation description, or nothing if the request is acceptable\n\n\n\n\n\n","title":"ModelContextProtocol.rebinding_violation"},{"category":"method","location":"api/#ModelContextProtocol.route_alive-Tuple{HttpTransport, Any}","page":"API Reference","text":"route_alive(transport::HttpTransport, route) -> Bool\n\nWhether a captured response route still has a live consumer: its channel is still registered and open. The connection handler unregisters and closes the channel when its client disconnects (or its stream is load-shed), so a dead route shows up here without anything being written to it.\n\n\n\n\n\n","title":"ModelContextProtocol.route_alive"},{"category":"method","location":"api/#ModelContextProtocol.send_notification-Tuple{HttpTransport, String}","page":"API Reference","text":"send_notification(transport::HttpTransport, notification::String) -> Nothing\n\nSend a notification. A notification emitted while the server loop is synchronously handling a request (progress, log messages) is request-scoped: it is routed onto that request's response channel and delivered on the POST's SSE response stream, per Streamable HTTP. Anything else — notifications from background tasks (MCP Tasks status updates) or emitted outside request handling — goes to the standalone GET SSE notification stream.\n\nThe request route is read from the server loop's task-local storage (set in run_server_loop), so background tasks — which never inherit it — can't misroute their notifications into whatever request the loop happens to be processing.\n\nArguments\n\ntransport::HttpTransport: The transport instance\nnotification::String: The notification message to send\n\nReturns\n\nNothing\n\n\n\n\n\n","title":"ModelContextProtocol.send_notification"},{"category":"method","location":"api/#ModelContextProtocol.set_negotiated_version!-Tuple{HttpTransport, String}","page":"API Reference","text":"set_negotiated_version!(transport::HttpTransport, version::String) -> Nothing\n\nUpdate the version advertised in MCP-Protocol-Version response headers (and accepted in request headers) to the version negotiated during initialize, so per-request headers echo what the initialize response returned.\n\n\n\n\n\n","title":"ModelContextProtocol.set_negotiated_version!"},{"category":"method","location":"api/#ModelContextProtocol.split_outside_quotes-Tuple{AbstractString, Char}","page":"API Reference","text":"split_outside_quotes(s::AbstractString, delim::Char) -> Vector{String}\n\nSplit s on delim occurrences that lie OUTSIDE double-quoted strings, honoring backslash escapes inside quotes (RFC 9110 quoted-string). Header parameters may legally quote separator characters (profile=\"a,b\"), so a naive split corrupts the ranges around them.\n\nArguments\n\ns::AbstractString: The header text to split\ndelim::Char: The separator character\n\nReturns\n\nVector{String}: The parts (quotes preserved verbatim)\n\n\n\n\n\n","title":"ModelContextProtocol.split_outside_quotes"},{"category":"method","location":"api/#ModelContextProtocol.write_message-Tuple{HttpTransport, String}","page":"API Reference","text":"write_message(transport::HttpTransport, message::String) -> Nothing\n\nWrite a message to the current request's response channel. The request handler will send this as the HTTP response.\n\nArguments\n\ntransport::HttpTransport: The transport instance\nmessage::String: The message to send\n\nReturns\n\nNothing\n\n\n\n\n\n","title":"ModelContextProtocol.write_message"},{"category":"method","location":"api/#ModelContextProtocol.check_allowlist-Tuple{AuthenticatedUser, Set{String}}","page":"API Reference","text":"check_allowlist(user::AuthenticatedUser, allowlist::Set{String};\n                case_insensitive::Bool=true) -> Bool\n\nCheck whether user is in the allowlist, by username then by subject.\n\nUsername matching honors case_insensitive (default true) because identity providers vary or normalize username case (e.g. Keycloak lowercases GitHub logins). The opaque OAuth subject is ALWAYS matched exactly — case-folding a stable identifier could collide two distinct principals.\n\nArguments\n\nuser::AuthenticatedUser: The authenticated user\nallowlist::Set{String}: Set of allowed usernames or subjects\ncase_insensitive::Bool=true: Match usernames case-insensitively\n\nReturns\n\ntrue if user is allowed.\n\n\n\n\n\n","title":"ModelContextProtocol.check_allowlist"},{"category":"method","location":"api/#ModelContextProtocol.add_token!-Tuple{SimpleTokenValidator, String, AuthenticatedUser}","page":"API Reference","text":"add_token!(validator::SimpleTokenValidator, token::String, user::AuthenticatedUser)\n\nAdd a valid token to the validator.\n\n\n\n\n\n","title":"ModelContextProtocol.add_token!"},{"category":"method","location":"api/#ModelContextProtocol.decode_jwt_header-Tuple{String}","page":"API Reference","text":"decode_jwt_header(token::String) -> Union{Dict{String,Any},Nothing}\n\nDecode the JWT header (first segment) without verification. Returns nothing if the token format is invalid.\n\n\n\n\n\n","title":"ModelContextProtocol.decode_jwt_header"},{"category":"method","location":"api/#ModelContextProtocol.decode_jwt_payload-Tuple{String}","page":"API Reference","text":"decode_jwt_payload(token::String) -> Union{Dict{String,Any},Nothing}\n\nDecode JWT payload without verification (for claim inspection). Returns nothing if token format is invalid.\n\n\n\n\n\n","title":"ModelContextProtocol.decode_jwt_payload"},{"category":"method","location":"api/#ModelContextProtocol.fetch_jwks_http_body-Tuple{String}","page":"API Reference","text":"fetch_jwks_http_body(url::String) -> Union{String,Nothing}\n\nGET a JWKS over HTTP(S) with bounded connect/read timeouts, streaming the body and aborting once MAX_JWKS_BYTES is exceeded. Returns the body string on a 200, or nothing on non-200, oversize, or transport error.\n\n\n\n\n\n","title":"ModelContextProtocol.fetch_jwks_http_body"},{"category":"method","location":"api/#ModelContextProtocol.fetch_jwks_keys-Tuple{String}","page":"API Reference","text":"fetch_jwks_keys(url::String) -> Union{Vector{Dict{String,Any}},Nothing}\n\nFetch and parse the keys array of a JWKS document from an http(s):// or file:// URL. Network fetches use bounded connect/read timeouts and reject responses larger than MAX_JWKS_BYTES (read is aborted once the cap is exceeded, so an oversized or unbounded body cannot exhaust memory). Returns nothing on any failure (fetch, oversize, parse, or missing keys field) — callers fail closed.\n\n\n\n\n\n","title":"ModelContextProtocol.fetch_jwks_keys"},{"category":"method","location":"api/#ModelContextProtocol.lookup_jwks_key!-Tuple{JWKSValidator, String}","page":"API Reference","text":"lookup_jwks_key!(validator::JWKSValidator, kid::String) -> Union{JWTs.JWK,Nothing}\n\nLook up a verification key by kid, re-fetching the JWKS on a miss (key rotation) subject to the refresh rate limit. The network fetch happens outside the validator lock; the key-set swap and re-lookup happen under it.\n\n\n\n\n\n","title":"ModelContextProtocol.lookup_jwks_key!"},{"category":"method","location":"api/#ModelContextProtocol.validate_jwt_claims-Tuple{Dict{String, Any}, OAuthConfig, Int64}","page":"API Reference","text":"validate_jwt_claims(claims::Dict{String,Any}, config::OAuthConfig, clock_skew::Int) -> AuthResult\n\nValidate JWT claims (iss, aud, exp, nbf).\n\n\n\n\n\n","title":"ModelContextProtocol.validate_jwt_claims"},{"category":"type","location":"api/#ModelContextProtocol.RequestAuthContext","page":"API Reference","text":"RequestAuthContext\n\nAuthentication context attached to authenticated requests.\n\n\n\n\n\n","title":"ModelContextProtocol.RequestAuthContext"},{"category":"method","location":"api/#ModelContextProtocol.auth_error_response-Tuple{Symbol, String}","page":"API Reference","text":"auth_error_response(error_code::Symbol, message::String) -> Tuple{Int,String,Dict{String,String}}\n\nGenerate HTTP response for authentication errors.\n\nReturns\n\nTuple of (status_code, body, headers)\n\n\n\n\n\n","title":"ModelContextProtocol.auth_error_response"},{"category":"constant","location":"api/#ModelContextProtocol.GitHubAuthorizationServer","page":"API Reference","text":"GitHubAuthorizationServer\n\nPre-configured authorization server URL for GitHub OAuth.\n\n\n\n\n\n","title":"ModelContextProtocol.GitHubAuthorizationServer"},{"category":"constant","location":"api/#ModelContextProtocol.WELL_KNOWN_PATH","page":"API Reference","text":"WELL_KNOWN_PATH\n\nStandard path for Protected Resource Metadata.\n\n\n\n\n\n","title":"ModelContextProtocol.WELL_KNOWN_PATH"},{"category":"method","location":"api/#ModelContextProtocol.handle_well_known_request-Tuple{ProtectedResourceMetadata}","page":"API Reference","text":"handle_well_known_request(metadata::ProtectedResourceMetadata) -> Tuple{Int,String,Dict{String,String}}\n\nHandle a request to the .well-known/oauth-protected-resource endpoint.\n\nReturns\n\nTuple of (status_code, body, headers).\n\n\n\n\n\n","title":"ModelContextProtocol.handle_well_known_request"},{"category":"method","location":"api/#ModelContextProtocol.metadata_to_json-Tuple{ProtectedResourceMetadata}","page":"API Reference","text":"metadata_to_json(metadata::ProtectedResourceMetadata) -> String\n\nSerialize Protected Resource Metadata to JSON for HTTP response.\n\n\n\n\n\n","title":"ModelContextProtocol.metadata_to_json"},{"category":"constant","location":"api/#ModelContextProtocol.GITHUB_API_URL","page":"API Reference","text":"GITHUB_API_URL\n\nBase URL for GitHub API.\n\n\n\n\n\n","title":"ModelContextProtocol.GITHUB_API_URL"},{"category":"type","location":"api/#ModelContextProtocol.GitHubOAuthValidatorWithOrg","page":"API Reference","text":"GitHubOAuthValidatorWithOrg <: TokenValidator\n\nWrapper validator that adds organization membership checking.\n\nFields\n\nbase_validator::GitHubOAuthValidator: The underlying GitHub token validator\nrequired_org::Union{String,Nothing}: Required organization membership\n\n\n\n\n\n","title":"ModelContextProtocol.GitHubOAuthValidatorWithOrg"},{"category":"method","location":"api/#ModelContextProtocol.check_github_org_membership-Tuple{String, String}","page":"API Reference","text":"check_github_org_membership(token::String, org::String) -> Bool\n\nCheck if the authenticated user is a member of the specified GitHub organization.\n\n\n\n\n\n","title":"ModelContextProtocol.check_github_org_membership"},{"category":"method","location":"api/#ModelContextProtocol.fetch_github_user-Tuple{String}","page":"API Reference","text":"fetch_github_user(token::String) -> Union{Dict{String,Any},Nothing}\n\nFetch user information from GitHub API using an access token. Returns nothing if the token is invalid or the request fails.\n\n\n\n\n\n","title":"ModelContextProtocol.fetch_github_user"},{"category":"constant","location":"api/#ModelContextProtocol.MCP_LOG_LEVELS","page":"API Reference","text":"MCP_LOG_LEVELS\n\nThe log levels defined by the MCP spec (RFC 5424 severities), lowest to highest.\n\n\n\n\n\n","title":"ModelContextProtocol.MCP_LOG_LEVELS"},{"category":"type","location":"api/#ModelContextProtocol.MCPLogger","page":"API Reference","text":"MCPLogger(stream::IO=stderr, level::LogLevel=Info) -> MCPLogger\n\nCreate a new MCPLogger instance with specified stream and level.\n\nArguments\n\nstream::IO=stderr: The output stream where log messages will be written\nlevel::LogLevel=Info: The minimum logging level to display\n\nReturns\n\nMCPLogger: A new logger instance\n\n\n\n\n\n","title":"ModelContextProtocol.MCPLogger"},{"category":"type","location":"api/#ModelContextProtocol.MCPLogger-2","page":"API Reference","text":"MCPLogger <: AbstractLogger\n\nDefine a custom logger for MCP server that formats messages according to protocol requirements.\n\nOnce a LEGACY session is initialized, log records are delivered to the client as MCP notifications/message notifications via the server's transport (send_notification): stdio writes them to stdout alongside responses; Streamable HTTP delivers them on the originating request's SSE response stream (or the standalone notification stream when no request is being handled). Before initialization — or if transport delivery fails — records fall back to stream as JSON lines. Modern-era (2026-07-28+) requests never deliver through these ambient gates: a request that opted in via io.modelcontextprotocol/logLevel is served under a request-scoped ModernRequestLogger wrapping this logger, and every other modern request keeps the loop-armed suppression flag, so its records reach only the fallback stream.\n\nFields\n\nstream::IO: Fallback output stream for log messages (used before the client initializes and when transport delivery is unavailable)\nmin_level::LogLevel: Minimum logging level to display (mutable so logging/setLevel can adjust it on the installed logger)\nmessage_limits::Dict{Any,Int}: Message limit settings for rate limiting\ntransport::Any: The server transport notifications are delivered over (Union{Nothing,Transport}; typed Any to avoid include-order coupling)\ntransport_active::Base.RefValue{Bool}: Gate flipped on client initialization — notifications MUST NOT be emitted to a client that has not initialized\n\n\n\n\n\n","title":"ModelContextProtocol.MCPLogger"},{"category":"type","location":"api/#ModelContextProtocol.ModernRequestLogger","page":"API Reference","text":"ModernRequestLogger(inner::MCPLogger, transport, route,\n                    level::Union{String,Nothing}) <: AbstractLogger\n\nThe logging scope of ONE modern-era (2026-07-28+) request. handle_modern_request installs it around the serve path with with_logger, which is what makes every guarantee task-inheritance-proof: child tasks a handler spawns inherit the logstate (unlike task-local storage), so their records land here too, subject to the same rules as the serving task's.\n\nSemantics per SEP-2575: with a requested level, records at or above it (RFC-5424) are delivered as notifications/message on the ORIGINATING request's response stream — the route captured at construction, pushed via deliver_log_notification (which never closes the request's response channel under backlog pressure) on the serving transport — never on the standalone GET stream, a subscriptions/listen stream, or another server's transport. A level is only ever set when the arming site verified the current logger serves this server's transport; an identity-MISMATCHED MCPLogger (another server's) still gets a scope, but a wire-silent one (level = nothing), so child tasks cannot deliver through the foreign transport either. Without a level (no opt-in, no logging capability, or that mismatch), nothing is ever delivered. Delivery ends when closed is set (the final response is on its way) or the response channel is gone — late records are dropped, not diverted. Undelivered records fall back to the operator's inner.stream under inner's own min_level, so a debug opt-in below the operator's level never spams stderr.\n\nFields\n\ninner::MCPLogger: The server's installed logger (fallback stream + operator level)\ntransport::Any: The serving server's transport\nroute::Any: The request's captured notification route\nlevel::Union{String,Nothing}: The validated requested MCP level, or nothing for a wire-silent scope\nclosed::Base.RefValue{Bool}: Set once the request's response is under way\nlk::ReentrantLock: Serializes delivery against closure — close_scope! takes it to flip closed, so a child task's in-flight delivery either completes before the final response or observes closed and drops; the check-then-deliver window is never open across teardown\n\n\n\n\n\n","title":"ModelContextProtocol.ModernRequestLogger"},{"category":"method","location":"api/#Logging.handle_message-Tuple{ModelContextProtocol.MCPLogger, Vararg{Any, 7}}","page":"API Reference","text":"Logging.handle_message(logger::MCPLogger, level, message, _module, group, id, filepath, line; kwargs...) -> Nothing\n\nFormat and output log messages according to the MCP protocol format.\n\nArguments\n\nlogger::MCPLogger: The MCP logger instance\nlevel: The log level of the message\nmessage: The log message content\n_module: The module where the log was generated\ngroup: The log group\nid: The log message ID\nfilepath: The source file path\nline: The source line number\nkwargs...: Additional contextual information to include in the log\n\nReturns\n\nNothing: Function writes to the logger stream but doesn't return a value\n\n\n\n\n\n","title":"Logging.handle_message"},{"category":"method","location":"api/#ModelContextProtocol.close_scope!-Tuple{ModelContextProtocol.ModernRequestLogger}","page":"API Reference","text":"close_scope!(logger::ModernRequestLogger) -> Nothing\n\nEnd a request's logging scope: after this, no record is delivered on the request's response stream. Taking the delivery lock makes closure wait out any in-flight delivery, so a notification can never trail the final response.\n\nArguments\n\nlogger::ModernRequestLogger: The scope to close\n\nReturns\n\nNothing\n\n\n\n\n\n","title":"ModelContextProtocol.close_scope!"},{"category":"method","location":"api/#ModelContextProtocol.format_log_notification-NTuple{5, Any}","page":"API Reference","text":"format_log_notification(level, message, _module, filepath, line; kwargs...) -> (String, String)\n\nBuild the wire form of a log record as an MCP notifications/message.\n\nArguments\n\nlevel: The Julia LogLevel of the record\nmessage: The log message content\n_module: The module where the log was generated\nfilepath: The source file path\nline: The source line number\nkwargs...: Additional context, stringified into the metadata\n\nReturns\n\nTuple{String,String}: The MCP level name and the serialized JSON-RPC notification\n\n\n\n\n\n","title":"ModelContextProtocol.format_log_notification"},{"category":"function","location":"api/#ModelContextProtocol.init_logging","page":"API Reference","text":"init_logging(level::LogLevel=Info) -> Nothing\n\nInitialize logging for the MCP server with a custom MCP-formatted logger.\n\nArguments\n\nlevel::LogLevel=Info: The minimum logging level to display\n\nReturns\n\nNothing: Function sets the global logger but doesn't return a value\n\n\n\n\n\n","title":"ModelContextProtocol.init_logging"},{"category":"method","location":"api/#ModelContextProtocol.mcp_level_severity-Tuple{String}","page":"API Reference","text":"mcp_level_severity(level::String) -> Int\n\nThe RFC-5424 severity rank of an MCP log level (1 = debug, lowest … 8 = emergency, highest), for at-or-above comparisons.\n\nArguments\n\nlevel::String: One of MCP_LOG_LEVELS\n\nReturns\n\nInt: The 1-based rank within MCP_LOG_LEVELS (unrecognized levels rank highest, so they never widen delivery)\n\n\n\n\n\n","title":"ModelContextProtocol.mcp_level_severity"},{"category":"method","location":"api/#ModelContextProtocol.mcp_level_to_julia-Tuple{String}","page":"API Reference","text":"mcp_level_to_julia(level::String) -> LogLevel\n\nMap an MCP/RFC-5424 log level string to the closest Julia LogLevel.\n\nArguments\n\nlevel::String: One of MCP_LOG_LEVELS\n\nReturns\n\nLogLevel: Debug, Info, Warn, or Error (the four Julia standard levels)\n\n\n\n\n\n","title":"ModelContextProtocol.mcp_level_to_julia"},{"category":"method","location":"api/#StructTypes.names-Tuple{Type{CallToolResult}}","page":"API Reference","text":"StructTypes.names(::Type{CallToolResult})\n\nMap Julia field names to their MCP wire keys: is_error → isError (the spec key — clients rely on it to detect tool errors) and structured_content → structuredContent.\n\n\n\n\n\n","title":"StructTypes.names"},{"category":"method","location":"api/#StructTypes.omitempties-Tuple{Type{CallToolResult}}","page":"API Reference","text":"StructTypes.omitempties(::Type{CallToolResult}) -> Tuple{Symbol,Symbol}\n\nOmit structured_content and _meta from the response when they are nothing, so tools that don't use them emit no structuredContent/_meta keys.\n\n\n\n\n\n","title":"StructTypes.omitempties"},{"category":"method","location":"api/#StructTypes.omitempties-Tuple{Type{ModelContextProtocol.ClientCapabilities}}","page":"API Reference","text":"StructTypes.omitempties(::Type{ClientCapabilities}) -> Tuple{Symbol,Symbol,Symbol}\n\nSpecify which fields should be omitted from JSON serialization when they are empty or null.\n\nArguments\n\n::Type{ClientCapabilities}: The ClientCapabilities type\n\nReturns\n\nTuple{Symbol,Symbol,Symbol}: Fields to omit when empty\n\n\n\n\n\n","title":"StructTypes.omitempties"},{"category":"method","location":"api/#StructTypes.omitempties-Tuple{Type{ModelContextProtocol.ListPromptsResult}}","page":"API Reference","text":"StructTypes.omitempties(::Type{ListPromptsResult}) -> Tuple{Symbol}\n\nSpecify which fields should be omitted from JSON serialization when they are empty or null.\n\nArguments\n\n::Type{ListPromptsResult}: The ListPromptsResult type\n\nReturns\n\nTuple{Symbol}: Fields to omit when empty\n\n\n\n\n\n","title":"StructTypes.omitempties"},{"category":"section","location":"modern/#The-Modern-Era-(2026-07-28)","page":"The Modern Era (2026-07-28)","text":"ModelContextProtocol.jl is a dual-era server. Alongside the classic initialize-handshake sessions (protocol versions 2024-11-05 through 2025-11-25), it serves the 2026-07-28 specification's stateless modern era: any request whose params _meta carries io.modelcontextprotocol/protocolVersion is validated and answered as a self-contained exchange — no handshake, no session state — on the same endpoint. Both eras share your tools, resources, prompts, and templates; you register components once and clients of either generation are served.\n\nNothing on the 2026-07-28 spec's server side is unimplemented: the sections below tour the modern surface and the handler APIs that drive it.","title":"The Modern Era (2026-07-28)"},{"category":"section","location":"modern/#Stateless-requests-and-server/discover","page":"The Modern Era (2026-07-28)","text":"A modern request carries the protocol version and the client's capabilities in _meta, plus optional clientInfo and the per-request logLevel opt-in described below:\n\n{\n  \"jsonrpc\": \"2.0\", \"id\": 1, \"method\": \"tools/list\",\n  \"params\": {\n    \"_meta\": {\n      \"io.modelcontextprotocol/protocolVersion\": \"2026-07-28\",\n      \"io.modelcontextprotocol/clientCapabilities\": {}\n    }\n  }\n}\n\nValidation is per-request: a missing required field is -32602, an unsupported version is -32022 with the supported list, and methods removed from the modern era (ping, logging/setLevel, resources/subscribe, resources/unsubscribe, tasks/result, tasks/list) are -32601. server/discover replaces the handshake for capability discovery — it returns supported versions across both eras, the modern capability set (including declared extensions), and the CacheableResult fields (ttlMs, cacheScope) the spec requires. Every modern result carries a resultType and identifies the server via io.modelcontextprotocol/serverInfo in its _meta.\n\nOver Streamable HTTP, modern requests are validated against the SEP-2243 standard headers — MCP-Protocol-Version and Mcp-Method on every request, Mcp-Name mirroring the body on tools/call, prompts/get, resources/read, and the tasks/* methods — with -32020 (HTTP 400) on any mismatch, 404 for unknown methods, and sessions ignored (never minted).","title":"Stateless requests and server/discover"},{"category":"section","location":"modern/#Multi-round-trip-requests-(MRTR)","page":"The Modern Era (2026-07-28)","text":"The modern era has no server-initiated requests. A tool handler that needs client input (elicitation, sampling, roots) returns an InputRequired value; the server answers with resultType: \"input_required\", the requests to fulfill, and an integrity-protected requestState; the client retries tools/call with its inputResponses and the echoed state, and the handler simply runs again:\n\ntool = MCPTool(\n    name = \"greeter\",\n    description = \"Greets a confirmed user\",\n    parameters = ToolParameter[],\n    handler = (args, ctx) -> begin\n        resp = get(input_responses(ctx), \"who\", nothing)\n        resp === nothing && return InputRequired(\n            Dict(\"who\" => elicit_request(\"Who should I greet?\")))\n        TextContent(text = \"Hello!\")\n    end)\n\nBuild requests with elicit_request, sampling_request, or roots_request; read the retry's responses with input_responses and carried-over handler state with input_state. A request whose input requests need capabilities the client did not declare is rejected with -32021 naming them. The requestState token is HMAC-bound to the principal, a TTL, and a canonical digest of the original params — pass mrtr_state_key to mcp_server when running shared-key replicas.","title":"Multi round-trip requests (MRTR)"},{"category":"section","location":"modern/#The-tasks-extension-(io.modelcontextprotocol/tasks)","page":"The Modern Era (2026-07-28)","text":"Long-running work moves off the serial loop through the SEP-2663 tasks extension. Creation is server-directed: the client declares the extension in its per-request capabilities, and the handler decides — task_detach hands the running call off to a background task and immediately answers with a flat CreateTaskResult; the handler's eventual return value (or thrown error) becomes the task's terminal state, observable via tasks/get (results inlined; there is no tasks/result):\n\nslow = MCPTool(\n    name = \"slow_job\",\n    description = \"Expensive computation\",\n    parameters = ToolParameter[],\n    task_support = :optional,   # :required rejects non-declaring clients (-32021)\n    handler = (args, ctx) -> begin\n        task_detach(ctx)        # false (and the call stays synchronous) when\n                                # the client did not declare the extension\n        for step in 1:100\n            task_cancelled(ctx) && return TextContent(text = \"stopped\")\n            # ... work ...\n        end\n        TextContent(text = \"done\")\n    end)\n\ntasks/cancel is an idempotent empty ack (cooperative — poll task_cancelled); a tool-level isError result completes the task (failed is reserved for protocol errors); extension tasks and legacy SEP-1686 tasks share one era-tagged store, and neither era can see the other's records.","title":"The tasks extension (io.modelcontextprotocol/tasks)"},{"category":"section","location":"modern/#Mid-task-input","page":"The Modern Era (2026-07-28)","text":"After detaching, a handler can ask the client for input during execution with task_await_input: the task parks as input_required, tasks/get surfaces the outstanding requests under server-minted keys, and the client answers through tasks/update (partial fulfillment is accepted):\n\nconfirm = MCPTool(\n    name = \"confirm_delete\",\n    description = \"Delete with confirmation\",\n    parameters = [ToolParameter(name = \"filename\", description = \"Target\",\n                                type = \"string\", required = true)],\n    task_support = :required,   # mid-task input needs the detached task, so\n                                # non-declaring clients are rejected up front\n    handler = (args, ctx) -> begin\n        task_detach(ctx)\n        resp = task_await_input(ctx, elicit_request(\"Really delete $(args[\"filename\"])?\"))\n        TextContent(text = \"done\")\n    end)\n\nRequest params are frozen at registration (plain JSON data only), a task whose ttl elapses while parked is failed and its waiters unwound, and cancellation unwinds the handler with TaskCancelledException (distinguish expiry from a client cancel via task_cancelled(ctx)). Returning InputRequired before detaching runs the normal MRTR round, so a tool can gather input synchronously and then escalate — the SEP-2663 composition rule.","title":"Mid-task input"},{"category":"section","location":"modern/#Status-notifications","page":"The Modern Era (2026-07-28)","text":"Clients subscribe to task status pushes through subscriptions/listen with a taskIds filter: every transition — parking on input, resuming, completing, failing, cancelling, expiring — delivers the task's complete state to subscribed streams, tagged with the subscription id. The acknowledgment echoes only ids the requestor could tasks/get, and deliveries re-check authorization per push.","title":"Status notifications"},{"category":"section","location":"modern/#Subscriptions","page":"The Modern Era (2026-07-28)","text":"subscriptions/listen replaces the legacy GET stream and resources/subscribe: a long-lived request whose response stream carries only the notification types the client opted into (toolsListChanged, promptsListChanged, resourcesListChanged, resourceSubscriptions URIs, and taskIds). Announce changes server-side with notify_list_changed and notify_resource_updated.","title":"Subscriptions"},{"category":"section","location":"modern/#Argument-completion","page":"The Modern Era (2026-07-28)","text":"completion/complete serves suggestions for prompt arguments and resource-template variables in both eras. Configure sources with the completions field on MCPPrompt / ResourceTemplate — a Vector{String} (served filtered by prefix) or a function receiving the partial value and the request context's resolved arguments:\n\nprompt = MCPPrompt(\n    name = \"city_report\",\n    arguments = [PromptArgument(name = \"city\", required = true)],\n    messages = [PromptMessage(content = TextContent(text = \"Report for {city}\"))],\n    completions = Dict{String,Any}(\"city\" => [\"paris\", \"london\", \"tokyo\"]))","title":"Argument completion"},{"category":"section","location":"modern/#Header-parameter-mirroring-(x-mcp-header)","page":"The Modern Era (2026-07-28)","text":"For transport-level routing, a tool parameter may declare a header-mirroring suffix — ToolParameter(name = \"route\", description = \"Routing key\", type = \"string\", header = \"Routing-Key\"), or x-mcp-header in a raw input_schema — and modern HTTP clients mirror the argument as an Mcp-Param-<suffix> request header. The server validates every mirror against the body (strict Base64 sentinel handling, exact numeric comparison) and rejects violations with -32020/HTTP 400. Registration refuses invalid annotations: suffixes must be HTTP tokens, unique per tool, on string/integer/boolean properties reachable through plain properties chains.","title":"Header parameter mirroring (x-mcp-header)"},{"category":"section","location":"modern/#Per-request-logging","page":"The Modern Era (2026-07-28)","text":"logging/setLevel does not exist in the modern era. A request opts into notifications/message delivery by setting io.modelcontextprotocol/logLevel in its _meta; records at or above that level are delivered on that request's own response stream only — including debug records below the operator's installed level — and requests without the opt-in receive none.","title":"Per-request logging"},{"category":"section","location":"tools/#MCP-Tools","page":"Tools","text":"Tools represent callable functions that language models can invoke. Each tool has a name, description, parameters, and a handler function.","title":"MCP Tools"},{"category":"section","location":"tools/#Tool-Structure","page":"Tools","text":"Every tool in ModelContextProtocol.jl is represented by the MCPTool struct, which contains:\n\nname: Unique identifier for the tool\ndescription: Human-readable explanation of the tool's purpose\nparameters: List of input parameters the tool accepts (for simple types)\ninput_schema: Custom JSON Schema for complex parameter types (takes precedence over parameters)\nhandler: Function that executes when the tool is called\nreturn_type: The expected return type of the handler (defaults to Vector{Content})\nrequired_scopes: OAuth scopes the caller must hold to invoke this tool, enforced at tools/call when HTTP authentication is active (see Authentication); empty by default, meaning no per-tool requirement","title":"Tool Structure"},{"category":"section","location":"tools/#Creating-Tools","page":"Tools","text":"Here's how to create a basic tool:\n\ncalculator_tool = MCPTool(\n    name = \"calculate\",\n    description = \"Perform basic arithmetic\",\n    parameters = [\n        ToolParameter(\n            name = \"expression\",\n            type = \"string\",\n            description = \"Math expression to evaluate\",\n            required = true\n        )\n    ],\n    handler = params -> TextContent(\n        text = JSON3.write(Dict(\n            \"result\" => eval(Meta.parse(params[\"expression\"]))\n        ))\n    )\n)","title":"Creating Tools"},{"category":"section","location":"tools/#Parameters","page":"Tools","text":"Tool parameters are defined using the ToolParameter struct:\n\nname: Parameter identifier\ndescription: Explanation of the parameter\ntype: JSON schema type (e.g., \"string\", \"number\", \"boolean\")\nrequired: Whether the parameter must be provided (default: false)\ndefault: Default value for the parameter (default: nothing)\nheader: Optional header-mirroring suffix, emitted as x-mcp-header in the generated schema (default: nothing). Modern HTTP clients mirror the argument as an Mcp-Param-<suffix> request header, which the server validates against the body — see The Modern Era","title":"Parameters"},{"category":"section","location":"tools/#Complex-Input-Schemas","page":"Tools","text":"For tools requiring arrays, enums, nested objects, or other advanced JSON Schema features, use input_schema instead of parameters. When input_schema is provided, the parameters field is ignored.","title":"Complex Input Schemas"},{"category":"section","location":"tools/#Array-Parameters","page":"Tools","text":"tag_tool = MCPTool(\n    name = \"filter_by_tags\",\n    description = \"Filter items by multiple tags\",\n    input_schema = Dict{String,Any}(\n        \"type\" => \"object\",\n        \"properties\" => Dict{String,Any}(\n            \"tags\" => Dict{String,Any}(\n                \"type\" => \"array\",\n                \"items\" => Dict{String,Any}(\"type\" => \"string\"),\n                \"description\" => \"List of tags to filter by\",\n                \"minItems\" => 1\n            )\n        ),\n        \"required\" => [\"tags\"]\n    ),\n    handler = function(params)\n        tags = params[\"tags\"]\n        TextContent(text = \"Filtering by tags: $(join(tags, \", \"))\")\n    end\n)","title":"Array Parameters"},{"category":"section","location":"tools/#Enum-Parameters","page":"Tools","text":"sort_tool = MCPTool(\n    name = \"sort_results\",\n    description = \"Sort results by field and order\",\n    input_schema = Dict{String,Any}(\n        \"type\" => \"object\",\n        \"properties\" => Dict{String,Any}(\n            \"field\" => Dict{String,Any}(\n                \"type\" => \"string\",\n                \"enum\" => [\"name\", \"date\", \"relevance\", \"size\"],\n                \"description\" => \"Field to sort by\"\n            ),\n            \"order\" => Dict{String,Any}(\n                \"type\" => \"string\",\n                \"enum\" => [\"asc\", \"desc\"],\n                \"default\" => \"asc\"\n            )\n        ),\n        \"required\" => [\"field\"]\n    ),\n    handler = function(params)\n        field = params[\"field\"]\n        order = get(params, \"order\", \"asc\")\n        TextContent(text = \"Sorting by $field ($order)\")\n    end\n)","title":"Enum Parameters"},{"category":"section","location":"tools/#Nested-Objects","page":"Tools","text":"filter_tool = MCPTool(\n    name = \"advanced_filter\",\n    description = \"Filter with complex criteria\",\n    input_schema = Dict{String,Any}(\n        \"type\" => \"object\",\n        \"properties\" => Dict{String,Any}(\n            \"query\" => Dict{String,Any}(\"type\" => \"string\"),\n            \"options\" => Dict{String,Any}(\n                \"type\" => \"object\",\n                \"properties\" => Dict{String,Any}(\n                    \"limit\" => Dict{String,Any}(\n                        \"type\" => \"integer\",\n                        \"default\" => 10,\n                        \"minimum\" => 1,\n                        \"maximum\" => 100\n                    ),\n                    \"offset\" => Dict{String,Any}(\n                        \"type\" => \"integer\",\n                        \"default\" => 0\n                    )\n                )\n            )\n        ),\n        \"required\" => [\"query\"]\n    ),\n    handler = function(params)\n        query = params[\"query\"]\n        options = get(params, \"options\", Dict())\n        limit = get(options, \"limit\", 10)\n        TextContent(text = \"Query: $query (limit=$limit)\")\n    end\n)","title":"Nested Objects"},{"category":"section","location":"tools/#Return-Values","page":"Tools","text":"Tool handlers can return various types which are automatically converted:\n\nContent instance: A single TextContent, ImageContent, or EmbeddedResource\nVector{<:Content}: Multiple content items (can mix different content types)\nDict: Automatically converted to JSON and wrapped in TextContent\nString: Automatically wrapped in TextContent\nTuple{Vector{UInt8}, String}: Automatically wrapped in ImageContent (bytes, mime_type)\nCallToolResult: For full control over the response including error handling\n\nWhen return_type is Vector{Content} (default), single items are automatically wrapped in a vector.","title":"Return Values"},{"category":"section","location":"tools/#Registering-Tools","page":"Tools","text":"Tools can be registered with a server in two ways:\n\nDuring server creation:\n\nserver = mcp_server(\n    name = \"my-server\",\n    tools = my_tool  # Single tool or vector of tools\n)\n\nAfter server creation:\n\nregister!(server, my_tool)","title":"Registering Tools"},{"category":"section","location":"tools/#Directory-Based-Organization","page":"Tools","text":"Tools can be organized in directory structures and auto-registered:\n\nmy_server/\n└── tools/\n    ├── calculator.jl\n    └── time_tool.jl\n\nEach file must define exactly one MCPTool as the file's final expression — the loader registers the value of the last expression and ignores everything else defined in the file:\n\n# calculator.jl\nusing ModelContextProtocol\nusing JSON3\n\ncalculator_tool = MCPTool(\n    name = \"calculate\",\n    description = \"Basic calculator\",\n    parameters = [\n        ToolParameter(\n            name = \"expression\",\n            type = \"string\",\n            description = \"Math expression to evaluate\",\n            required = true\n        )\n    ],\n    handler = params -> TextContent(\n        text = JSON3.write(Dict(\"result\" => eval(Meta.parse(params[\"expression\"]))))\n    )\n)\n\nThen auto-register from the directory:\n\nserver = mcp_server(\n    name = \"my-server\",\n    auto_register_dir = \"my_server\"\n)","title":"Directory-Based Organization"},{"category":"section","location":"tools/#Advanced-Examples","page":"Tools","text":"","title":"Advanced Examples"},{"category":"section","location":"tools/#Tool-with-Multiple-Content-Returns","page":"Tools","text":"analyze_tool = MCPTool(\n    name = \"analyze_data\",\n    description = \"Analyze data and return text + image\",\n    parameters = [\n        ToolParameter(name = \"data\", description = \"Data to analyze\", type = \"string\", required = true)\n    ],\n    handler = function(params)\n        # Return multiple content items\n        return [\n            TextContent(text = \"Analysis complete\"),\n            ImageContent(\n                data = generate_chart_bytes(),  # Your chart generation\n                mime_type = \"image/png\"\n            ),\n            TextContent(text = \"See chart above for details\")\n        ]\n    end,\n    return_type = Vector{Content}\n)","title":"Tool with Multiple Content Returns"},{"category":"section","location":"tools/#Tool-with-Error-Handling","page":"Tools","text":"safe_tool = MCPTool(\n    name = \"safe_operation\",\n    description = \"Tool with explicit error handling\",\n    parameters = [\n        ToolParameter(name = \"path\", description = \"File path\", type = \"string\", required = true)\n    ],\n    handler = function(params)\n        if !isfile(params[\"path\"])\n            # Return error result\n            return CallToolResult(\n                content = [Dict(\"type\" => \"text\", \"text\" => \"File not found\")],\n                is_error = true\n            )\n        end\n        \n        content = read(params[\"path\"], String)\n        return TextContent(text = content)\n    end\n)","title":"Tool with Error Handling"},{"category":"section","location":"tools/#Structured-Output","page":"Tools","text":"Declare an output_schema and return machine-readable results in structuredContent alongside the human-readable content (the spec recommends providing both):\n\nstats_tool = MCPTool(\n    name = \"get_stats\",\n    description = \"Compute dataset statistics\",\n    parameters = [],\n    output_schema = Dict{String,Any}(\n        \"type\" => \"object\",\n        \"properties\" => Dict{String,Any}(\"count\" => Dict{String,Any}(\"type\" => \"integer\"))\n    ),\n    handler = args -> CallToolResult(\n        content = [Dict{String,Any}(\"type\" => \"text\", \"text\" => \"{\\\"count\\\": 42}\")],\n        structured_content = Dict(\"count\" => 42)\n    )\n)\n\nThe schema is emitted as outputSchema in tools/list; the result field serializes as structuredContent.","title":"Structured Output"},{"category":"section","location":"tools/#Tool-Annotations","page":"Tools","text":"Annotations are behavioral hints clients can use for trust and approval decisions. They are emitted verbatim in tools/list:\n\nMCPTool(\n    name = \"delete_file\",\n    description = \"Delete a file\",\n    parameters = [ToolParameter(name = \"path\", type = \"string\", description = \"File path\", required = true)],\n    handler = my_handler,\n    annotations = Dict{String,Any}(\n        \"readOnlyHint\" => false,\n        \"destructiveHint\" => true,\n        \"idempotentHint\" => true,\n        \"openWorldHint\" => false\n    )\n)","title":"Tool Annotations"},{"category":"section","location":"tools/#Context-Aware-Handlers-and-Progress","page":"Tools","text":"A handler may accept a second argument to receive the per-request context — useful for progress reporting on long-running tools and for reading the authenticated user when HTTP auth is enabled:\n\nMCPTool(\n    name = \"long_job\",\n    description = \"Process with progress updates\",\n    parameters = [],\n    handler = (args, ctx) -> begin\n        for i in 1:10\n            send_progress(ctx, i; total = 10, message = \"step $i\")\n            # ... work ...\n        end\n        TextContent(text = \"done\")\n    end\n)\n\nsend_progress emits notifications/progress (over stdout for stdio, over the SSE stream for HTTP) and is a safe no-op when the client did not send a progressToken. The context also exposes ctx.authenticated_user and ctx.request_id. Plain one-argument handlers keep working unchanged.","title":"Context-Aware Handlers and Progress"},{"category":"section","location":"tools/#Audio-and-Resource-Links-in-Results","page":"Tools","text":"Tools can return audio and references to large artifacts without embedding them:\n\nhandler = args -> [\n    TextContent(text = \"Analysis complete\"),\n    AudioContent(data = wav_bytes, mime_type = \"audio/wav\"),\n    ResourceLink(\n        uri = \"file:///results/overlay.png\",\n        name = \"overlay.png\",\n        mime_type = \"image/png\",\n        size = 123_456\n    )\n]\n\nResourceLink serializes to the spec resource_link content block, letting clients fetch or subscribe to the artifact instead of receiving inline base64.","title":"Audio and Resource Links in Results"},{"category":"section","location":"tools/#Long-Running-Tools:-Tasks-(experimental)","page":"Tools","text":"This section describes the legacy-era tasks model (SEP-1686), used by clients that negotiated protocol 2025-11-25 at initialize. It lets those clients run a tool call in the background instead of waiting on the response: the client augments tools/call with a task field, the server immediately answers with a task handle, executes the handler in a background Julia task, and the client polls tasks/get until the task completes, then fetches the real result via tasks/result.\n\nThe modern era (2026-07-28) has a distinct tasks extension (SEP-2663) with different mechanics: creation is server-directed — the handler calls task_detach rather than the client augmenting the call — terminal results are inlined in tasks/get, and there is no tasks/result. See The Modern Era for that surface. The per-tool task_support field below is shared by both models.\n\nTools opt in per tool:\n\nMCPTool(\n    name = \"train_model\",\n    description = \"Long-running training job\",\n    parameters = [],\n    handler = (args, ctx) -> begin\n        for epoch in 1:100\n            task_cancelled(ctx) && return TextContent(text = \"stopped early\")\n            send_progress(ctx, epoch; total = 100, message = \"epoch $epoch\")\n            # ... work ...\n        end\n        TextContent(text = \"trained\")\n    end,\n    task_support = :optional   # :forbidden (default) | :optional | :required\n)\n\n:forbidden (the default): task-augmented calls are rejected (-32601), the tool always runs synchronously.\n:optional: the client chooses per call — plain calls run synchronously, calls with a task field run in the background.\n:required: the tool only runs as a task; synchronous calls are rejected (-32601).\n\nThe setting is advertised per tool as execution.taskSupport in tools/list, and the server only offers the tasks capability to clients that negotiated protocol 2025-11-25. Older clients fall back exactly as the spec mandates: their task metadata is ignored and the call runs synchronously. Modern-era clients never see this capability: they obtain tasks by declaring the io.modelcontextprotocol/tasks extension per request, and their task records are kept in a store isolated from the legacy ones.\n\nWhat the server handles for you:\n\ntasks/get — status polling (working → completed/failed/cancelled), with createdAt/lastUpdatedAt timestamps, the actual ttl, and a suggested pollInterval.\ntasks/result — blocks until the task is terminal, then returns exactly what the call would have returned (including tool errors), tagged with the spec's io.modelcontextprotocol/related-task metadata. The serial request loop is never blocked: on HTTP the POST simply stays open; on stdio the response is written out-of-band when ready.\ntasks/cancel — marks the task cancelled and wakes any blocked tasks/result. Handlers can notice via task_cancelled and stop early; even if the handler runs to completion, the late result is discarded. Cancelling an already terminal task is rejected (-32602).\ntasks/list — cursor-paginated listing of the requestor's tasks. On an HTTP transport without authentication the server cannot tell requestors apart, so tasks/list is withheld there (per the spec's security guidance). With HTTP auth enabled, tasks are bound to the authenticated principal: other principals cannot see, poll, fetch, or cancel them.\nnotifications/tasks/status — optional status-change notifications, delivered over the transport-correct channel (stdout for stdio, the SSE stream for HTTP).\n\nTask records are retained for the requested ttl (clamped to a server maximum of one hour; default five minutes) and swept after expiry. Progress notifications keep working inside task handlers — the progressToken from the original call stays valid for the task's lifetime.\n\nnote: Experimental\nTasks are experimental in the MCP spec and may evolve in future protocol versions. Server-initiated elicitation and sampling remain out of scope in the legacy era; the modern era covers the equivalent flows through MRTR (InputRequired) and mid-task input via task_await_input — see The Modern Era.\n\ntip: CPU-bound task handlers\nTask handlers run via Threads.@spawn. In our measurements (Julia 1.10 and 1.12, single-threaded server processes), even CPU-bound handlers did not starve the request loop — long blocking tasks/result calls, polling, and cancellation stayed responsive while a busy handler ran. Still, Julia's scheduler cannot preempt a loop with no yield points, so for servers running heavy compute tools the robust configuration is to start Julia with threads (julia -t auto) and/or call yield() (or task_cancelled(ctx), which is also a natural cancellation point) periodically inside hot loops.","title":"Long-Running Tools: Tasks (experimental)"},{"category":"section","location":"resources/#MCP-Resources","page":"Resources","text":"Resources provide data that language models can access. Each resource has a URI, name, MIME type, and a data provider function.","title":"MCP Resources"},{"category":"section","location":"resources/#Resource-Structure","page":"Resources","text":"Every resource in ModelContextProtocol.jl is represented by the MCPResource struct:\n\nuri: Unique URI identifier for the resource\nname: Human-readable resource name\ndescription: Explanation of the resource's purpose\nmime_type: Content type (e.g., \"application/json\", \"text/plain\")\ndata_provider: Function that returns the resource's data\nannotations: Optional metadata about the resource\ntitle: Optional human-friendly display name\nicons: Optional icons for UI display\n_meta: Optional metadata for protocol extensions, emitted verbatim in resources/list","title":"Resource Structure"},{"category":"section","location":"resources/#Creating-Resources","page":"Resources","text":"Here's how to create a basic resource:\n\nusing URIs\n\nweather_resource = MCPResource(\n    uri = \"weather://current\",\n    name = \"Current Weather\",\n    description = \"Current weather conditions\",\n    mime_type = \"application/json\",\n    data_provider = () -> Dict(\n        \"temperature\" => 22.5,\n        \"conditions\" => \"Partly Cloudy\",\n        \"updated\" => Dates.format(now(), \"yyyy-mm-dd HH:MM:SS\")\n    )\n)\n\nNote: The uri field accepts both strings and URI objects. Strings are automatically converted to URIs.","title":"Creating Resources"},{"category":"section","location":"resources/#Data-Providers","page":"Resources","text":"The data_provider is a zero-argument function called on every resources/read. It can return different types of data:\n\nFor simple data (automatically serialized to JSON):\nReturn Julia objects (Dict, Array, etc.) that can be JSON-serialized\nThese become a text contents entry with the resource's mime_type\nFor verbatim text: return a String — used as the text contents as-is\nFor explicit control over content (including binary):\nReturn TextResourceContents for text data\nReturn BlobResourceContents for binary data (base64 blob on the wire)\nReturn a Vector of them for multiple contents entries","title":"Data Providers"},{"category":"section","location":"resources/#Registering-Resources","page":"Resources","text":"Resources can be registered with a server in two ways:\n\nDuring server creation:\n\nserver = mcp_server(\n    name = \"my-server\",\n    resources = my_resource  # Single resource or vector of resources\n)\n\nAfter server creation:\n\nregister!(server, my_resource)","title":"Registering Resources"},{"category":"section","location":"resources/#Directory-Based-Organization","page":"Resources","text":"Resources can be organized in directory structures and auto-registered:\n\nmy_server/\n└── resources/\n    ├── weather.jl\n    └── stock_data.jl\n\nEach file must define exactly one MCPResource as the file's final expression — the loader registers the value of the last expression and ignores everything else defined in the file:\n\n# weather.jl\nusing ModelContextProtocol\nusing Dates\n\nweather_resource = MCPResource(\n    uri = \"weather://current\",\n    name = \"Current Weather\",\n    description = \"Current weather conditions\",\n    mime_type = \"application/json\",\n    data_provider = () -> Dict(\n        \"temperature\" => 22.5,\n        \"conditions\" => \"Partly Cloudy\",\n        \"updated\" => Dates.format(now(), \"yyyy-mm-dd HH:MM:SS\")\n    )\n)\n\nThen auto-register from the directory:\n\nserver = mcp_server(\n    name = \"my-server\",\n    auto_register_dir = \"my_server\"\n)","title":"Directory-Based Organization"},{"category":"section","location":"resources/#Advanced-Examples","page":"Resources","text":"","title":"Advanced Examples"},{"category":"section","location":"resources/#Resource-with-Dynamic-Data","page":"Resources","text":"The data_provider is called with no arguments on every resources/read. What it returns determines the wire contents:\n\nA TextResourceContents or BlobResourceContents (or a vector of them) is serialized directly — this is how binary resources are served (base64 blob on the wire) and how you control the contents uri/mimeType per entry.\nA String becomes the text contents verbatim, with the resource's mime_type.\nAnything else is JSON-encoded into a text contents entry.\n\n# JSON data (encoded automatically)\nlog_resource = MCPResource(\n    uri = \"app://logs/recent\",\n    name = \"Recent Log Entries\",\n    description = \"The most recent application log entries\",\n    mime_type = \"application/json\",\n    data_provider = function ()\n        entries = isfile(\"app.log\") ?\n            collect(Iterators.take(eachline(\"app.log\"), 50)) : String[]\n        return Dict(\"count\" => length(entries), \"entries\" => entries)\n    end\n)","title":"Resource with Dynamic Data"},{"category":"section","location":"resources/#Resource-with-Binary-Data","page":"Resources","text":"Return a BlobResourceContents to serve binary data (base64-encoded on the wire):\n\nimage_resource = MCPResource(\n    uri = \"images://logo\",\n    name = \"Logo Image\",\n    description = \"Company logo\",\n    mime_type = \"image/png\",\n    data_provider = () -> BlobResourceContents(\n        uri = \"images://logo\",\n        mime_type = \"image/png\",\n        blob = read(\"logo.png\")   # Vector{UInt8}\n    )\n)\n\nThis pairs naturally with ResourceLink tool results: a tool can return a link to a large artifact, and the client then reads the binary via resources/read.","title":"Resource with Binary Data"},{"category":"section","location":"resources/#URI-Templates","page":"Resources","text":"For a parameterized family of resources — content-addressed artifacts, per-id results, file trees — register a ResourceTemplate instead of one resource per URI. Templates use RFC 6570 level-1 placeholders ({var}, matching one path segment) and are advertised to clients via resources/templates/list:\n\nartifact_template = ResourceTemplate(\n    name = \"artifact\",\n    uri_template = \"app://artifact/{id}\",\n    description = \"Content-addressed result artifacts\",\n    mime_type = \"image/png\",\n    data_provider = (uri, vars) -> BlobResourceContents(\n        uri = uri,\n        mime_type = \"image/png\",\n        blob = read(artifact_path(vars[\"id\"]))\n    )\n)\n\nserver = mcp_server(\n    name = \"my-server\",\n    resource_templates = [artifact_template]   # or register!(server, artifact_template)\n)\n\nA resources/read whose URI matches no exact resource is routed through the templates: the first match with a provider serves it. The provider receives the requested URI — provider(uri) — or opts into provider(uri, vars) to also get the extracted placeholder values. Return values follow the same contract as resource providers (ResourceContents/vector, String verbatim, JSON fallback). Exact-URI resources always take precedence over templates.","title":"URI Templates"},{"category":"section","location":"resources/#Completing-Template-Variables","page":"Resources","text":"A template's completions field supplies completion/complete suggestions for its placeholders, so clients can offer values as the user types a URI. It maps a variable name to either a Vector{String} (served filtered by prefix against the partial value) or a function — value -> values, or (value, context_args) -> values to also receive the arguments already resolved for the request:\n\nartifact_template = ResourceTemplate(\n    name = \"artifact\",\n    uri_template = \"app://artifact/{id}\",\n    description = \"Content-addressed result artifacts\",\n    data_provider = (uri, vars) -> read_artifact(vars[\"id\"]),\n    completions = Dict{String,Any}(\"id\" => value -> recent_artifact_ids(value))\n)\n\nSuggestions are capped at 100 values with total/hasMore reported to the client. The wire details are in The Modern Era; completion/complete is served in both eras and the completion capability is advertised by default.","title":"Completing Template Variables"},{"category":"section","location":"resources/#Subscriptions-and-Change-Notifications","page":"Resources","text":"Clients can follow resource changes instead of re-reading, through a different mechanism in each era:\n\nLegacy era: resources/subscribe / resources/unsubscribe record the client's interest per URI on its session (the subscribe resource capability is advertised by default). Both are idempotent and answer with an empty result; a subscribed session then receives notifications/resources/updated for those URIs — on stdout for stdio, on the standalone GET SSE stream for Streamable HTTP.\nModern era: subscriptions/listen replaces both the legacy GET stream and resources/subscribe — a single long-lived request whose response stream carries only the notification types the client opted into. See The Modern Era.\n\nAnnounce changes from server code with two exported functions, which deliver to the open subscriptions/listen streams that opted into them and to the subscribed legacy session:\n\n# A resource's contents changed\nnotify_resource_updated(server, \"app://logs/recent\")\n\n# A component list changed after registering or removing something at runtime\nnotify_list_changed(server, :resources)   # or :tools, :prompts\n\nBoth return the number of clients notified (listen streams plus the legacy session when it was reached). There is no backlog — only clients connected at the time of the call are reached — and notify_list_changed reaches the legacy session only when the corresponding listChanged capability is declared.\n\nSeparately, subscribe!(server, uri, callback) and unsubscribe!(server, uri, callback) maintain an in-process registry of callbacks keyed by URI, for server-side code that wants to react to its own updates. notify_resource_updated invokes each matching callback as callback(uri) (errors are logged and swallowed); the callbacks themselves send nothing to clients.","title":"Subscriptions and Change Notifications"},{"category":"section","location":"examples/#Examples","page":"Examples","text":"This page provides comprehensive examples of using ModelContextProtocol.jl to create MCP servers with various features and configurations.","title":"Examples"},{"category":"section","location":"examples/#Basic-Usage","page":"Examples","text":"","title":"Basic Usage"},{"category":"section","location":"examples/#Simple-Echo-Server","page":"Examples","text":"A minimal MCP server with a single tool:\n\nusing ModelContextProtocol\n\n# Create a simple echo tool\necho_tool = MCPTool(\n    name = \"echo\",\n    description = \"Echo back the provided message\",\n    parameters = [\n        ToolParameter(\n            name = \"message\",\n            type = \"string\",\n            description = \"Message to echo back\",\n            required = true\n        )\n    ],\n    handler = function(params)\n        return TextContent(text = \"Echo: $(params[\"message\"])\")\n    end\n)\n\n# Create and configure server\nserver = mcp_server(\n    name = \"echo-server\",\n    version = \"1.0.0\",\n    tools = [echo_tool]\n)\n\n# Display server configuration\nprintln(\"Server created: $(server.config.name) v$(server.config.version)\")\nprintln(\"Available tools: \", [t.name for t in server.tools])","title":"Simple Echo Server"},{"category":"section","location":"examples/#Calculator-Server","page":"Examples","text":"A server with multiple arithmetic operations:\n\nusing ModelContextProtocol\n\n# Create calculator tool with multiple operations\ncalc_tool = MCPTool(\n    name = \"calculate\",\n    description = \"Perform arithmetic calculations\",\n    parameters = [\n        ToolParameter(name = \"a\", type = \"number\", description = \"First number\", required = true),\n        ToolParameter(name = \"b\", type = \"number\", description = \"Second number\", required = true),\n        ToolParameter(name = \"operation\", type = \"string\",\n                     description = \"Operation: add, subtract, multiply, divide\", required = true)\n    ],\n    handler = function(params)\n        a = params[\"a\"]\n        b = params[\"b\"]\n        op = params[\"operation\"]\n\n        result = if op == \"add\"\n            a + b\n        elseif op == \"subtract\"\n            a - b\n        elseif op == \"multiply\"\n            a * b\n        elseif op == \"divide\"\n            b != 0 ? a / b : \"Error: Division by zero\"\n        else\n            \"Error: Unknown operation\"\n        end\n\n        return TextContent(text = \"Result: $result\")\n    end\n)\n\nserver = mcp_server(\n    name = \"calculator\",\n    version = \"1.0.0\",\n    tools = [calc_tool]\n)\n\nprintln(\"Calculator server configured with tool: $(server.tools[1].name)\")","title":"Calculator Server"},{"category":"section","location":"examples/#Advanced-Examples","page":"Examples","text":"","title":"Advanced Examples"},{"category":"section","location":"examples/#Multi-Content-Tool-Returns","page":"Examples","text":"Tools can return multiple content items of different types:\n\nusing ModelContextProtocol\nusing Base64\n\n# Tool that returns multiple content types\nanalysis_tool = MCPTool(\n    name = \"analyze_data\",\n    description = \"Analyze data and return multiple results\",\n    parameters = [\n        ToolParameter(name = \"data\", type = \"string\", description = \"Data to analyze\", required = true)\n    ],\n    handler = function(params)\n        data = params[\"data\"]\n\n        # Return multiple content items\n        space_char = ' '\n        return [\n            TextContent(text = \"Analysis Summary: Processed $(length(data)) characters\"),\n            TextContent(text = \"Data contains $(count(==(space_char), data)) spaces\"),\n            # Could also include ImageContent with base64 data\n            # ImageContent(data = base64_encoded_image, mime_type = \"image/png\")\n        ]\n    end,\n    return_type = Vector{Content}  # Explicitly specify multi-content return\n)\n\nserver = mcp_server(\n    name = \"analyzer\",\n    version = \"1.0.0\",\n    tools = [analysis_tool]\n)\n\nprintln(\"Multi-content tool configured: $(server.tools[1].name)\")","title":"Multi-Content Tool Returns"},{"category":"section","location":"examples/#Structured-Tool-Output","page":"Examples","text":"A tool can declare an output_schema and return machine-readable data alongside the human-readable content, by returning a CallToolResult with structured_content:\n\nusing ModelContextProtocol\n\nstats_tool = MCPTool(\n    name = \"summarize\",\n    description = \"Summarize a list of numbers\",\n    parameters = [\n        ToolParameter(name = \"values\", description = \"Comma-separated numbers\",\n                      type = \"string\", required = true)\n    ],\n    output_schema = Dict{String,Any}(\n        \"type\" => \"object\",\n        \"properties\" => Dict{String,Any}(\n            \"count\" => Dict{String,Any}(\"type\" => \"integer\"),\n            \"mean\"  => Dict{String,Any}(\"type\" => \"number\")\n        )\n    ),\n    handler = function(params)\n        values = parse.(Float64, split(params[\"values\"], \",\"))\n        summary = Dict(\"count\" => length(values), \"mean\" => sum(values) / length(values))\n        return CallToolResult(\n            content = [TextContent(text = \"count=$(summary[\"count\"]), mean=$(summary[\"mean\"])\")],\n            structured_content = summary\n        )\n    end\n)\n\nserver = mcp_server(\n    name = \"stats-server\",\n    version = \"1.0.0\",\n    tools = [stats_tool]\n)\n\nprintln(\"Structured-output tool configured: $(server.tools[1].name)\")\n\nThe schema is emitted as outputSchema in tools/list, and structured_content serializes as structuredContent in the tool result. The spec recommends supplying both forms, as above.","title":"Structured Tool Output"},{"category":"section","location":"examples/#Resource-Server-with-Custom-Data","page":"Examples","text":"Implementing resources with custom data providers:\n\nusing ModelContextProtocol\nusing URIs  # For URI construction\n\n# Create a resource with a custom data provider\n# (called with no arguments; plain data is JSON-encoded — return a String for\n# verbatim text or Text/BlobResourceContents for full control incl. binary)\nconfig_resource = MCPResource(\n    uri = URI(\"config://app/settings\"),\n    name = \"Application Settings\",\n    description = \"Current application configuration\",\n    mime_type = \"application/json\",\n    data_provider = function ()\n        # In real implementation, read from actual config\n        return Dict(\n            \"theme\" => \"dark\",\n            \"language\" => \"en\",\n            \"debug\" => false\n        )\n    end,\n    annotations = Dict{String,Any}()\n)\n\n# Create another resource for file access\nfile_resource = MCPResource(\n    uri = URI(\"file:///readme.txt\"),\n    name = \"README File\",\n    description = \"Project readme file\",\n    mime_type = \"application/json\",\n    data_provider = function ()\n        # In real implementation, read the actual file\n        return Dict(\"content\" => \"# Project README\\n\\nThis is the readme content.\")\n    end\n)\n\nserver = mcp_server(\n    name = \"resource-server\",\n    version = \"1.0.0\",\n    resources = [config_resource, file_resource]\n)\n\nprintln(\"Server configured with $(length(server.resources)) resources\")","title":"Resource Server with Custom Data"},{"category":"section","location":"examples/#Prompt-Templates-Server","page":"Examples","text":"Creating reusable prompt templates:\n\nusing ModelContextProtocol\n\n# Import Role enum values\nuser_role = ModelContextProtocol.user\nassistant_role = ModelContextProtocol.assistant\n\n# Function to generate messages for code review\nfunction generate_code_review_messages(args)\n    lang = args[\"language\"]\n    focus = get(args, \"focus\", \"general quality\")\n    return [\n        PromptMessage(\n            role = user_role,\n            content = TextContent(\n                text = \"Please review this $lang code focusing on $focus. \" *\n                      \"Look for bugs, performance issues, and adherence to best practices.\"\n            )\n        ),\n        PromptMessage(\n            role = assistant_role,\n            content = TextContent(\n                text = \"I'll review your $lang code with a focus on $focus. \" *\n                      \"Please share the code you'd like me to examine.\"\n            )\n        )\n    ]\nend\n\n# Create a code review prompt template\ncode_review_prompt = MCPPrompt(\n    name = \"code_review\",\n    description = \"Generate a code review request\",\n    arguments = [\n        PromptArgument(name = \"language\", description = \"Programming language\", required = true),\n        PromptArgument(name = \"focus\", description = \"Review focus area\", required = false)\n    ],\n    messages = generate_code_review_messages(Dict(\"language\" => \"Julia\", \"focus\" => \"performance\"))\n)\n\n# Create a data analysis prompt with static messages\nanalysis_prompt = MCPPrompt(\n    name = \"data_analysis\",\n    description = \"Request data analysis\",\n    arguments = [\n        PromptArgument(name = \"dataset\", description = \"Dataset description\", required = true)\n    ],\n    messages = [\n        PromptMessage(\n            role = user_role,\n            content = TextContent(\n                text = \"Analyze the dataset. \" *\n                      \"Provide insights on patterns, anomalies, and recommendations.\"\n            )\n        )\n    ]\n)\n\nserver = mcp_server(\n    name = \"prompt-server\",\n    version = \"1.0.0\",\n    prompts = [code_review_prompt, analysis_prompt]\n)\n\nprintln(\"Server configured with prompts: \", [p.name for p in server.prompts])","title":"Prompt Templates Server"},{"category":"section","location":"examples/#Transport-Configuration","page":"Examples","text":"","title":"Transport Configuration"},{"category":"section","location":"examples/#HTTP-Transport-with-Custom-Settings","page":"Examples","text":"using ModelContextProtocol\n\nserver = mcp_server(\n    name = \"http-server\",\n    version = \"1.0.0\",\n    tools = [\n        MCPTool(\n            name = \"status\",\n            description = \"Get server status\",\n            parameters = [],\n            handler = args -> TextContent(text = \"Server is running\")\n        )\n    ]\n)\n\n# HttpTransport is exported; constructing it does not bind the port yet\nserver.transport = HttpTransport(host = \"127.0.0.1\", port = 8765)\n\nprintln(\"Server configured: $(server.config.name) on port $(server.transport.port)\")\n\nconnect binds the port and starts the HTTP listener; start! then blocks in the server loop — both are omitted above so building these docs does not leave a server running:\n\nconnect(server.transport)\nstart!(server)\n\n# Equivalent, without assigning the transport first — connect is still required\n# (start! only assigns the transport; a disconnected one ends the loop at once):\ntransport = HttpTransport(host = \"127.0.0.1\", port = 8765)\nconnect(transport)\nstart!(server; transport = transport)\n\nSee Transports for session management, SSE, and the security options.","title":"HTTP Transport with Custom Settings"},{"category":"section","location":"examples/#Common-Patterns","page":"Examples","text":"","title":"Common Patterns"},{"category":"section","location":"examples/#Error-Handling-in-Tools","page":"Examples","text":"Properly handling errors in tool implementations:\n\nusing ModelContextProtocol\n\nsafe_division_tool = MCPTool(\n    name = \"safe_divide\",\n    description = \"Divide two numbers with error handling\",\n    parameters = [\n        ToolParameter(name = \"dividend\", description = \"Number to be divided\", type = \"number\", required = true),\n        ToolParameter(name = \"divisor\", description = \"Number to divide by\", type = \"number\", required = true)\n    ],\n    handler = function(params)\n        dividend = params[\"dividend\"]\n        divisor = params[\"divisor\"]\n\n        # Return error result for invalid input\n        if divisor == 0\n            return CallToolResult(\n                content = [TextContent(text = \"Error: Division by zero is not allowed\")],\n                is_error = true\n            )\n        end\n\n        result = dividend / divisor\n        return TextContent(text = \"Result: $result\")\n    end\n)\n\nserver = mcp_server(\n    name = \"safe-calc\",\n    version = \"1.0.0\",\n    tools = [safe_division_tool]\n)\n\nprintln(\"Error-handling tool configured: $(server.tools[1].name)\")","title":"Error Handling in Tools"},{"category":"section","location":"examples/#Directory-Based-Auto-Registration","page":"Examples","text":"Organizing components in a directory structure:\n\nusing ModelContextProtocol\n\n# Example directory structure:\n# my_server/\n# ├── tools/\n# │   ├── file_ops.jl    # File operation tools\n# │   └── data_proc.jl   # Data processing tools\n# ├── resources/\n# │   └── configs.jl     # Configuration resources\n# └── prompts/\n#     └── templates.jl   # Prompt templates\n\n# Each file exports components like:\n# file: tools/file_ops.jl\n# read_file = MCPTool(name = \"read_file\", ...)\n# write_file = MCPTool(name = \"write_file\", ...)\n\n# Auto-register all components from directory\nserver = mcp_server(\n    name = \"auto-server\",\n    version = \"2.0.0\",\n    auto_register_dir = \"my_server\"  # Scans and registers all components\n)\n\n# Alternatively, register individual components\nserver2 = mcp_server(name = \"manual-server\", version = \"1.0.0\")\n# Use register! with specific components:\n# register!(server2, my_tool)  # Register a tool\n# register!(server2, my_resource)  # Register a resource\n# register!(server2, my_prompt)  # Register a prompt\n\nprintln(\"Auto-registration example configured\")","title":"Directory-Based Auto-Registration"},{"category":"section","location":"examples/#Tool-with-Default-Parameters","page":"Examples","text":"Using default values for optional parameters:\n\nusing ModelContextProtocol\nusing Dates\n\nformat_date_tool = MCPTool(\n    name = \"format_date\",\n    description = \"Format the current date/time\",\n    parameters = [\n        ToolParameter(\n            name = \"format\",\n            type = \"string\",\n            description = \"Date format string\",\n            required = false,\n            default = \"yyyy-mm-dd HH:MM:SS\"  # Default format\n        ),\n        ToolParameter(\n            name = \"timezone\",\n            type = \"string\",\n            description = \"Timezone (not implemented in this example)\",\n            required = false,\n            default = \"UTC\"\n        )\n    ],\n    handler = function(params)\n        format_str = params[\"format\"]  # Will use default if not provided\n        current_time = Dates.format(now(), format_str)\n        return TextContent(text = \"Current time: $current_time\")\n    end\n)\n\nserver = mcp_server(\n    name = \"time-server\",\n    version = \"1.0.0\",\n    tools = [format_date_tool]\n)\n\nprintln(\"Tool with defaults configured: $(server.tools[1].name)\")","title":"Tool with Default Parameters"},{"category":"section","location":"examples/#Testing-Your-Implementation","page":"Examples","text":"","title":"Testing Your Implementation"},{"category":"section","location":"examples/#Unit-Testing-Tools","page":"Examples","text":"Example of testing tool handlers:\n\nusing ModelContextProtocol\nusing Test\n\n# Create a test tool\ntest_tool = MCPTool(\n    name = \"test_tool\",\n    description = \"Tool for testing\",\n    parameters = [\n        ToolParameter(name = \"input\", description = \"Text to process\",\n                      type = \"string\", required = true)\n    ],\n    handler = function(params)\n        return TextContent(text = \"Processed: $(params[\"input\"])\")\n    end\n)\n\n# Test the handler directly\n@testset \"Tool Handler Tests\" begin\n    # Test normal operation\n    result = test_tool.handler(Dict(\"input\" => \"test\"))\n    @test result isa TextContent\n    @test result.text == \"Processed: test\"\n\n    # Test with different inputs\n    result2 = test_tool.handler(Dict(\"input\" => \"hello\"))\n    @test result2.text == \"Processed: hello\"\nend","title":"Unit Testing Tools"},{"category":"section","location":"examples/#Integration-Testing-with-curl","page":"Examples","text":"Testing HTTP server endpoints:\n\n# Start your HTTP server first\njulia --project server_http.jl &\n\n# Wait for server to start\nsleep 5\n\n# Test initialization\ncurl -X POST http://127.0.0.1:8765/ \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"jsonrpc\":\"2.0\",\"method\":\"initialize\",\"params\":{\"protocolVersion\":\"2025-11-25\",\"capabilities\":{},\"clientInfo\":{\"name\":\"test\",\"version\":\"1.0\"}},\"id\":1}'\n\n# Save session ID from response, then test tool listing\ncurl -X POST http://127.0.0.1:8765/ \\\n  -H \"Content-Type: application/json\" \\\n  -H \"Mcp-Session-Id: YOUR_SESSION_ID\" \\\n  -d '{\"jsonrpc\":\"2.0\",\"method\":\"tools/list\",\"params\":{},\"id\":2}'","title":"Integration Testing with curl"},{"category":"section","location":"examples/#Modern-Era-Requests-(2026-07-28)","page":"Examples","text":"The same server also answers stateless 2026-07-28 requests — no initialize, no session. The era is selected per request by the _meta key io.modelcontextprotocol/protocolVersion, and over HTTP the SEP-2243 headers MCP-Protocol-Version and Mcp-Method must mirror the body:\n\ncurl -X POST http://127.0.0.1:8765/ \\\n  -H \"Content-Type: application/json\" \\\n  -H \"Accept: application/json, text/event-stream\" \\\n  -H \"MCP-Protocol-Version: 2026-07-28\" \\\n  -H \"Mcp-Method: tools/list\" \\\n  -d '{\"jsonrpc\":\"2.0\",\"method\":\"tools/list\",\"params\":{\"_meta\":{\"io.modelcontextprotocol/protocolVersion\":\"2026-07-28\",\"io.modelcontextprotocol/clientCapabilities\":{}}},\"id\":1}'\n\nCalling a tool additionally needs Mcp-Name mirroring params.name. See The Modern Era (2026-07-28) for the full modern surface.","title":"Modern-Era Requests (2026-07-28)"},{"category":"section","location":"examples/#Best-Practices","page":"Examples","text":"Always validate input parameters in tool handlers\nUse appropriate content types for different data formats\nProvide clear descriptions for tools, resources, and prompts\nHandle errors gracefully with CallToolResult when needed\nTest with MCP Inspector before deploying to production\nUse 127.0.0.1 instead of localhost on Windows for HTTP transport\nOrganize complex servers using directory-based auto-registration\nDocument parameter schemas thoroughly for better LLM understanding","title":"Best Practices"},{"category":"section","location":"examples/#See-Also","page":"Examples","text":"Tools Documentation for detailed tool implementation\nResources Documentation for resource management\nPrompts Documentation for prompt templates\nTransports for stdio and Streamable HTTP configuration\nThe Modern Era (2026-07-28) for stateless requests, MRTR, tasks, and subscriptions\nAuthentication for OAuth Resource Server token validation\nDeployment for exposing an authenticated server to remote clients\nAPI Reference for complete function documentation","title":"See Also"},{"category":"section","location":"transports/#Transport-Protocols","page":"Transports","text":"ModelContextProtocol.jl supports two transport protocols for communication between MCP servers and clients.","title":"Transport Protocols"},{"category":"section","location":"transports/#stdio-Transport","page":"Transports","text":"The stdio transport uses standard input and output streams for communication. This is the simplest transport method and works well for command-line applications and process-to-process communication.","title":"stdio Transport"},{"category":"section","location":"transports/#Basic-Usage","page":"Transports","text":"using ModelContextProtocol\n\n# Create a server with stdio transport (default)\nserver = mcp_server(\n    name = \"my-server\",\n    version = \"1.0.0\",\n    tools = [my_tool]\n)\n\n# Start the server (uses stdio by default)\nstart!(server)\n\nThe server will read JSON-RPC messages from stdin and write responses to stdout.","title":"Basic Usage"},{"category":"section","location":"transports/#Streamable-HTTP-Transport","page":"Transports","text":"The Streamable HTTP transport implements the MCP protocol over HTTP with Server-Sent Events (SSE) support. This enables web-based clients and provides real-time streaming capabilities.","title":"Streamable HTTP Transport"},{"category":"section","location":"transports/#Basic-HTTP-Server","page":"Transports","text":"using ModelContextProtocol\n\n# Create HTTP transport (the legacy protocol version is negotiated per client;\n# the server speaks 2025-11-25 down to 2024-11-05, and serves modern-era\n# 2026-07-28 requests on the same endpoint)\ntransport = HttpTransport(\n    host = \"127.0.0.1\",\n    port = 3000\n)\n\n# Create server\nserver = mcp_server(\n    name = \"http-server\",\n    version = \"1.0.0\", \n    tools = [my_tool]\n)\n\n# Set transport and start\nserver.transport = transport\nModelContextProtocol.connect(transport)\nstart!(server)","title":"Basic HTTP Server"},{"category":"section","location":"transports/#Configuration-Options","page":"Transports","text":"The HttpTransport constructor accepts the following keyword arguments, shown with their real defaults:\n\ntransport = HttpTransport(\n    host = \"127.0.0.1\",                 # Bind address (loopback by default)\n    port = 8080,                        # Port number\n    endpoint = \"/\",                     # Base endpoint path\n    allowed_origins = String[],         # Extra Origins accepted by the DNS-rebinding guard\n    allowed_hosts = String[],           # Extra Host hostnames accepted by that guard\n    protocol_version = LATEST_PROTOCOL_VERSION,  # Advertised legacy version (\"2025-11-25\")\n    session_required = false,           # Require a session on non-initialize legacy requests\n    auth = nothing,                     # Optional AuthMiddleware (see Authentication below)\n    resource_metadata = nothing,        # Optional RFC 9728 Protected Resource Metadata\n    sse_keepalive_secs = 15.0           # Idle interval between SSE keepalive comments\n)\n\nsse_keepalive_secs must be finite and positive — the periodic write is the only way to notice a silently-dead SSE peer, so Inf (which would disable detection) and non-positive values are rejected at construction.\n\nprotocol_version is the advertised legacy version: 2025-11-25 is the newest version reachable through initialize, negotiated per client down to 2024-11-05, and response headers echo the negotiated value. It is not the newest protocol the server speaks — modern-era clients select 2026-07-28 per request through the params _meta key io.modelcontextprotocol/protocolVersion, with no handshake and no session; see The Modern Era (2026-07-28).\n\nSSE is always available via GET with Accept: text/event-stream — there is no switch.","title":"Configuration Options"},{"category":"section","location":"transports/#Session-Management","page":"Transports","text":"HTTP transport uses session-based communication for security and state tracking:\n\nInitialization: Client sends initialization request\nSession Creation: Server responds with Mcp-Session-Id header  \nSubsequent Requests: Client includes session ID in Mcp-Session-Id header\n\n# Initialize and get session ID\ncurl -X POST http://localhost:3000/ \\\n  -H 'Content-Type: application/json' \\\n  -H 'MCP-Protocol-Version: 2025-11-25' \\\n  -d '{\"jsonrpc\":\"2.0\",\"method\":\"initialize\",\"params\":{},\"id\":1}' \\\n  -i\n\n# Use session ID in subsequent requests  \ncurl -X POST http://localhost:3000/ \\\n  -H 'Content-Type: application/json' \\\n  -H 'MCP-Protocol-Version: 2025-11-25' \\\n  -H 'Mcp-Session-Id: <session-id-from-response>' \\\n  -d '{\"jsonrpc\":\"2.0\",\"method\":\"tools/list\",\"params\":{},\"id\":2}'","title":"Session Management"},{"category":"section","location":"transports/#Server-Sent-Events-(SSE)","page":"Transports","text":"Notifications related to an in-flight request — notifications/progress from a ctx-aware tool, notifications/message log events emitted during handling — are delivered on that request's own response: when they occur, the POST response is a text/event-stream carrying the notifications followed by the final JSON-RPC response. A request that emits nothing gets a plain application/json response. Clients must accept both content types (send Accept: application/json, text/event-stream); if a client accepts only JSON, request-scoped notifications are dropped and it receives the plain response.\n\nOut-of-band notifications — background MCP Tasks status updates (notifications/tasks/status) and anything emitted outside request handling — flow on a standalone stream that clients open with a GET request:\n\ncurl -N -H 'Accept: text/event-stream' http://127.0.0.1:3000/\n\nThat GET stream is legacy-only. The modern era removed it along with resources/subscribe: a client instead issues subscriptions/listen, a long-lived POST whose response stream carries the notification types it opted into (list-changed events, resource URIs, task ids). A GET that declares a modern MCP-Protocol-Version gets 405 Method Not Allowed. See The Modern Era (2026-07-28).","title":"Server-Sent Events (SSE)"},{"category":"section","location":"transports/#Modern-era-requests-(2026-07-28)","page":"Transports","text":"Modern-era clients need no initialize and no session: each request carries io.modelcontextprotocol/protocolVersion in its params _meta and is answered as a self-contained exchange on the same endpoint. Over HTTP, SEP-2243 additionally requires standard headers that mirror the body:\n\nMCP-Protocol-Version and Mcp-Method on every modern request, matching the body's _meta version and its method.\nMcp-Name on the methods that name a primary target — tools/call and prompts/get (mirroring params.name), resources/read (params.uri), and tasks/get, tasks/update, tasks/cancel (params.taskId). A value that is not header-safe may be wrapped in the =?base64?…?= sentinel, which the server decodes before comparing.\n\nHeader names are matched case-insensitively; a duplicated header, one carrying non-visible-ASCII bytes, or any value that disagrees with the body is a -32020 HeaderMismatch returned with HTTP 400. An unknown method is -32601 with HTTP 404, and an unsupported protocol version is -32022 with HTTP 400. Mcp-Session-Id is ignored on modern requests and sessions are never minted for them.\n\ncurl -X POST http://127.0.0.1:3000/ \\\n  -H 'Content-Type: application/json' \\\n  -H 'Accept: application/json, text/event-stream' \\\n  -H 'MCP-Protocol-Version: 2026-07-28' \\\n  -H 'Mcp-Method: tools/list' \\\n  -d '{\"jsonrpc\":\"2.0\",\"method\":\"tools/list\",\"params\":{\"_meta\":{\"io.modelcontextprotocol/protocolVersion\":\"2026-07-28\",\"io.modelcontextprotocol/clientCapabilities\":{}}},\"id\":1}'","title":"Modern-era requests (2026-07-28)"},{"category":"section","location":"transports/#Security-Features","page":"Transports","text":"","title":"Security Features"},{"category":"section","location":"transports/#Authentication-(OAuth-Resource-Server)","page":"Transports","text":"The HTTP transport can require a bearer token on every request. Validators include GitHub tokens (validated against the GitHub API with optional allowlist/organization checks), JWTs verified against a JWKS endpoint, JWT claims, and RFC 7662 token introspection:\n\nusing ModelContextProtocol\n\nauth = create_github_auth(\n    allowed_users = [\"alice\", \"bob\"],   # empty list = any authenticated GitHub user\n    required_org  = \"MyLab\",            # optional organization gate\n)\nmeta = create_github_resource_metadata(\"https://mcp.example.org\")\n\ntransport = HttpTransport(host = \"0.0.0.0\", port = 3000,\n                          auth = auth, resource_metadata = meta)\n\nFor JWTs issued by an external authorization server (Keycloak, Auth0, etc.), use JWKSValidator — it verifies token signatures against the server's published JSON Web Key Set (RFC 7517) and then applies the standard claim checks (issuer, audience, expiry, scopes), all fail-closed:\n\nauth = create_auth_middleware(\n    OAuthConfig(\n        issuer   = \"https://auth.example.org/realms/lab\",\n        audience = \"https://mcp.example.org\",\n    ),\n    validator = JWKSValidator(\"https://auth.example.org/realms/lab/protocol/openid-connect/certs\"),\n)\n\nKeys are fetched lazily and re-fetched on unknown key ids (rotation), rate-limited to one fetch per refresh_interval_seconds (default 300) so attacker-supplied kid values cannot hammer the JWKS endpoint. The alg allowlist defaults to the RSA family (RS256/RS384/RS512) and rejects alg=none outright.\n\nClients send Authorization: Bearer <token>; unauthorized requests get 401/403 with an RFC 6750 WWW-Authenticate header, and discovery metadata is served at /.well-known/oauth-protected-resource (RFC 9728). Tool handlers can read the verified identity by accepting the request context: handler = (args, ctx) -> ... and using ctx.authenticated_user. Note: JWTValidator checks claims only (no signature verification); prefer JWKSValidator for tokens from external issuers, or the GitHub / introspection validators when tokens must be verified against an authority.","title":"Authentication (OAuth Resource Server)"},{"category":"section","location":"transports/#DNS-Rebinding-Protection-(Host/Origin-Validation)","page":"Transports","text":"A loopback-bound server without bearer auth automatically rejects requests whose Host or Origin header is neither local (localhost, 127.0.0.1, [::1]) nor allowlisted, with 403 Forbidden. This blocks DNS-rebinding attacks, where a malicious website resolves its own domain to 127.0.0.1 and drives your local server from the victim's browser. Legitimate local clients are unaffected — they send a loopback Host — and enabling auth disables the guard (a browser cannot attach the bearer token, and authenticated deployments commonly sit behind a reverse proxy that forwards a public Host).\n\nOpen specific holes with the allowlists:\n\ntransport = HttpTransport(\n    allowed_hosts = [\"mcp.example.org\"],       # extra Host hostnames (reverse proxy)\n    allowed_origins = [\n        \"http://localhost:3000\",\n        \"https://my-app.com\"                    # extra Origins (browser clients)\n    ]\n)","title":"DNS-Rebinding Protection (Host/Origin Validation)"},{"category":"section","location":"transports/#Session-Validation","page":"Transports","text":"Sessions provide security and state isolation:\n\n# Require valid sessions for all non-initialization requests\ntransport = HttpTransport(session_required = true)\n\n# Disable session requirement (less secure)\ntransport = HttpTransport(session_required = false)","title":"Session Validation"},{"category":"section","location":"transports/#Error-Handling","page":"Transports","text":"The HTTP transport returns appropriate HTTP status codes:\n\n200 OK - Successful requests with JSON response\n202 Accepted - Notification requests (no response body)\n400 Bad Request - Missing required session ID, malformed requests, and modern-era protocol violations (SEP-2243 header mismatch -32020, missing client capability -32021, unsupported version -32022)\n401 Unauthorized - Missing, malformed, or invalid bearer token (auth enabled), or a supplied session ID that does not match the session\n403 Forbidden - Authenticated but not permitted (missing scope or allowlist), or a request rejected by the DNS-rebinding Host/Origin guard\n404 Not Found - Unknown endpoints, and unknown methods on modern-era requests\n500 Internal Server Error - Server-side errors","title":"Error Handling"},{"category":"section","location":"transports/#Performance-Considerations","page":"Transports","text":"For production deployments:\n\nBinding: Use host = \"0.0.0.0\" to accept external connections\nPort Selection: Avoid common ports; use application-specific ports\nSession Management: Monitor session count and implement cleanup\nSSE Connections: Limit concurrent SSE streams per client\nOrigin Validation: Always configure allowed origins in production","title":"Performance Considerations"},{"category":"section","location":"transports/#Troubleshooting","page":"Transports","text":"","title":"Troubleshooting"},{"category":"section","location":"transports/#Connection-Issues","page":"Transports","text":"# Check if server is listening\nusing Sockets\n@assert isopen(connect(transport.host, transport.port))","title":"Connection Issues"},{"category":"section","location":"transports/#Session-Problems","page":"Transports","text":"Ensure Mcp-Session-Id header is included after initialization\nCheck that session ID contains only visible ASCII characters (0x21-0x7E)\nVerify server hasn't restarted (sessions are lost on restart)","title":"Session Problems"},{"category":"section","location":"transports/#Protocol-Version-Mismatches","page":"Transports","text":"The server accepts any supported version in the MCP-Protocol-Version header: the legacy versions (2025-11-25, 2025-06-18, 2025-03-26, 2024-11-05), which are negotiated during initialize, and the modern 2026-07-28, which arrives per-request in the params _meta and must also be asserted in the header under SEP-2243\nResponse headers echo the negotiated version after initialization\nA modern request whose header and body versions disagree is -32020/400, not a negotiation failure — check both before suspecting the server\nCheck server logs for protocol version negotiation messages","title":"Protocol Version Mismatches"},{"category":"section","location":"transports/#Migration-from-stdio","page":"Transports","text":"To migrate from stdio to HTTP transport:\n\n# Before (stdio)\nserver = mcp_server(name = \"my-server\", tools = [my_tool])\nstart!(server)\n\n# After (HTTP)\ntransport = HttpTransport(port = 3000)\nserver = mcp_server(name = \"my-server\", tools = [my_tool])\nserver.transport = transport\nModelContextProtocol.connect(transport)\nstart!(server)\n\nKey changes:\n\nCreate and configure HttpTransport\nSet server.transport before starting\nCall ModelContextProtocol.connect(transport) to start HTTP server\nUpdate client code to use HTTP requests with session management","title":"Migration from stdio"},{"category":"section","location":"transports/#Examples","page":"Transports","text":"See the examples/ directory for complete working examples:\n\nexamples/simple_http_server.jl - Simple HTTP server setup\nexamples/reg_dir_http.jl - HTTP server with directory auto-registration","title":"Examples"},{"category":"section","location":"#ModelContextProtocol.jl","page":"Home","text":"Julia implementation of the Model Context Protocol (MCP), enabling seamless integration between AI applications and external data sources, tools, and services.","title":"ModelContextProtocol.jl"},{"category":"section","location":"#Features","page":"Home","text":"✅ Dual-Era Protocol Support - the full 2026-07-28 modern era (stateless requests, server/discover, MRTR, the tasks extension, argument completion, subscriptions, header parameter mirroring, per-request logging) alongside classic sessions negotiated from 2025-11-25 back to 2024-11-05\n✅ Multiple Transports - stdio (default) and Streamable HTTP with Server-Sent Events and session management\n✅ All Content Types - text, images, audio, embedded resources, and resource_link references\n✅ Structured Tool Output - output_schema declarations with structuredContent results\n✅ Tool Annotations - behavioral hints (readOnlyHint, destructiveHint, …) for client trust decisions\n✅ Progress Notifications - long-running tools report progress through context-aware handlers\n✅ Background Tasks - the modern-era tasks extension (server-directed handoff, mid-task input, status notifications) plus legacy SEP-1686 tasks for 2025-11-25 sessions\n✅ Argument Completion - completion/complete suggestions for prompt arguments and template variables\n✅ OAuth Resource Server - bearer-token validation for HTTP (JWKS signature verification, GitHub tokens, JWT claims, RFC 7662 introspection) with RFC 9728 discovery\n✅ Logging Control - runtime logging/setLevel (legacy) and per-request logLevel opt-in (modern)\n✅ Auto-Registration - automatic component discovery from directory structure\n✅ Type-Safe - leverages Julia's type system for robust implementations\n\nNote: This is a server-side implementation. Legacy-era server-initiated elicitation/sampling (the modern era covers these via MRTR) and the OAuth Authorization Server (token issuance) are not implemented; SSE streams are not resumable via Last-Event-ID, and synchronous requests are served on a single serial loop (tasks move long tool calls off-loop).","title":"Features"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(\"ModelContextProtocol\")","title":"Installation"},{"category":"section","location":"#Quick-Start","page":"Home","text":"Create a simple MCP server with a tool:\n\nusing ModelContextProtocol\n\n# Create a server with a simple echo tool\nserver = mcp_server(\n    name = \"echo-server\",\n    version = \"1.0.0\",\n    tools = [\n        MCPTool(\n            name = \"echo\",\n            description = \"Echo back the input message\",\n            parameters = [\n                ToolParameter(\n                    name = \"message\",\n                    type = \"string\",\n                    description = \"Message to echo\",\n                    required = true\n                )\n            ],\n            handler = (params) -> TextContent(text = params[\"message\"])\n        )\n    ]\n)\n\n# Start the server (stdio transport by default)\nstart!(server)","title":"Quick Start"},{"category":"section","location":"#Using-HTTP-Transport","page":"Home","text":"For web-based integrations, use the HTTP transport:\n\n# Create server, then attach an HTTP transport\nserver = mcp_server(\n    name = \"http-server\",\n    version = \"1.0.0\",\n    tools = MCPTool[]  # Your tools here\n)\n\nserver.transport = HttpTransport(host = \"127.0.0.1\", port = 3000)\nconnect(server.transport)\nstart!(server)\n\n# Test with curl\n# curl -X POST http://127.0.0.1:3000/ \\\n#   -H \"Content-Type: application/json\" \\\n#   -d '{\"jsonrpc\":\"2.0\",\"method\":\"tools/list\",\"params\":{},\"id\":1}'","title":"Using HTTP Transport"},{"category":"section","location":"#Documentation-Structure","page":"Home","text":"Examples - Complete working examples and common patterns\nTools - Creating and using MCP tools\nResources - Managing data sources and subscriptions\nPrompts - Defining prompt templates for LLMs\nTransports - Transport options and configuration\nThe Modern Era - The 2026-07-28 stateless protocol surface\nAuto-Registration - Directory-based component organization\nClaude Desktop Integration - Integration with Claude Desktop\nAuthentication - OAuth Resource Server setup for HTTP transport\nDeployment - Exposing a server to remote clients\nAPI Reference - Complete API documentation","title":"Documentation Structure"},{"category":"section","location":"#Protocol-Compliance","page":"Home","text":"ModelContextProtocol.jl serves the full MCP 2026-07-28 modern era statelessly, alongside classic sessions negotiated at 2025-11-25 down through 2025-06-18 and 2025-03-26 to 2024-11-05. This includes:\n\nJSON-RPC 2.0 message protocol (batching rejected per spec)\nTool discovery and invocation, structured output, annotations, _meta\nResource management with subscriptions and resource_link references\nPrompt templates with arguments and media content; argument completion\nProgress notifications (notifications/progress) and logging (logging/setLevel for sessions, per-request logLevel in the modern era)\nSession management and OAuth Resource Server authentication for HTTP transport\nModern-era tasks, MRTR, subscriptions/listen, and SEP-2243 header validation","title":"Protocol Compliance"},{"category":"section","location":"#Basic-Concepts","page":"Home","text":"","title":"Basic Concepts"},{"category":"section","location":"#Tools","page":"Home","text":"Tools are functions that can be invoked by the LLM:\n\ntool = MCPTool(\n    name = \"calculate\",\n    description = \"Perform basic arithmetic\",\n    parameters = [\n        ToolParameter(name = \"a\", description = \"First operand\", type = \"number\", required = true),\n        ToolParameter(name = \"b\", description = \"Second operand\", type = \"number\", required = true),\n        ToolParameter(name = \"op\", description = \"Operator: +, -, * or /\", type = \"string\", required = true)\n    ],\n    handler = function(params)\n        a, b = params[\"a\"], params[\"b\"]\n        result = if params[\"op\"] == \"+\"\n            a + b\n        elseif params[\"op\"] == \"-\"\n            a - b\n        elseif params[\"op\"] == \"*\"\n            a * b\n        elseif params[\"op\"] == \"/\"\n            a / b\n        else\n            error(\"Unknown operation\")\n        end\n        return TextContent(text = string(result))\n    end\n)","title":"Tools"},{"category":"section","location":"#Resources","page":"Home","text":"Resources provide data access to the LLM:\n\nresource = MCPResource(\n    uri = \"file:///data/config.json\",\n    name = \"Application Config\",\n    description = \"Current application configuration\",\n    mime_type = \"application/json\",\n    data_provider = () -> JSON3.read(read(\"config.json\", String), Dict{String,Any})\n)","title":"Resources"},{"category":"section","location":"#Prompts","page":"Home","text":"Prompts are templates for generating conversations:\n\nprompt = MCPPrompt(\n    name = \"code_review\",\n    description = \"Request a code review\",\n    arguments = [\n        PromptArgument(\n            name = \"language\",\n            description = \"Programming language\",\n            required = true\n        )\n    ],\n    messages = [\n        PromptMessage(\n            content = TextContent(\n                text = \"Please review this {language} code for best practices.\"\n            )\n        )\n    ]\n)\n\nTemplate placeholders like {language} are substituted from the arguments supplied in prompts/get.","title":"Prompts"},{"category":"section","location":"#Testing-Your-Server","page":"Home","text":"","title":"Testing Your Server"},{"category":"section","location":"#With-MCP-Inspector","page":"Home","text":"Test your server using the official MCP Inspector:\n\n# For stdio transport (Inspector spawns the server command directly)\nnpx @modelcontextprotocol/inspector julia --project=. server.jl\n\n# For HTTP transport\nnpx @modelcontextprotocol/inspector http://127.0.0.1:3000/\n\n# Quick smoke test without the browser UI (CLI mode)\nnpx @modelcontextprotocol/inspector --cli julia --project=. server.jl --method tools/list","title":"With MCP Inspector"},{"category":"section","location":"#With-curl-(HTTP-only)","page":"Home","text":"# Initialize connection\ncurl -X POST http://127.0.0.1:3000/ \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"jsonrpc\":\"2.0\",\"method\":\"initialize\",\"params\":{\"protocolVersion\":\"2025-11-25\",\"capabilities\":{},\"clientInfo\":{\"name\":\"test\",\"version\":\"1.0\"}},\"id\":1}'\n\n# List available tools\ncurl -X POST http://127.0.0.1:3000/ \\\n  -H \"Content-Type: application/json\" \\\n  -H \"Mcp-Session-Id: <session-id-from-init>\" \\\n  -d '{\"jsonrpc\":\"2.0\",\"method\":\"tools/list\",\"params\":{},\"id\":2}'","title":"With curl (HTTP only)"},{"category":"section","location":"#Next-Steps","page":"Home","text":"Explore the Examples for complete working implementations\nRead the User Guide to understand each component type\nCheck the API Reference for detailed function documentation\nSet up Claude Desktop Integration for real-world usage","title":"Next Steps"},{"category":"section","location":"#Contributing","page":"Home","text":"ModelContextProtocol.jl is part of the JuliaSMLM organization. Contributions are welcome! Please see our GitHub repository for issues and pull requests.","title":"Contributing"},{"category":"section","location":"#License","page":"Home","text":"This project is licensed under the MIT License.","title":"License"}]
}
